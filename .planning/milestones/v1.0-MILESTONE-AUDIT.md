---
milestone: v1.0
audited: 2026-06-20
status: passed
supersedes: "2026-05-29 audit (covered 8 phases; v1.0 subsequently grew to 12)"
scores:
  requirements: 19/19
  phases: 12/12
  integration: 9/9 segment builders wired (0 orphaned, 0 missing)
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-cutting
    items:
      - "pyproject.toml version (0.1.0) still lags _APP_VERSION (0.2.0) in claude-statusline.py:629. NWS User-Agent uses the script constant at runtime, so no functional/ToS break — metadata inconsistency only. Carried over unfixed from the 2026-05-29 audit."
      - "SUMMARY.md `requirements-completed` frontmatter is empty for 15 of 19 requirements (only 01-01, 01-03, 02.2-01, 02.2-02 populate it). VERIFICATION.md tables and REQUIREMENTS.md checkboxes both independently confirm all 19 satisfied — tracking-field gap only."
      - "REQUIREMENTS.md footer says 'Last updated: 2026-05-28 after Plan 01-01' though all 19 checkboxes are [x] and the traceability table reads Done/Complete — stale footer, content correct. Traceability table also only maps RUN/TOP/CTX/LIM/FMT/CFG→Phase 1 and WX→Phase 2; the 10 enhancement phases (02.1, 02.2, 3, 03.1, 4, 5, 05.1, 6, 7, 07.1) carry no v1 REQ-IDs (CONTEXT-driven / ENH-04) and are not in the table."
  - phase: testing-environment
    items:
      - "60 astral/requests-dependent weather/sun/moon tests self-skip under system python3 (`_WEATHER_OK is False`). The weather code path (WX-01..06) is exercised only via the installed venv interpreter (~/.claude/claude-statusline/.venv), which main() re-execs into at runtime. Coverage is real but environment-gated; no CI on this machine runs the weather path under system python."
  - phase: 02-weather-layer
    items:
      - "WX-05 requirement text says weather cache '~15min' but the code default is weather_ttl=600 (10 min) at claude-statusline.py:152, matching PROJECT.md/ROADMAP.md. Requirement prose is the outlier; behavior (cached, render never blocks) satisfies WX-05 either way."
nyquist:
  status: disabled
  note: "workflow.nyquist_validation = false — Nyquist compliance discovery skipped per config. No *-VALIDATION.md files in any phase (consistent)."
---

# Milestone v1.0 — Audit Report (refresh)

**Milestone:** v1.0 (claude-statusline)
**Audited:** 2026-06-20
**Status:** ✅ PASSED
**Supersedes:** the 2026-05-29 audit, which certified 8 phases. v1.0 subsequently grew to **12 phases** (Phases 6, 7, 07.1 shipped in June). This refresh re-audits all 12 before milestone closure.

**Definition of Done (from ROADMAP.md):** A complete, working two-line statusline — Phase 1 wires everything available from stdin (project, model+thinking, context bar, rate limits, colors, config); Phase 2 layers in NWS weather with caching, local sun events, and alert override. Each phase produces an installable, visible bar. The 10 subsequent phases are CONTEXT-driven enhancements layered on that core without new v1 REQ-IDs.

---

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | **19/19** satisfied | 3-source cross-reference (VERIFICATION + SUMMARY + traceability) |
| Phases | **12/12** verified | Every phase carries a `status: passed`/`verified` VERIFICATION.md |
| Integration | **0 orphaned / 0 missing** | 9 segment builders all reach output via `render_top_line`/`render_bottom_line` |
| E2E Flows | **5/5** | Standard render + 4 degradation paths exit 0 |
| Test suite | **727 passed, 60 skipped, 0 failed** | 60 skips are astral/requests env gates (documented) |

---

## Phases (12/12 verified)

| Phase | Title | VERIFICATION status | Notes |
|-------|-------|---------------------|-------|
| 1 | Core Statusline | passed | Anti-pattern scan clean; CR-01/WR-01 fixed |
| 2 | Weather Layer | passed | All 6 WX reqs verified; render never does inline network call |
| 02.1 | Nerd Font icon set | passed | Dual emoji/nerd resolver; cmap guard test |
| 02.2 | Watch/Warning/Advisory | passed | (was gaps_found → closed) VTEC classification + tally |
| 3 | Bar-fill presets | passed | bar_style shade/solid/solid-dim/gradient; human-verify gate approved |
| 03.1 | Default bar test drift | passed | Test-isolation fix; production unchanged; the 2 prior failures resolved |
| 4 | git info + worktree | passed | Timeout-guarded segment; no regressions |
| 5 | GSD status info | passed | CR-01 milestone-complete-not-fall-through fix confirmed |
| 05.1 | GSD test hermeticity | passed | 9 wall-clock-leak tests fixed; guard test added |
| 6 | Claude Status indicator | passed | Human visual items confirmed 2026-06-16 |
| 7 | Filter/dismiss incidents | verified | Human UAT 3/3; dismiss-on-refresh issue closed by gap plan 07-04 |
| 07.1 | Resolved vs unresolved | passed | All 14 must-haves; CR-01/CR-02 + 3 warnings closed |

No phase is missing VERIFICATION.md. No unresolved debt markers: every `TBD` match across the codebase is documented as the GSD roadmap-placeholder *parser* string-matching, not an incomplete-implementation marker. No stubs on any render path.

---

## Requirements Coverage (3-source cross-reference)

All 19 v1 requirements are mapped (RUN/TOP/CTX/LIM/FMT/CFG → Phase 1; WX-01..06 → Phase 2). For each, three independent sources agree:

- **REQUIREMENTS.md:** all 19 `[x]`, traceability rows read Done/Complete.
- **VERIFICATION.md:** Phase 1 and Phase 2 carry `status: passed` with explicit per-requirement SATISFIED evidence.
- **Integration checker:** all 19 traced WIRED end-to-end from `main()` to a visible segment (see Integration below).

**SUMMARY frontmatter caveat:** `requirements-completed` is populated for only 4 of 19. Per the strict status matrix, "passed + missing frontmatter" → *partial (verify manually)*. **Manually verified:** each requirement carries an explicit SATISFIED row with code evidence in its phase VERIFICATION.md, and the integration checker independently confirmed each is wired into the render path. The empty frontmatter is a tracking omission, not an implementation gap. Classified **satisfied**.

**Orphan check:** No requirement appears in the traceability table but absent from VERIFICATION — zero orphans.

---

## Integration (cross-phase wiring)

`main()` (claude-statusline.py:4063) → `render_top_line` (composes 5 segments) + `render_bottom_line` (composes 4 segments). **Zero orphaned builders, zero missing connections.**

- **Top line** (single-space join, None-filtered): `_project_segment` (TOP-01) · `_git_segment` (Ph4) · `_gsd_segment` (Ph5) · `_model_segment` (TOP-02/03) · `_weather_segment` (WX-01..06, Ph02.1/02.2)
- **Bottom line** (triple-space join, None-filtered): `_context_segment` (CTX-01/02, Ph3 bar_style) · `_rate_segment` ×2 (LIM-01..04) · `_claude_status_segment` (Ph6/7/07.1)

LIM-03/04 reconfirmed live: weekly at 95% shows cross-day `Sat 5:00am` reset; 5h at 50% (green) shows no reset. FMT-01 bands (green<70 / yellow 70–90 / red>90) applied to context, 5h, weekly.

---

## E2E Flows (5/5, all exit 0)

1. Standard full stdin → both lines, every segment live (incl. active-alert override + precip).
2. Empty stdin → exits 0, top line still renders (RUN-02).
3. Malformed JSON → exits 0.
4. `{}` / wrong-typed fields → exits 0, graceful per-segment omission.
5. Emoji `icon_set` fallback path → renders through both render functions.

**Broken flows: 0.**

> Non-blocking observation: the GSD segment (Phase 5) currently *omits* in this repo because live `.planning/` infers `lifecycle=idle` (milestone complete, no active/next plan) — the intended silent-omit path, not a wiring break. The builder is called from `render_top_line` and renders for active/done states (covered by passing builder tests). Phase 5 has no v1 REQ-IDs mapped, so this affects no requirement.

---

## Tech Debt (non-blocking — none affect the definition of done)

1. **Version lag** — `pyproject.toml` 0.1.0 vs `_APP_VERSION` 0.2.0. Metadata only (runtime uses the script constant for the NWS User-Agent). Unfixed since the May audit.
2. **Empty SUMMARY `requirements-completed`** for 15/19 — tracking field; coverage independently confirmed twice over.
3. **Stale REQUIREMENTS.md footer** + traceability table predates the 10 enhancement phases (those carry no v1 REQ-IDs by design).
4. **60 weather tests self-skip under system python3** — covered only via the venv interpreter that `main()` re-execs into; no system-python CI exercises WX-01..06.
5. **WX-05 text/code drift** — requirement says "~15min", code defaults to 10 min (matching PROJECT/ROADMAP). Behavior satisfies WX-05.

None are blockers. All are tracking/metadata; an optional pre-close cleanup could fix #1 (bump pyproject to 0.2.0) and #3 (refresh the footer) in minutes.

---

## Tech-Debt Resolution (Phase 10)

Resolved 2026-06-25 during Phase 10 (Tech-Debt Cleanup, plan 10-01). All five items from the audit `tech_debt` block above are now closed. No silent gaps remain.

- **DEBT-01 (version lag):** `pyproject.toml` bumped from `0.1.0` to `0.2.0` in commit cf19e9d — now byte-equal to `_APP_VERSION = "0.2.0"` in `claude-statusline.py:878`.

- **DEBT-02 (empty SUMMARY `requirements-completed`):** RETIRED — not backfilled. The v1.0 phase SUMMARY files that lacked the field were cleared in commit a413558 ("chore: clear v1.0 phase directories for v1.1 milestone") and exist only in git history. Coverage was independently confirmed by VERIFICATION.md + the REQUIREMENTS.md traceability table (3-source cross-reference, see audit above). Resurrecting cleared files to add a redundant tracking field is higher-risk and lower-value than this retirement note; v1.1 SUMMARYs (Phases 08, 09) already populate `requirements_completed` going forward. No implementation gap exists — the field was a tracking omission, not a coverage gap.

- **DEBT-03 (stale REQUIREMENTS.md footer/traceability):** Resolved in commit 5202469 — LINK-01..03 (Phase 9 complete) and DEBT-01..05 (Phase 10 this plan) all flipped to Complete in the v1.1 REQUIREMENTS.md traceability table and requirement list; footer updated to `Last updated: 2026-06-25 after Phase 10 tech-debt cleanup`.

- **DEBT-04 (weather tests under system python3):** Verified — no fix required. Command run: `/usr/bin/pytest tests/test_weather_sun.py tests/test_weather_fetch.py tests/test_weather_cache.py tests/test_weather_alerts.py tests/test_weather_links.py tests/test_weather_link_target.py -q` → **228 passed, 47 skipped, 0 errors, exit 0**. The 47 skips are `_ASTRAL_OK`/`_WEATHER_OK` guards: system python3 has `requests` but not `astral`, so astral-dependent branches skip with an explicit reason string. No empty `skipTest("")` calls exist (`grep -rn 'skipTest("")' tests/test_weather*.py` returns nothing). Full pass-coverage runs under the `.venv` interpreter that `main()` re-execs into at runtime.

- **DEBT-05 (WX-05 TTL text/code drift):** Resolved in commit cf19e9d — corrected the WX-05 line in `v1.0-REQUIREMENTS.md` from "Weather (~15min)" to "Weather (~10min)" to match the canonical code default `weather_ttl=600` (10 min). No code change made — the 10-min default is canonical (fresher weather; matches PROJECT.md/ROADMAP.md).

---

## Verdict

✅ **PASSED.** All 19 v1.0 requirements satisfied and wired end-to-end; all 12 phases verified; cross-phase integration clean (0 orphaned / 0 missing); 5/5 E2E flows exit 0; full suite green (727 passed, 0 failed). Remaining items are tracking-only tech debt with no functional impact. Milestone v1.0 meets its definition of done and is ready to complete.

---
*Audited 2026-06-20. Supersedes the 2026-05-29 8-phase audit (preserved in git history).*
