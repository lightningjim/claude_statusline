# Phase 02.2: Differentiate between watches, warnings, and advisories - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-06
**Phase:** 02.2-differentiate-between-watches-warnings-and-advisories
**Areas discussed:** Classification signal, Visual distinction (glyph + color + intensity), Selection priority, Urgency/certainty

---

## Classification signal

| Option | Description | Selected |
|--------|-------------|----------|
| VTEC sig + name fallback | Read VTEC significance letter (W/A/Y/S) from properties.parameters.VTEC; fall back to event-name suffix for non-VTEC products | ✓ |
| Event-name suffix only | Classify purely by trailing word of properties.event | |
| VTEC only (strict) | VTEC only; non-VTEC products drop to other/statement bucket | |

**User's choice:** VTEC significance primary, event-name fallback.
**Notes:** Authoritative source preferred; fallback covers non-VTEC products (Air Quality Alert, Special Weather Statement, etc.). Four classes: Warning/Watch/Advisory/Statement-Other.

---

## Visual distinction — Glyph

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct glyph per class | Warning/Watch/Advisory each get their own glyph (nerd primary, emoji fallback), replacing single ⚠ | ✓ |
| Single ⚠, color only | Keep one glyph; class via color + event text | |
| Per-class glyph + drop class word | Distinct glyph AND strip redundant class word from event text | |

**User's choice:** Distinct glyph per class.
**Notes:** Must follow Phase 02.1 icon_set toggle + installed-font cmap guard. Class word NOT dropped (full NWS event name retained).

---

## Visual distinction — Color

| Option | Description | Selected |
|--------|-------------|----------|
| Class-driven (W=red, Watch=yel, Adv=cyan) | Color encodes action level; replaces severity-driven alert color | ✓ |
| Class base + severity escalation | Class sets base; Extreme/Severe intensifies | |
| Keep severity-driven color | Color stays mapped to CAP severity | |

**User's choice:** Class-driven hue — Warning=red, Watch=yellow, Advisory=cyan.
**Notes:** Decouples color from noisy CAP severity.

---

## Visual distinction — Intensity (urgency/certainty modulation)

| Option | Description | Selected |
|--------|-------------|----------|
| Intensity axis (bold→normal→dim) | Hue=class; brightness=immediacy. Immediate+Observed→bold; Expected/Likely→normal; Future/Possible→dim (ANSI bold + DIM) | ✓ |
| Bold flag only | Bold when urgency=Immediate OR certainty=Observed; else normal | |
| Decide mechanism in planning | Lock intent, planner picks ANSI mechanism | |

**User's choice:** Intensity axis (three bands).
**Notes:** Exact urgency/certainty → band threshold table left to Claude's discretion; three-band model locked.

---

## Selection priority

| Option | Description | Selected |
|--------|-------------|----------|
| Class-first, then severity | Warning > Watch > Advisory > Statement; severity breaks ties | ✓ (refined below) |
| Composite (class major, sev minor) | Weighted rank across factors | |
| Severity-first (current) | Highest CAP severity wins | |

**Follow-up — composite tie-break order**

| Option | Description | Selected |
|--------|-------------|----------|
| Class → severity → urgency → certainty | Class dominates; severity, then urgency, then certainty break ties | ✓ |
| Class → urgency → severity → certainty | Immediacy outranks CAP severity within a class | |
| Decide order in planning | Lock that all four feed the rank; planner finalizes precedence | |

**User's choice:** Class-first; tie-break severity → urgency → certainty.

---

## Urgency/certainty fold-in (scope)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep scope tight — class only | Defer urgency/certainty again | |
| Urgency as tiebreaker only | Urgency breaks ties only; no new visual | |
| Full fold-in to color/priority | Urgency/certainty shape color intensity and ranking | ✓ |

**User's choice:** Full fold-in.
**Notes:** Broadens scope vs. pure W/W/A, but user (visionary + meteorologist) deliberately chose richer fidelity. Manifested concretely as the intensity axis (D-06) + composite rank (D-07).

---

## Multi-alert "+N" display

| Option | Description | Selected |
|--------|-------------|----------|
| Keep simple +N count | '+N' = N other active alerts (as D2-11) | |
| +N of same-or-higher class only | Count only equal/higher-class alerts | |
| Per-class tally | Counts grouped by class glyph | ✓ |

**User's choice:** Per-class tally.
**Notes:** Richest option; must stay width-conscious on the top line.

---

## Claude's Discretion

- Exact per-class glyph codepoints (nerd) + emoji fallbacks, subject to cmap guard.
- Urgency/certainty → {bold, normal, dim} threshold table.
- VTEC parsing mechanism (significance = 5th `.`-delimited field; choose relevant VTEC when multiple).
- Per-class tally rendering/abbreviation under width pressure.

## Deferred Ideas

- Stripping the redundant class word from event text (offered, not chosen).
- Extra weather variables (dewpoint/wind/apparent temp) — v2.
- Multi-location / auto-geolocation (ENH-03).
