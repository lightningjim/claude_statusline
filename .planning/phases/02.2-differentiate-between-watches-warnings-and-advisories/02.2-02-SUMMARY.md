---
phase: 02.2-differentiate-between-watches-warnings-and-advisories
plan: 02
subsystem: weather
tags: [nws, alerts, vtec, ansi, nerd-font, tally, sanitization, render]

requires:
  - phase: 02.2-differentiate-between-watches-warnings-and-advisories
    plan: 01
    provides: _classify_alert_class, _alert_color(alert), select_alert returning (best, list), _ALERT_CLASS_GLYPHS_NERD/EMOJI, _WI_ALERT_*, BOLD

provides:
  - _build_alert_tally(remaining, icon_set) helper — per-class glyph+count tally in Warning>Watch>Advisory>Statement order
  - Reshaped _weather_segment alert-override block: class glyph + class hue + per-class tally + sanitized event text

affects:
  - _weather_segment trailing alert detail (end-user visible: per-class glyph, hue, tally)

tech-stack:
  added: []
  patterns:
    - "Per-class tally: group by _classify_alert_class, iterate fixed order tuple, skip zeros, join glyph+count with space"
    - "Inline event-text sanitization: keep printable + space, strip \\x1b, truncate to 64 (mirrors _sanitize_label logic)"
    - "icon_set dispatch for class glyph: nerd -> _ALERT_CLASS_GLYPHS_NERD.get(cls, _WI_ALERT_STATEMENT), else emoji table"
    - "select_alert (best, remaining_alerts) unpacking — remaining is now a LIST for per-class tally"

key-files:
  created: []
  modified:
    - claude-statusline.py
    - tests/test_weather_alerts.py

key-decisions:
  - "_build_alert_tally placed immediately after _alert_color — logically grouped with classification/color helpers"
  - "Inline event-text sanitization (copy of _sanitize_label logic) with maxlen=64 — not calling _sanitize_label which uses GSD-specific constant"
  - "Per-class tally uses two spaces (not one) before tally string per PATTERNS.md Section 7 render-block spec"
  - "_weather_segment docstring updated to reflect D-04/D-05/D-06/D-08 render contract"
  - "TestWeatherSegmentAlertOverrideV2 adds new class with its own emoji-pinned cfg fixture rather than extending the existing helper (cleaner isolation)"

requirements-completed: [WX-04]

duration: 20min
completed: 2026-06-07
---

# Phase 02.2 Plan 02: Wire Classification Layer into _weather_segment Render Summary

**Per-class glyph + class hue + urgency/certainty intensity + per-class tally with sanitized event text — the meteorologist-facing W/W/A render payoff wired into the live trailing-detail path**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-06-07
- **Completed:** 2026-06-07
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `_build_alert_tally(remaining, icon_set)`: groups remaining alerts by class via `_classify_alert_class`, iterates fixed Warning>Watch>Advisory>Statement/Other order, omits zero-count classes, resolves glyph via icon_set toggle (nerd/_ALERT_CLASS_GLYPHS_NERD / emoji/_ALERT_CLASS_GLYPHS_EMOJI), joins with space; wrapped in try/except → "" (never raises, D-10)
- `_weather_segment` alert-override block reshaped: unpacks `(best, remaining_alerts)` from `select_alert`, calls `_alert_color(best)` (class-hue + urgency intensity), resolves per-class glyph via icon_set, inline-sanitizes event text (strip `\x1b` + control chars, truncate to 64), builds per-class tally via `_build_alert_tally`, falls through to sun event on any exception (D2-12)
- Replaced `test_multiple_alerts_plus_n_suffix` with `test_multiple_alerts_per_class_tally`: asserts Watch glyph+2 tally; assertNotIn("+2")
- Replaced `test_warning_glyph_in_alert_detail` with `test_warning_class_glyph_in_alert_detail`: asserts emoji Warning glyph; assertNotIn("⚠")
- Added 6 new tests in `TestWeatherSegmentAlertOverrideV2`: advisory-is-cyan, watch-is-yellow+glyph, Tstm+Flood e2e class selection, sanitization (no raw ESC), malformed fall-through (no crash), Immediate+Observed BOLD+RED+Warning-glyph
- Total test count: 480 passed, 59 skipped (all skips are `_WEATHER_OK=False` — astral not in base env)

## Task Commits

1. **Task 1: _build_alert_tally + TestBuildAlertTally (GREEN)** - `be26b7f` (feat)
2. **Task 2: Reshape _weather_segment alert-override + update/add tests** - `80832f0` (feat)

## Files Created/Modified

- `claude-statusline.py` — `_build_alert_tally` function added after `_alert_color`; `_weather_segment` alert-override block reshaped; docstring updated
- `tests/test_weather_alerts.py` — `import re` added; `test_multiple_alerts_plus_n_suffix` replaced; `test_warning_glyph_in_alert_detail` replaced; `TestWeatherSegmentAlertOverrideV2` class added with 6 new tests

## Decisions Made

- `_build_alert_tally` placed directly after `_alert_color` — all classification/color/tally helpers grouped together
- Inline sanitization (not `_sanitize_label` call) because `_sanitize_label` uses `_GSD_LABEL_MAXLEN` which is GSD-framework-specific; inline pattern with `maxlen=64` is the correct approach per PATTERNS.md
- `TestWeatherSegmentAlertOverrideV2` added as a new class rather than extending the existing one — cleaner separation and avoids touching the stable skipped-test suite
- `import re` added to test file (used for ANSI stripping in sanitization test)

## Deviations from Plan

### None

Plan executed exactly as written. Both mandatory test replacements and all 6 new tests implemented. Verification criteria met:
- `grep -n "_alert_color(severity)" claude-statusline.py` returns nothing
- `grep -c 'assertIn("+2"' tests/test_weather_alerts.py` is 0
- `⚠ ` appears only in the updated docstring (not in executable render path)
- Full suite green (480 passed, 59 skipped — all skips are `_WEATHER_OK=False`)

## Known Stubs

None — all render paths are wired to live classification/color/tally helpers. The `_WEATHER_OK=False` skips reflect an environment constraint (astral not installed), not stubs in the implementation.

## Threat Surface Scan

T-02.2-04 mitigated: inline sanitization strips `\x1b` and all non-printable chars from NWS event text, truncates to 64 chars, covered by `test_event_text_sanitized_no_esc_byte`.

T-02.2-05 mitigated: event truncated to 64 chars; tally is at most 4 glyph+count pairs (compact).

T-02.2-06 mitigated: `select_alert`/`_classify_alert_class`/`_alert_color`/`_build_alert_tally` all never-raise; outer try/except in `_weather_segment` falls through to sun event on any failure; covered by `test_malformed_alert_override_falls_through_to_sun`.

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- `claude-statusline.py` imports cleanly: True
- `_build_alert_tally` defined and callable: True
- `_weather_segment` alert-override block uses `_alert_color(best)`: True
- `assertIn("+2"` count in test file: 0
- `assertIn("⚠"` old form replaced: confirmed (assertNotIn instead)
- Commits be26b7f and 80832f0 in git log: True
- Full suite 480 passed, 59 skipped: True

## Next Phase Readiness

Phase 02.2 is complete. The trailing alert detail now renders:
- Per-class glyph (nerd or emoji) via icon_set toggle (D-04)
- Class hue: Warning=RED, Watch=YELLOW, Advisory=CYAN, Statement/Other=RESET (D-05)
- Urgency/certainty intensity: Immediate+Observed=BOLD, Future/Possible=DIM (D-06)
- Class-first selection: Warning beats co-active Watch (D-07)
- Per-class tally for remainder (not flat +N) (D-08)
- Sanitized + length-bounded event text (T-02.2-04/05)
- Malformed-alert fall-through to sun event preserved (T-02.2-06, RUN-01/02)

---
*Phase: 02.2-differentiate-between-watches-warnings-and-advisories*
*Completed: 2026-06-07*
