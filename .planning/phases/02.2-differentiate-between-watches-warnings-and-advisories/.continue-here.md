---
context: phase
phase: 02.2-differentiate-between-watches-warnings-and-advisories
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-06-07T05:29:16.743Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Committing RED partial work as done | Plan 02.2-02 Task 1's failing tests (`TestBuildAlertTally`) are written but `_build_alert_tally` is not yet implemented. Committing this as a normal task commit would land a RED suite. | blocking | Before resuming, either implement `_build_alert_tally` to GREEN first, OR `git checkout tests/test_weather_alerts.py` to reset. Do not commit while `pytest tests/test_weather_alerts.py` reports failures. |

<current_state>
Phase 02.2 (inserted, urgent) — differentiate NWS watches/warnings/advisories in the top-line alert detail.

**Wave 1 / Plan 02.2-01: COMPLETE and committed** (4 commits, SUMMARY written, 162 tests passed).
Shipped the pure classification/color/selection layer in `claude-statusline.py`:
- `_classify_alert_class(alert)` — VTEC significance (W/A/Y/S) primary, event-name suffix fallback, never-raise → Statement/Other
- `_alert_intensity(alert)` — urgency+certainty → bold / normal / dim
- `_alert_color(alert)` — class-driven hue (Warning=red, Watch=yellow, Advisory=cyan) + intensity
- `select_alert(...)` — class-first ranking, returns `(best, remaining_list)` (was `(best, int)`)
- `_ALERT_CLASS_GLYPHS_NERD` / `_ALERT_CLASS_GLYPHS_EMOJI`, `_ALERT_CLASS_RANK`, `BOLD` constant (cmap-guarded glyphs)

**Wave 2 / Plan 02.2-02: PARTIALLY STARTED, NOT committed.** The executor was stopped mid-Task-1.
It appended 8 RED tests (`TestBuildAlertTally`) to `tests/test_weather_alerts.py` (uncommitted) but did NOT
implement `_build_alert_tally` in `claude-statusline.py`. Suite is currently RED: 8 failed.
`claude-statusline.py` was NOT modified by Wave 2.
</current_state>

<completed_work>
- Plan 02.2-01 (Wave 1): DONE — all 3 tasks committed (2e62a3c, 1ff6d5f, ef3c040, d5fc5ac)
- Plan 02.2-02 Task 1: RED tests written (uncommitted), implementation not started
</completed_work>

<remaining_work>
- Plan 02.2-02 Task 1: implement `_build_alert_tally(remaining_alerts, icon_set)` in `claude-statusline.py`
  to satisfy the 8 `TestBuildAlertTally` tests (group remaining alerts by class glyph in Warning→Watch→Advisory
  order, count per class, omit zero-count classes, never raise). Then commit (GREEN).
- Plan 02.2-02 Task 2: wire `_weather_segment`'s trailing-detail render block —
  replace the single warning glyph with the per-class glyph (icon_set-aware via the Plan-01 tables),
  color the primary via `_alert_color(best)` (class hue + intensity), sanitize + width-bound the NWS
  `event` text, and render the remainder as the per-class tally instead of flat `+N`.
  Update `test_warning_glyph_in_alert_detail` (drop `assertIn("⚠", ...)`) and
  `test_multiple_alerts_plus_n_suffix` (drop `assertIn("+2", ...)`) per the plan.
- Then: phase verification (gsd-verifier), code-review gate, mark-complete.
</remaining_work>

<decisions_made>
- Ran executors sequentially on the main tree (no worktree isolation) even though `workflow.use_worktrees=true`:
  both plans are single-plan waves running sequentially (Plan 02 depends on 01 and shares files), so worktrees
  add merge-back risk for zero parallelism gain.
- Phase is 02.2 (not 2.1 as typed): 02.1 (Nerd Font icon set) already existed, so the SDK assigned the next decimal.
- Milestone v1.0 was reopened (completed → in-progress) to accommodate this inserted phase, per user choice at insert.
- All CONTEXT decisions D-01..D-08 are covered by the two plans (verified by the plan-checker, 2 iterations).
</decisions_made>

<blockers>
- Working tree is RED (8 failing `TestBuildAlertTally` tests, uncommitted) because `_build_alert_tally` isn't
  implemented yet. This is the expected TDD RED state for Plan 02.2-02 Task 1, not a defect. See the
  blocking anti-pattern above before committing anything.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02.2-differentiate-between-watches-warnings-and-advisories/02.2-02-PLAN.md` — the plan being executed (Tasks 1 & 2)
2. `.planning/phases/02.2-differentiate-between-watches-warnings-and-advisories/02.2-01-SUMMARY.md` — the Wave-1 layer Plan 02 consumes (exact function signatures)
3. `.planning/phases/02.2-differentiate-between-watches-warnings-and-advisories/02.2-PATTERNS.md` — render-block analog, icon_set dispatch, inline sanitization pattern
4. `.planning/phases/02.2-differentiate-between-watches-warnings-and-advisories/02.2-CONTEXT.md` — locked decisions D-01..D-08

## Infrastructure State
- No running services. Single-file Python project; tests via `python -m pytest tests/ -q`.
- Uncommitted: `tests/test_weather_alerts.py` (+110 lines, Plan 02 Task 1 RED tests), `.planning/config.json` (chain flag toggled false — not real work).

<context>
Plan 02.2-02 is the render-wiring half of the phase. The hard part (classification, color, selection)
is done and committed in Wave 1. Task 1 (`_build_alert_tally`) is pure and testable; the RED tests are
already written and pin the contract (group-by-class, fixed Warning→Watch→Advisory order, omit empties,
never raise). Task 2 is the single edit site in `_weather_segment`'s trailing-detail block.
</context>

<next_action>
Re-run `/gsd:execute-phase 02.2`. discover_plans will skip the completed 02.2-01 and resume at 02.2-02.
The executor should: (1) confirm/keep the RED tests, implement `_build_alert_tally` to GREEN, commit;
(2) do Task 2 render wiring + test updates, commit; then verification proceeds.
If you'd rather start Plan 02 clean, first run `git checkout tests/test_weather_alerts.py` to drop the
partial RED tests, then `/gsd:execute-phase 02.2`.
</next_action>
