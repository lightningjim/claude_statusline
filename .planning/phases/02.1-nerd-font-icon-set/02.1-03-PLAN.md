---
phase: 02.1-nerd-font-icon-set
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - claude-statusline.py
  - tests/test_nerd_icons.py
  - tests/test_weather_sun.py
  - tests/test_weather_alerts.py
autonomous: false
requirements: [ENH-04]
must_haves:
  truths:
    - "With icon_set=\"nerd\", the sun event uses wi-sunrise / wi-sunset glyphs"
    - "With icon_set=\"nerd\", the thinking indicator uses a Nerd Font glyph when thinking.enabled is true"
    - "With icon_set=\"nerd\", the 5h and weekly rate-limit segments use Nerd Font glyphs"
    - "With icon_set=\"emoji\", sun (🌅/🌇), thinking (💭) and rate (⏳/🗓) glyphs all revert to Phase 2 emoji"
    - "A single icon_set switch flips all four segments together — never a half-nerd/half-emoji bar (D-07)"
    - "The bottom-line green/yellow/red usage-threshold coloring is unchanged by the glyph swaps (D-08)"
    - "Any glyph-selection failure degrades to the emoji glyph or omits the segment without crashing"
    - "Pre-existing emoji-asserting sun/alert tests stay green by pinning their cfg to icon_set=\"emoji\" (no Phase 2 regression)"
  artifacts:
    - path: "claude-statusline.py"
      provides: "icon_set-branched glyphs in _sun_segment, _model_segment, and the render_bottom_line rate callers; icon_set threaded from cfg"
      contains: "icon_set"
    - path: "tests/test_nerd_icons.py"
      provides: "Sun/thinking/rate glyph tests across nerd and emoji; all-segments-together assertion"
    - path: "tests/test_weather_sun.py"
      provides: "Existing sun tests pinned to icon_set=\"emoji\" so the emoji-codepoint assertions stay valid under the new nerd default"
    - path: "tests/test_weather_alerts.py"
      provides: "Existing fallback-to-sun tests pinned to icon_set=\"emoji\" so the emoji-codepoint assertions stay valid under the new nerd default"
  key_links:
    - from: "render_top_line / render_bottom_line"
      to: "_sun_segment / _model_segment / _rate_segment callers"
      via: "icon_set read from cfg['display'] and passed through (mirrors show_thinking_glyph threading)"
      pattern: "icon_set"
    - from: "_sun_segment (nerd)"
      to: "_WI_SUNRISE / _WI_SUNSET"
      via: "icon_set branch with try/except emoji fallback"
      pattern: "_WI_SUNRISE|_WI_SUNSET"
---

<objective>
Convert the remaining three segments to Nerd Font glyphs under the single global `icon_set`
switch (D-01, D-07): sun events to `wi-sunrise`/`wi-sunset` (visually unified with the weather
block, D-01), the thinking indicator to its Nerd Font glyph, and the 5h/weekly rate-limit
glyphs to their Nerd Font glyphs. The `icon_set="emoji"` path reverts all three to the Phase 2
emoji (D-06). One switch flips all four converted segments together (D-07).

A blocking human-verify checkpoint closes the phase: the four converted segments must render
correctly in the user's actual Nerd-Font terminal (no font info is available on stdin, which is
why the toggle is manual, D-06).

Implements ENH-04 sun/thinking/rate segments per D-01, D-07; threads icon_set per the existing
show_thinking_glyph pattern.
Purpose: complete the four-segment conversion behind one toggle, then human-verify the glyphs.
Output: icon_set-branched glyph selection in three segment sites + propagation + checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-nerd-font-icon-set/02.1-CONTEXT.md
@.planning/phases/02.1-nerd-font-icon-set/02.1-PATTERNS.md
@.planning/phases/02.1-nerd-font-icon-set/02.1-01-SUMMARY.md
@.planning/phases/02.1-nerd-font-icon-set/02.1-02-SUMMARY.md

<interfaces>
<!-- Plan 01 constants to consume + the three sites to convert. -->

From Plan 01: _WI_SUNRISE, _WI_SUNSET, _NF_THINKING, _NF_HOURGLASS, _NF_CALENDAR glyph constants.

icon_set propagation pattern (mirror show_thinking_glyph, render_top_line lines 1170-1174):
  display  = cfg.get("display", {})
  icon_set = display.get("icon_set", "nerd")
  # then pass icon_set= into each segment builder that needs it

_sun_segment glyph block (lines 992-1006) — emoji escapes to branch:
  glyph = "\U0001f305"  # 🌅 sunrise
  glyph = "\U0001f307"  # 🌇 sunset

_model_segment thinking site (lines 916-918):
  suffix = " 💭" if (thinking_enabled and show_thinking_glyph) else ""

_rate_segment callers (lines 1204-1210) — glyph is already an ARGUMENT, swap at call site only:
  _rate_segment(five_hour_block, "⏳", warn=warn, crit=crit)
  _rate_segment(seven_day_block, "🗓", warn=warn, crit=crit)

DO NOT TOUCH: _FILLED/_EMPTY (D-02); color_for/is_green and the bottom-line GREEN/YELLOW/RED
threshold coloring (D-08 — that is usage semantics, not glyph styling).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: icon_set-branched glyphs for sun, thinking, and rate-limit + propagation + pin pre-existing emoji tests</name>
  <files>claude-statusline.py, tests/test_nerd_icons.py, tests/test_weather_sun.py, tests/test_weather_alerts.py</files>
  <read_first>
    - claude-statusline.py lines 908-921 (_model_segment), 948-1008 (_sun_segment), 1134-1157 (_rate_segment), 1164-1218 (render_top_line / render_bottom_line incl. the rate callers and the show_thinking_glyph threading)
    - tests/test_weather_sun.py — TestSunSegment.setUp() builds self.cfg; the three tests test_before_sunrise_returns_sunrise_glyph / test_before_sunset_returns_sunset_glyph / test_after_sunset_returns_next_sunrise assert the EMOJI codepoints \U0001f305 (🌅) / \U0001f307 (🌇). These call _sun_segment WITHOUT passing icon_set, so the new nerd default would return _WI_SUNRISE/_WI_SUNSET and break them — they must be pinned to the emoji path.
    - tests/test_weather_alerts.py — the three fallback-to-sun tests test_beyond_max_stale_falls_back_to_sun / test_no_alerts_falls_back_to_sun / test_cold_alerts_section_falls_back_to_sun assert emoji sun glyphs \U0001f305 / \U0001f307 via _run_segment_with_cache's cfg; same nerd-default breakage — the cfg must pin icon_set="emoji".
    - .planning/phases/02.1-nerd-font-icon-set/02.1-CONTEXT.md (D-01 four segments incl. wi-sunrise/wi-sunset; D-06 default nerd, emoji fallback; D-07 single switch; Claude's Discretion on thinking/rate glyphs)
    - .planning/phases/02.1-nerd-font-icon-set/02.1-PATTERNS.md (_sun_segment, _model_segment, _rate_segment sections incl. the icon_set propagation pattern and never-crash fallback)
    - .planning/phases/02.1-nerd-font-icon-set/02.1-01-SUMMARY.md (exact _WI_SUNRISE/_WI_SUNSET/_NF_THINKING/_NF_HOURGLASS/_NF_CALENDAR names)
  </read_first>
  <behavior>
    - Test: _sun_segment with icon_set="nerd" produces a string containing _WI_SUNRISE (before sunrise / after sunset branches) or _WI_SUNSET (before sunset branch); with icon_set="emoji" it contains the Phase 2 🌅/🌇 escapes.
    - Test: _model_segment with thinking.enabled true + icon_set="nerd" appends the _NF_THINKING glyph; with icon_set="emoji" appends " 💭"; with thinking disabled appends neither.
    - Test: render_bottom_line with icon_set="nerd" renders the 5h segment with _NF_HOURGLASS and the weekly segment with _NF_CALENDAR; with icon_set="emoji" renders ⏳ and 🗓.
    - Test (single switch, D-07): setting icon_set="nerd" makes ALL of sun + thinking + rate (and the Plan 02 weather) use nerd glyphs in one render; setting "emoji" makes ALL revert — assert no mixed state (no emoji escape present in a nerd render of the converted segments, and vice versa).
    - Test (threshold coloring intact, D-08): a >90% rate-limit value is still wrapped in RED and a <70% value in GREEN regardless of icon_set — the glyph swap does not disturb color_for.
    - Test (degradation): a forced failure in the nerd glyph selection falls back to the emoji glyph (sun/thinking) or the segment omits (returns None) without raising; _ASTRAL_OK False still returns None from _sun_segment as before.
    - Test (no Phase 2 regression): the existing emoji-asserting sun tests (test_weather_sun.py) and fallback-to-sun tests (test_weather_alerts.py) still pass because their cfg now explicitly pins icon_set="emoji" — they keep covering the emoji codepoint path; the nerd codepoints are asserted only in test_nerd_icons.py.
  </behavior>
  <action>
    Thread icon_set through using the existing show_thinking_glyph pattern. In render_top_line read
    display = cfg.get("display", {}); icon_set = display.get("icon_set", "nerd") and pass icon_set=
    into _model_segment and into _sun_segment (via _weather_segment's existing cfg, or directly where
    _sun_segment is the trailing fallback). In render_bottom_line read the same icon_set before the
    _rate_segment calls.

    _sun_segment: accept icon_set (kwarg, default "nerd") or read from the cfg it already receives.
    Branch the glyph assignment: icon_set=="nerd" -> _WI_SUNRISE / _WI_SUNSET; else the existing
    \U0001f305 / \U0001f307 emoji. Wrap the glyph selection so a missing nerd constant falls back to
    the emoji glyph (never-crash, D-10). Keep the three time-based selection branches, the _ASTRAL_OK
    guard, the local-tz handling, and the strftime format UNCHANGED.

    _model_segment: accept icon_set (kwarg, default "nerd"). Resolve thinking_glyph = _NF_THINKING
    when icon_set=="nerd" else "💭"; suffix = f" {thinking_glyph}" only when (thinking_enabled and
    show_thinking_glyph). Keep the try/except return-None contract.

    Rate segments: _rate_segment already takes the glyph as an argument — do NOT change its signature.
    In render_bottom_line, resolve _glyph_5h / _glyph_wk before the calls: nerd -> _NF_HOURGLASS /
    _NF_CALENDAR, emoji -> "⏳" / "🗓"; pass them into the existing _rate_segment calls. Do NOT touch
    color_for / is_green / the GREEN-YELLOW-RED threshold coloring (D-08) or _FILLED/_EMPTY (D-02).

    PIN PRE-EXISTING EMOJI TESTS (D-06 changes the default to nerd, which flips the return values of
    _sun_segment for any call site that does not pass icon_set — so pre-existing tests that assert the
    emoji codepoints must be pinned to the emoji path or they break, halting execution at <verify>):
      - tests/test_weather_sun.py: in TestSunSegment.setUp(), add 'display': {'icon_set': 'emoji'} to
        self.cfg so the three assertions (test_before_sunrise_returns_sunrise_glyph,
        test_before_sunset_returns_sunset_glyph, test_after_sunset_returns_next_sunrise) keep covering
        the EMOJI path and continue to assert \U0001f305 / \U0001f307. Do NOT change their assertions;
        the corresponding nerd-path assertions (_WI_SUNRISE / _WI_SUNSET) live in test_nerd_icons.py.
      - tests/test_weather_alerts.py: add 'display': {'icon_set': 'emoji'} to the cfg passed inside
        _run_segment_with_cache so the three fallback-to-sun tests
        (test_beyond_max_stale_falls_back_to_sun, test_no_alerts_falls_back_to_sun,
        test_cold_alerts_section_falls_back_to_sun) keep getting the emoji sun glyphs they assert.
      Make these the minimal cfg-pinning edits only — do not otherwise alter those test files.

    Extend tests/test_nerd_icons.py with the behavior assertions, including the single-switch
    all-segments-together assertion (D-07) and the threshold-coloring-intact assertion (D-08).
  </action>
  <verify>
    <automated>python -m pytest tests/test_nerd_icons.py tests/test_bottom_line.py tests/test_weather_sun.py tests/test_weather_alerts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - source: _sun_segment branches on icon_set to _WI_SUNRISE/_WI_SUNSET (nerd) vs the 🌅/🌇 escapes (emoji), with emoji fallback on failure
    - source: _model_segment branches the thinking glyph on icon_set (_NF_THINKING vs 💭); render_bottom_line resolves _NF_HOURGLASS/_NF_CALENDAR vs ⏳/🗓 before the _rate_segment calls (signature unchanged)
    - source: icon_set read via cfg.get("display",{}).get("icon_set","nerd") in render_top_line and render_bottom_line (mirrors show_thinking_glyph threading)
    - tests: tests/test_weather_sun.py setUp cfg and tests/test_weather_alerts.py _run_segment_with_cache cfg both pin 'display': {'icon_set': 'emoji'} so their emoji-codepoint assertions stay valid under the new nerd default
    - behavior: one icon_set value flips sun+thinking+rate (and weather) together — no mixed nerd/emoji bar (D-07)
    - behavior: rate-limit threshold coloring (RED>90 / GREEN<70) unchanged by the glyph swap (D-08); _FILLED/_EMPTY untouched (D-02)
    - behavior: nerd glyph-selection failure falls back to emoji / omits without raising
    - command: python -m pytest tests/test_nerd_icons.py tests/test_bottom_line.py tests/test_weather_sun.py tests/test_weather_alerts.py -q exits 0
  </acceptance_criteria>
  <done>Sun (wi-sunrise/wi-sunset), thinking, and rate-limit (hourglass/calendar) glyphs branch on the single icon_set switch with emoji fallback; pre-existing emoji sun/alert tests pinned to the emoji path so they stay green under the nerd default; threshold coloring and bar chars untouched; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify all four converted segments in a Nerd Font terminal</name>
  <files>(verification-only checkpoint — no files modified)</files>
  <action>Pause for the user to visually verify the four converted segments in their real Nerd Font terminal. This is a blocking human-verify checkpoint — automation (the four glyph swaps) is already complete in Plans 02 and this plan's Task 1; this task verifies the rendered glyphs are correct codepoints (not tofu) and that the icon_set toggle flips all segments together. See what-built / how-to-verify / resume-signal.</action>
  <verify>
    <human-check>User confirms all four converted segments render correct Nerd Font glyphs (not tofu □/▯) under icon_set="nerd" and all revert to Phase 2 emoji under icon_set="emoji"; the ▓░ bar and bottom-line threshold colors are unchanged.</human-check>
  </verify>
  <done>User types "approved" after confirming all four segments render correctly in both nerd and emoji modes (or reports specific tofu/wrong glyphs for codepoint correction).</done>
  <what-built>
    All four glyph-producing segments now render Nerd Font glyphs under icon_set="nerd" (the default):
    weather conditions/alerts at full wi-* granularity with live moon phase and semantic color
    (Plan 02), and the sun events (wi-sunrise/wi-sunset), thinking indicator, and 5h/weekly
    rate-limit glyphs (this plan). icon_set="emoji" reverts every segment to the Phase 2 glyphs.
    The ▓░ context bar is intentionally unchanged (Phase 3).
  </what-built>
  <how-to-verify>
    1. From the project root, render with the bundled fixture (thinking is enabled, rate limits present):
       python claude-statusline.py < .examples/claude_stdin.json
       Confirm it exits 0 and prints two lines. In your Nerd Font terminal, confirm the thinking glyph
       after the model reads as the intended Nerd Font glyph (not a tofu □/▯ box) and the 5h/weekly
       glyphs are the hourglass/calendar Nerd Font glyphs.
    2. To see weather/sun glyphs, set a real [location] lat/lon in
       ~/.claude/claude-statusline/claude-statusline.toml, install/refresh per the normal flow, and
       view the live bar. Confirm: the condition glyph is a crisp single-cell wi-* glyph with the
       expected semantic color (sun=yellow, rain=blue, snow=cyan, storm=magenta/red, fog=gray); a
       clear night shows the current real moon phase; the sun event uses wi-sunrise/wi-sunset.
    3. Set icon_set = "emoji" in the TOML and re-render — confirm ALL four segments revert to the
       Phase 2 emoji (🌅/🌇, 💭, ⏳/🗓, ☀️/🌧️ etc.) and the bar is never half-nerd/half-emoji (D-07).
    4. Confirm the ▓░ context bar looks identical to Phase 2 (unchanged, D-02) and the bottom-line
       green/yellow/red usage colors are unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if all four segments render correctly in both nerd and emoji modes, or describe which glyph(s) render as tofu/wrong so the codepoints can be corrected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config TOML -> cfg['display']['icon_set'] -> stdout | icon_set string selects between static glyph constants; no untrusted data reaches the ANSI/glyph strings. |
| stdin session JSON -> _model_segment/_rate_segment | thinking.enabled and rate-limit numbers; already-validated Phase 1 path; glyphs are static constants appended around coerced values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | Tampering | glyph constants emitted for sun/thinking/rate | accept | All glyphs are static developer-authored constants; no stdin/config value is echoed as a glyph. |
| T-02.1-08 | Denial of Service | nerd glyph selection failure on render | mitigate | _sun_segment / _model_segment wrap bodies in try/except -> emoji fallback or return None (D-10); render never raises. |
| T-02.1-09 | Information disclosure | none new | accept | No new data sources, network, or file writes; only render-layer glyph selection over existing trusted inputs. |

No high-severity threats introduced: this plan only swaps static glyph constants behind a config
toggle over already-trusted stdin/config data. No new inputs, network, parsing, or file writes.
</threat_model>

<verification>
- python -m pytest -q exits 0 (full suite green)
- python claude-statusline.py < .examples/claude_stdin.json exits 0 and prints two lines
- grep -F '_FILLED = "▓"' claude-statusline.py matches (context bar untouched, D-02)
- grep -c "color_for" claude-statusline.py unchanged from Phase 2 (threshold coloring intact, D-08)
- Human-verify checkpoint approved (glyphs render correctly in nerd and emoji modes)
</verification>

<success_criteria>
- Sun events use wi-sunrise/wi-sunset under icon_set="nerd"; thinking and 5h/weekly use Nerd Font glyphs (D-01)
- A single icon_set switch flips all four converted segments together; "emoji" reverts all (D-06, D-07)
- Bottom-line threshold coloring and the ▓░ bar are unchanged (D-08, D-02)
- Glyph-selection failures degrade to emoji / omit without crashing (RUN-01/02)
- Pre-existing emoji sun/alert tests stay green (pinned to icon_set="emoji") — no Phase 2 regression
- Human-verify checkpoint approved in a real Nerd Font terminal
</success_criteria>

<output>
Create `.planning/phases/02.1-nerd-font-icon-set/02.1-03-SUMMARY.md` when done
</output>
</output>
