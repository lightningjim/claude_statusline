# Phase 02.1: Nerd Font icon set - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-28
**Phase:** 02.1-nerd-font-icon-set
**Areas discussed:** Segment scope, Weather granularity, Emoji fallback, Glyph coloring

---

## Area selection

User selected all four offered gray areas to discuss: Segment scope, Weather granularity,
Emoji fallback, Glyph coloring.

---

## Segment scope

Which segments adopt Nerd Font glyphs (weather assumed in)?

| Option | Description | Selected |
|--------|-------------|----------|
| Sun events (🌅/🌇) | Swap to wi-sunrise / wi-sunset; unified with weather block | ✓ |
| Thinking (💭) | Swap 💭 for nf-md-brain / lightbulb / thought-bubble | ✓ |
| Rate-limit (⏳/🗓) | Swap to nf-fa-hourglass + nf-fa-calendar on bottom line | ✓ |
| Context bar (▓░) | NOT recommended — Unicode blocks, render same in any font; Phase 3 territory | |

**User's choice:** Sun events + Thinking + Rate-limit (plus weather). Context bar left out.
**Notes:** Context bar `▓░` deferred to Phase 3 (block-fill presets) as flagged.

---

## Weather granularity

How granular should the NWS condition → wi-* mapping be?

| Option | Description | Selected |
|--------|-------------|----------|
| Exploit wi-* granularity | Day/night splits, distinct snow/sleet/fzra/tsra/fog/wind, full NWS token map | ✓ |
| 1:1 replacement | Closest single wi-* per current emoji, same 7 buckets | |
| Middle ground | A few high-value distinctions only | |

**User's choice:** Exploit full wi-* granularity.
**Notes:** Aligns with meteorologist fidelity goal from Phase 2.

### Follow-up — Moon glyph for clear nights

| Option | Description | Selected |
|--------|-------------|----------|
| Actual moon phase | Compute via astral.moon.phase(), map across 28 wi-moon-* glyphs | ✓ |
| Generic moon | Single wi-night-clear glyph for any clear night | |

**User's choice:** Actual moon phase.
**Notes:** No extra network — astral already a dependency.

---

## Emoji fallback

How should the emoji-vs-Nerd-Font choice be handled in config?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep toggle, Nerd default | icon_set kept, default flips to 'nerd', emoji map retained as fallback | ✓ |
| Commit fully to Nerd Font | Drop emoji map and toggle entirely | |
| Keep toggle, emoji default | Toggle kept, emoji stays default | |

**User's choice:** Keep toggle, Nerd default.

### Follow-up — Toggle granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Single global switch | One icon_set flips all four segments together | ✓ |
| Per-segment toggles | Separate key per segment | |

**User's choice:** Single global switch.

---

## Glyph coloring

How should the monochrome Nerd Font glyphs be colored?

| Option | Description | Selected |
|--------|-------------|----------|
| Semantic weather colors | sun=yellow, moon=dim, rain=blue, snow=cyan, storm=magenta/red, fog=gray; alerts keep severity | ✓ |
| Default foreground | No color except alerts | |
| Sun/moon + alerts only | Color sun events + alerts only | |

**User's choice:** Semantic weather colors.
**Notes:** Flagged that bottom-line green/yellow/red is reserved for usage thresholds; weather
coloring is a separate top-line concern.

---

## Claude's Discretion

- Exact wi-* PUA codepoints per condition/sun/moon glyph.
- Specific thinking glyph (brain vs lightbulb vs thought-bubble) and rate-limit glyphs.
- Precise ANSI/256-color values for the semantic weather palette.
- Internal shape of the dual (nerd/emoji) glyph tables.

## Deferred Ideas

- Runtime font detection (not possible from stdin; icon_set toggle is the substitute).
- Context bar block-fill variations → Phase 3.
- Animated/multi-frame glyphs → out of scope.
