---
phase: 03.1-resolve-default-bar-gradient-vs-shade-test-drift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bottom_line.py
autonomous: true
requirements: []          # No REQ-IDs mapped — CONTEXT-driven test-isolation fix tracked against D-01..D-04
must_haves:
  truths:
    - "The two named tests pass by asserting the no-config shade default (1 ▓ + 19 ░), not the user's live gradient bar (D-01, D-03)."
    - "Running the full suite leaves the user's live config at ~/.claude/claude-statusline/claude-statusline.toml present and still containing bar_style = \"gradient\" — untouched/restored on every exit path (D-03, threat T-03.1-01)."
    - "claude-statusline.py production code is byte-for-byte unchanged; DEFAULTS['bar_style'] remains \"shade\" (D-02)."
    - "No remaining default/baseline-asserting full-run test reads the developer's live config (D-04)."
  artifacts:
    - path: "tests/test_bottom_line.py"
      provides: "Config-isolated versions of the two named default-asserting tests + a reusable no-config isolation helper"
      contains: "_NO_CONFIG_HOME"
  key_links:
    - from: "tests/test_bottom_line.py::run_script"
      to: "claude-statusline.py load_config DEFAULTS fallback"
      via: "HOME-override so expanduser('~/.claude/...') resolves an empty config dir → silent DEFAULTS fallback"
      pattern: "home=.*_NO_CONFIG_HOME|env\\[.HOME.\\]"
---

<objective>
Resolve the Phase-3 default-bar test drift, which is a test-isolation defect (D-01),
not a code/intent drift. `DEFAULTS["bar_style"]` is already `"shade"` and MUST stay
that way (D-02). The two named tests call `run_script()` without isolating the real
config, so they render the developer's live `bar_style = "gradient"` bar and fail their
`▓` assertions. Realign those two tests by isolating them from the real config so they
faithfully exercise `load_config`'s `DEFAULTS` fallback (D-03), then audit and harden any
sibling full-run tests with the same config-leak class of defect (D-04).

Purpose: Make the default and the tests agree without touching production code, preserving
Phase-3 D-09 (zero visual change for existing installs) and never disturbing the user's
live statusline config.
Output: An updated `tests/test_bottom_line.py` with a reusable no-config isolation helper,
the two named tests corrected, and a documented D-04 audit result. No production diff.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-resolve-default-bar-gradient-vs-shade-test-drift/03.1-CONTEXT.md

<interfaces>
<!-- Facts confirmed from the codebase. Use directly — no exploration needed. -->

claude-statusline.py (DO NOT MODIFY — D-02):
- DEFAULTS["display"]["bar_style"] == "shade"  (line 169)
- load_config(path=None): if path is None it uses
  os.path.expanduser("~/.claude/claude-statusline/claude-statusline.toml");
  on ANY error (incl. file missing) it returns copy.deepcopy(DEFAULTS) silently (lines 203-225).
- The script resolves config (line 217), cache (line 250), and lock (line 346) paths
  via os.path.expanduser("~/..."), which honors $HOME. Pointing HOME at an empty temp dir
  makes the script find no config → silent DEFAULTS fallback → renders the SHADE default bar.

Live config (the user's real file — MUST stay intact):
- ~/.claude/claude-statusline/claude-statusline.toml line 34 == `bar_style = "gradient"`.

Existing isolation idioms already in the repo (mirror these — discretion D-03 picks HOME-override):
- tests/test_skeleton_render.py:44 — `run_script(stdin_bytes, home=None)` sets env["HOME"]
  to an isolated tempdir (_ISOLATED_HOME) so the script reads no real config. NON-DESTRUCTIVE:
  it never touches the user's real file. THIS is the preferred mechanism for this fix.
- tests/test_config.py:31 — `run_script(stdin_bytes, env=None)` merges an env override.
- tests/test_bottom_line.py:416 — `TestBarStylePresets._run_with_toml` uses the DESTRUCTIVE
  rename→write→restore-in-`finally` pattern. It is correct but mutates the real file; do NOT
  extend it for the no-config case — use the safer HOME-override instead.

The two failing tests (current observed render is the leaked gradient `'█▍                  '`):
- TestBottomLineFixture::test_bottom_line_bar_fill_cells  (lines 244-257; uses module-level run_script at line 34)
- TestBarStylePresets::test_default_no_config_shade_unchanged  (lines 446-456; uses module-level run_script at line 34)

Audit set (full-run tests across the suite) — assessed during planning:
- tests/test_skeleton_render.py — ALREADY isolated via home= param. Safe.
- tests/test_config.py — default assertions use load_config(path) directly; run_script calls
  assert structural behavior (line counts), not the default bar glyphs. Safe.
- tests/test_bootstrap_degradation.py — run_script asserts structural behavior (2 lines,
  project/model present), not default bar glyphs. Verify during the audit; likely no change.
- tests/test_git_segment.py / tests/test_gsd_segment.py — git/gsd unit scope; no default-bar asserts.
- tests/test_bottom_line.py — the two named tests above are the leak; also re-check the synthetic
  ▓/░-absence tests (lines 380-400) which currently pass only because deleting context_window
  removes the bar entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a non-destructive no-config isolation helper and correct the two named tests (D-01, D-02, D-03)</name>
  <files>tests/test_bottom_line.py</files>
  <read_first>
    - tests/test_bottom_line.py (the file being modified): module-level run_script (line 34),
      TestBottomLineFixture::test_bottom_line_bar_fill_cells (lines 244-257),
      TestBarStylePresets::test_default_no_config_shade_unchanged (lines 446-456),
      TestBarStylePresets._run_with_toml (line 416) and _extract_bar (line 438).
    - tests/test_skeleton_render.py lines 24-53 (the HOME-override run_script idiom + _ISOLATED_HOME) — mirror this.
    - claude-statusline.py lines 169 and 203-225 (confirm DEFAULTS['bar_style']=="shade" and the load_config DEFAULTS fallback). Read-only — DO NOT edit.
  </read_first>
  <action>
    In tests/test_bottom_line.py, give the module-level `run_script` an optional `home`
    parameter (default None) that, when provided, sets `env["HOME"]` for the subprocess
    (mirror tests/test_skeleton_render.py:44). When `home` is None, behavior is unchanged so
    the other passing tests are unaffected. Create a module-level `_NO_CONFIG_HOME` tempdir
    (via tempfile.mkdtemp) that contains NO claude-statusline.toml, so a run under that HOME
    forces load_config's silent DEFAULTS fallback (the shade default). Do NOT write any config
    into it. Update the two named tests to call `run_script(..., home=_NO_CONFIG_HOME)`:
    `TestBottomLineFixture::test_bottom_line_bar_fill_cells` and
    `TestBarStylePresets::test_default_no_config_shade_unchanged`. Keep their assertions on the
    true no-config shade default at the fixture's 7%: bar length 20, exactly 1 `▓` and 19 `░`.
    Per discretion D-03, ALSO assert no gradient glyphs are present (none of `█▏▎▍▌▋▊▉`) to make
    the config-leak failure mode self-documenting and less brittle. Do NOT use the destructive
    rename-restore `_run_with_toml` for these (HOME-override never touches the user's real file).
    Add a one-line comment on `_NO_CONFIG_HOME` explaining it drives the DEFAULTS fallback (D-01/D-03)
    and that it leaves the user's live config untouched (threat T-03.1-01). Make NO change to
    claude-statusline.py.
  </action>
  <verify>
    <automated>python -m pytest "tests/test_bottom_line.py::TestBottomLineFixture::test_bottom_line_bar_fill_cells" "tests/test_bottom_line.py::TestBarStylePresets::test_default_no_config_shade_unchanged" -q</automated>
  </verify>
  <acceptance_criteria>
    - The two-test pytest command above exits 0.
    - The corrected tests assert 1 `▓` + 19 `░` (shade default) AND assert absence of all gradient glyphs `█▏▎▍▌▋▊▉`, under a no-config HOME — not under the user's live config.
    - `git diff --quiet -- claude-statusline.py` exits 0 (production code byte-for-byte unchanged — proves D-02).
    - `grep -n '"bar_style": "shade"' claude-statusline.py` still matches at line 169.
    - After the run, `~/.claude/claude-statusline/claude-statusline.toml` still exists and `sed -n '34p'` still shows `bar_style = "gradient"` (live config untouched — D-03 / T-03.1-01).
    - `grep -n "_NO_CONFIG_HOME" tests/test_bottom_line.py` matches (helper added).
  </acceptance_criteria>
  <done>Both named tests pass via no-config HOME isolation asserting the shade default; production code unchanged; user's live gradient config intact.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and harden sibling full-run tests against the config-leak class, then prove the full suite is green (D-04)</name>
  <files>tests/test_bottom_line.py</files>
  <read_first>
    - tests/test_bottom_line.py (the file being modified): the synthetic ▓/░-absence tests
      (test_missing_rate_limits_still_renders_context / test_missing_context_window_still_renders_rate, lines 363-400)
      and all other module-level `run_script` callers in TestBottomLineFixture (lines 224-305).
    - tests/test_bootstrap_degradation.py (run_script at line 33; tests at lines 177-218) — confirm none assert default-bar glyphs.
    - tests/test_config.py (run_script at line 31; structural-only run_script tests) — confirm default assertions use load_config(path) directly, not the rendered bar.
    - tests/test_skeleton_render.py (already home-isolated) — confirm it is the safe template.
  </read_first>
  <action>
    Audit every test across the suite that invokes a `run_script()` helper AND asserts a
    default/baseline RENDER (specifically the default bar glyphs `▓`/`░`/`█`/partial-blocks or
    the no-config shade look) without config isolation. For test_bottom_line.py: route any such
    default-render assertion through the new no-config HOME isolation from Task 1 (reuse
    `_NO_CONFIG_HOME` via the `home=` param — do NOT re-skip isolation per test). For the
    synthetic ▓/░-absence tests (lines 363-400), if their pass depends on the absence of a bar
    rather than on the configured style, run them under `_NO_CONFIG_HOME` too so they assert
    against the shipped default, not the developer's live config. For the other test files in the
    audit set, if a test only asserts structural behavior (line counts, segment presence) it is
    NOT a config-leak risk — record that finding in the SUMMARY and make no change; only harden
    tests that assert default RENDER output. Do NOT modify claude-statusline.py.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0 (full suite green, no regressions).
    - Every full-run test that asserts default-bar render glyphs (`▓`/`░`/`█`/`▏▎▍▌▋▊▉`) does so under config isolation (no-config HOME) — verifiable: such assertions appear only in test methods whose `run_script(...)` call passes `home=_NO_CONFIG_HOME` (or in `_run_with_toml` which supplies its own config).
    - `grep -rn 'run_script' tests/ | grep -v 'home=\|env=\|def run_script'` — every remaining bare `run_script(...)` call belongs to a test that does NOT assert default-bar render glyphs (confirmed in the SUMMARY audit table).
    - `git diff --quiet -- claude-statusline.py` exits 0 (production code unchanged — D-02).
    - After the full run, `sed -n '34p' ~/.claude/claude-statusline/claude-statusline.toml` still shows `bar_style = "gradient"` (live config untouched — D-03 / T-03.1-01).
    - The SUMMARY records the audit result for all six run_script test files (which were leak-prone, which were structural-only and left unchanged).
  </acceptance_criteria>
  <done>Audit complete and documented; all default-render full-run tests are config-isolated; full suite green; production code and user's live config both untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → user's real filesystem | Test isolation interacts with the user's LIVE statusline config at `~/.claude/claude-statusline/claude-statusline.toml` — a real, in-use file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Denial of Service / data loss (Availability) | Test config isolation in tests/test_bottom_line.py touching `~/.claude/claude-statusline/claude-statusline.toml` | mitigate | Use the NON-DESTRUCTIVE HOME-override isolation (point `$HOME` at an empty temp dir) so the test never moves, overwrites, or deletes the user's real config file — the live file is structurally guaranteed untouched. The destructive rename→restore pattern (`_run_with_toml`) is explicitly NOT extended for the no-config case. If any future helper DOES move/replace the real file, it MUST back up and restore in a `finally` block (or addCleanup/tearDown) exactly like the existing `_run_with_toml`, so the live config is restored on every exit path including exceptions. Acceptance criteria in both tasks assert the live file still exists and still contains `bar_style = "gradient"` after the run. |

No other meaningful attack surface exists: this is local, test-only code with no untrusted input, network, or privilege boundary.
</threat_model>

<verification>
- `python -m pytest "tests/test_bottom_line.py::TestBottomLineFixture::test_bottom_line_bar_fill_cells" "tests/test_bottom_line.py::TestBarStylePresets::test_default_no_config_shade_unchanged" -q` exits 0.
- `python -m pytest tests/ -q` exits 0 (no regressions).
- `git diff --quiet -- claude-statusline.py` exits 0 (D-02: production code unchanged).
- `grep -n '"bar_style": "shade"' claude-statusline.py` still matches (DEFAULTS intact).
- `sed -n '34p' ~/.claude/claude-statusline/claude-statusline.toml` still shows `bar_style = "gradient"` (D-03: live config untouched after the full run).
</verification>

<success_criteria>
- Both named tests pass by asserting the no-config shade default (1 ▓ + 19 ░, no gradient glyphs).
- D-04 audit complete: no default-render full-run test reads the developer's live config; findings recorded in the SUMMARY.
- claude-statusline.py is byte-for-byte unchanged; DEFAULTS['bar_style'] == "shade".
- The user's live config file exists and still says `bar_style = "gradient"` after the suite runs.
</success_criteria>

<output>
Create `.planning/phases/03.1-resolve-default-bar-gradient-vs-shade-test-drift/03.1-01-SUMMARY.md` when done.
Include the D-04 audit table (per run_script test file: leak-prone vs structural-only, and what changed).
</output>
