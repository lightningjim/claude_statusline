---
phase: 09-clickable-links
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - claude-statusline.py
  - tests/test_status_links.py
  - tests/fixtures/status_incident_valid_id.json
autonomous: true
requirements: [LINK-01, LINK-03]
must_haves:
  truths:
    - "D-03: status event links to the specific incident page https://status.claude.com/incidents/{id} (no homepage substitution)"
    - "D-07: status clickable span = the whole rendered status segment"
    - "In a link-enabled render, the whole Claude Status segment ({color}{glyph} {label}{RESET}) is wrapped in OSC 8 to https://status.claude.com/incidents/{id}"
    - "The incident id is bound from the SAME incident the segment is about (surviving incident on active path, explaining incident on resolved/degraded path)"
    - "A missing/empty/invalid incident id (incl. a hyphenated slug like inc-001 that fails the ^[0-9a-z]+$ allowlist) → plain text, no link (no homepage substitution) — D-03a omit-not-fake"
    - "When links=off (or no valid id), the rendered status segment contains no \\x1b]8 bytes — byte-identical to today"
  artifacts:
    - path: "claude-statusline.py"
      provides: "OSC 8 wrap of the whole returned segment in _claude_status_segment + id binding"
      contains: "status.claude.com/incidents/"
    - path: "tests/test_status_links.py"
      provides: "Render-level tests: link present when enabled+valid id, no \\x1b]8 when off/invalid id, no homepage fallback"
      contains: "class"
    - path: "tests/fixtures/status_incident_valid_id.json"
      provides: "Tracked-incident fixture whose incident id matches ^[0-9a-z]+$ (no hyphen) — the links=on valid-id case"
      contains: "incidents"
  key_links:
    - from: "_claude_status_segment return"
      to: "osc8 helper"
      via: "osc8(f'{color}{detail}{RESET}', url, enabled=_osc8_enabled(_cfg))"
      pattern: "osc8\\("
    - from: "_claude_status_segment"
      to: "_valid_incident_id"
      via: "validate surviving/explaining incident id before building url"
      pattern: "_valid_incident_id"
---

<objective>
Wire OSC 8 into the Claude Status render site. The whole rendered status segment becomes a
clickable link to the specific incident page on status.claude.com, with the id bound from the
exact incident the segment is describing and validated via the Plan-01 allowlist validator.

Purpose: Deliver LINK-01 (status events clickable to the relevant status.claude.com incident
page) while preserving LINK-03 (zero stray escapes when off / no valid id) and honoring D-03a
(missing/invalid id → plain text, never the homepage).

Output: A modified `_claude_status_segment` return path and a new `tests/test_status_links.py`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-clickable-links/09-CONTEXT.md
@.planning/phases/09-clickable-links/09-PATTERNS.md
@.planning/phases/09-clickable-links/09-01-SUMMARY.md

<!-- 09-02-SUMMARY.md is intentionally NOT loaded: this plan declares depends_on 09-02 only for
     wave ordering (both 09-02 and 09-03 modify claude-statusline.py, so they must not run in the
     same wave). 09-02 touches the WEATHER render site (_weather_segment) and tests/test_weather_links.py;
     this plan touches the STATUS render site (_claude_status_segment) and tests/test_status_links.py —
     zero shared code. Both plans only consume the 09-01 osc8/_osc8_enabled/_valid_incident_id contracts,
     which are in the 09-01-SUMMARY already loaded above. No 09-02 context is needed to execute this plan. -->

<interfaces>
<!-- From Plan 09-01 (must exist before this plan runs): -->
osc8(text: str, url, *, enabled: bool) -> str        # returns text unchanged when not enabled or not url
_osc8_enabled(cfg: dict) -> bool                     # resolves off/auto/on
_valid_incident_id(value) -> str | None              # ^[0-9a-z]+$ via re.fullmatch, else None (rejects hyphens, ESC/ST/control bytes)

<!-- Existing status render site (claude-statusline.py _claude_status_segment, sig at L3857, _cfg in scope): -->
L3918-3919  raw_tracked = sec.get("tracked_incidents"); tracked_incs = list-guard
L3937       explaining_id = sec.get("incident_id")            # resolved/degraded path id
L3945       explaining_inc found among tracked_incs (.get("id"))
L3978       surviving_inc found among tracked_incs (.get("id"))   # active-incident path id
L4048-4052  detail assembly + the SINGLE return: return f"{color}{detail}{RESET}"
Whole function is inside try/except → return None (L4054-4055).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind incident id + OSC 8-wrap the whole returned status segment</name>
  <files>claude-statusline.py, tests/test_status_links.py</files>
  <read_first>
    - claude-statusline.py L3905-4055 (the _claude_status_segment body: id binding spots + the single return)
    - claude-statusline.py L4003 (icon_set render-time resolve — sibling for resolving links)
    - .planning/phases/09-clickable-links/09-PATTERNS.md § 6 and § 7 (status wrap + id binding spots)
    - .planning/phases/09-clickable-links/09-CONTEXT.md D-03/D-03a/D-07 (URL, omit-not-fake, whole-span)
    - .planning/phases/09-clickable-links/09-01-SUMMARY.md (osc8 / _osc8_enabled / _valid_incident_id contracts; note the id allowlist is ^[0-9a-z]+$ — hyphens are rejected)
  </read_first>
  <behavior>
    - Active-incident path: id bound from surviving_inc.get("id"); with links=on and id "abc123" the
      return wraps "{color}{glyph} {label}{RESET}" in OSC 8 → https://status.claude.com/incidents/abc123
    - Resolved/degraded path: id bound from explaining_inc.get("id") (the same incident, not "any")
    - Missing/empty id (None or "") → return the colored segment UNwrapped (no link, no homepage)
    - Invalid id — including a hyphenated Statuspage-style slug like "inc-001", "../x", or one
      containing ESC — → _valid_incident_id returns None → no link (D-03a)
    - links=off → return byte-identical to the pre-Phase-9 segment (no \x1b]8 bytes)
  </behavior>
  <action>
    In _claude_status_segment, capture the incident id alongside the existing binding logic: on the
    resolved/degraded branch bind from explaining_inc.get("id") (near L3945-3953); on the active path
    bind from surviving_inc.get("id") (near L3978); default None otherwise. At the single return point
    (L4052), resolve enabled = _osc8_enabled(_cfg) (mirror the icon_set resolve at L4003), validate the
    captured id via _valid_incident_id (→ None on bad/missing, incl. hyphenated slugs since the allowlist
    is ^[0-9a-z]+$), build url = f"https://status.claude.com/incidents/{inc_id}" only when validated else
    None, and change the return to: return osc8(f"{color}{detail}{RESET}", url, enabled=enabled). Per
    D-03a, NEVER substitute the status.claude.com homepage when the id is missing/invalid — url stays None
    and osc8 returns the colored text unchanged. Do not alter the detail text. The outer try/except →
    return None (L4054-4055) stays. Bind the id defensively (.get with None fallback) so an unexpected
    shape never raises.
  </action>
  <verify>
    <automated>python -m pytest tests/test_status_links.py -k StatusLinkEnabled -x -q</automated>
  </verify>
  <acceptance_criteria>
    - claude-statusline.py contains the literal `https://status.claude.com/incidents/` URL template and the return goes through `osc8(`
    - The id is bound from `surviving_inc`/`explaining_inc` (grep shows id capture near both binding sites), NOT a hardcoded homepage URL
    - With a valid-id tracked-incident fixture (id matches ^[0-9a-z]+$) and links=on, the rendered status segment contains `\x1b]8;;https://status.claude.com/incidents/<id>`
    - pytest exits 0 for the StatusLinkEnabled selector
  </acceptance_criteria>
  <done>The whole status segment is OSC 8-wrapped to the specific incident page when enabled+valid id; id bound from the correct incident; missing/invalid id → plain text, never homepage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render-level LINK-03 + no-homepage-fallback tests for the status site</name>
  <files>claude-statusline.py, tests/test_status_links.py, tests/fixtures/status_incident_valid_id.json</files>
  <read_first>
    - tests/test_claude_status.py L1-40 (status fixture-load + render-test convention)
    - tests/fixtures/status_incident_tracked.json (tracked incident — NOTE its incident id is "inc-001", which FAILS the ^[0-9a-z]+$ allowlist because of the hyphen; this fixture is the D-03a invalid-id case, NOT the valid-id case) and status_operational.json
    - .planning/phases/09-clickable-links/09-CONTEXT.md L98-100, D-03a (LINK-03 bar + no-homepage rule)
    - .planning/phases/09-clickable-links/09-01-SUMMARY.md (helper contracts; _valid_incident_id allowlist is ^[0-9a-z]+$)
  </read_first>
  <behavior>
    - links=off + tracked incident → rendered status segment contains no b"\x1b]8" / no "\\x1b]8"
    - links=on + an incident whose id matches ^[0-9a-z]+$ (the new status_incident_valid_id.json fixture) →
      contains "\x1b]8;;https://status.claude.com/incidents/<that-id>"
    - links=on + status_incident_tracked.json (id "inc-001", hyphenated → fails the allowlist) → no \x1b]8
      bytes and no incidents/ link target (this exercises the D-03a "hyphenated/invalid id → plain text" path)
    - links=on + incident whose id is empty/missing → no \x1b]8 bytes AND the string never contains
      "status.claude.com/incidents/" with an empty slug, and never the bare homepage "status.claude.com" as a link target (D-03a)
    - links=on + incident with an injection-bearing id (e.g. contains ESC) → no \x1b]8 bytes (validator rejects)
  </behavior>
  <action>
    First create tests/fixtures/status_incident_valid_id.json: copy the shape of
    tests/fixtures/status_incident_tracked.json but set the incidents[0].id (and the matching
    tracked-incident id the render site binds from) to a value that matches ^[0-9a-z]+$ with NO hyphen —
    e.g. "abc123def456" (real Statuspage ids are pure lowercase alphanumeric like "kh6z6xlq5g4w", the
    page id already in the tracked fixture). This is the ONLY "valid id" fixture for the links=on link-present
    assertion. Do NOT use status_incident_tracked.json for the link-present case — its id "inc-001" is
    hyphenated and correctly fails the allowlist; instead use it (or a copy) for the D-03a hyphenated-invalid-id
    → plain-text assertion.
    Then create tests/test_status_links.py using the status fixture-load convention from
    tests/test_claude_status.py. Add TestStatusLinkDisabled (no \x1b]8 with links=off on a tracked
    incident) and TestStatusLinkEnabled covering: incident URL bytes present using status_incident_valid_id.json;
    no-link on the hyphenated id from status_incident_tracked.json (D-03a); no-link on empty/missing id;
    no-link on injection id; and an explicit assertion that the homepage is NOT substituted as a link
    target when the id is absent/invalid. Build cfg with {"display": {"links": "on"/"off"}} to force the
    toggle independent of the developer's terminal. If no fixture has an empty/missing-id or injection-id
    incident, add small dedicated fixtures (e.g. tests/fixtures/status_incident_no_id.json,
    status_incident_evil_id.json) rather than mutating shared ones.
  </action>
  <verify>
    <automated>python -m pytest tests/test_status_links.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/status_incident_valid_id.json exists with an incident id matching ^[0-9a-z]+$ (no hyphen)
    - tests/test_status_links.py exists with TestStatusLinkDisabled and TestStatusLinkEnabled classes
    - The disabled test asserts the rendered status segment contains no `\x1b]8` byte sequence
    - The links=on valid-id test uses status_incident_valid_id.json and asserts the `\x1b]8;;https://status.claude.com/incidents/<id>` bytes are present
    - A test asserts that status_incident_tracked.json's hyphenated id "inc-001" yields no OSC 8 link (D-03a)
    - A test asserts that with a missing/empty id the output contains neither an OSC 8 link nor a homepage link target (D-03a)
    - pytest exits 0
  </acceptance_criteria>
  <done>Render-level tests prove LINK-03 (no escapes when off), LINK-01 (link present with an allowlist-valid id via the new fixture), and D-03a (hyphenated/missing/invalid id → plain text, no homepage substitution) for the status site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status.claude.com Statuspage summary → OSC 8 URL | the incident `id` arrives from the network feed and is interpolated into the OSC 8 URI field rendered by the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | incident id → `incidents/{id}` URL | mitigate | id passed through `_valid_incident_id` (Plan-01 allowlist `re.fullmatch` ^[0-9a-z]+$) before building the url; any non-match — incl. hyphenated slugs and ESC/ST/BEL/control bytes — → url None → osc8 returns plain text, zero `\x1b]8` bytes. (Task 1) |
| T-09-06 | Spoofing | homepage-as-stand-in link | mitigate | D-03a: on missing/invalid id the url stays None (no homepage substitution), so a user is never sent to an unrelated page; verified by the no-homepage test. (Task 2) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages — stdlib only. No package-legitimacy gate required. |
</threat_model>

<verification>
- `python -m pytest tests/test_status_links.py -q` passes.
- `python -m pytest tests/test_claude_status.py -q` still passes (no regression in existing status rendering).
- `python -m pytest tests/ -q` passes end-to-end (all three new test files plus the existing suite).
- `grep -c 'status.claude.com/incidents/' claude-statusline.py` returns ≥ 1; `grep -c "https://status.claude.com\"" claude-statusline.py` shows no bare-homepage link target.
</verification>

<success_criteria>
- Claude Status segment is a clickable OSC 8 link to the specific incident page when links enabled and id valid (LINK-01).
- Missing/invalid id (incl. hyphenated slugs) → plain text, never homepage (D-03a); off renders byte-identically with no escapes (LINK-03).
- Full test suite passes with no regression.
</success_criteria>

<output>
Create `.planning/phases/09-clickable-links/09-03-SUMMARY.md` when done.
</output>
