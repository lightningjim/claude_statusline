---
phase: 09-clickable-links
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - claude-statusline.py
  - tests/test_weather_links.py
autonomous: true
requirements: [LINK-02, LINK-03]
must_haves:
  truths:
    - "In a link-enabled render, the weather alert glyph+event+timing fragment is wrapped in OSC 8 pointing at the NWS zone URL"
    - "The trailing per-class tally (+N) stays OUTSIDE the OSC 8 span"
    - "UGC is extracted from props.geocode.UGC: first forecast-zone (^[A-Z]{2}Z[0-9]{3}$), else first county (^[A-Z]{2}C[0-9]{3}$), else no link"
    - "The OSC 8 wrap composes with the existing single {color}{detail}{RESET} SGR wrap (SGR valid inside the span)"
    - "When links=off (or no valid UGC), the rendered weather segment contains no \\x1b]8 bytes — byte-identical alert text to today"
  artifacts:
    - path: "claude-statusline.py"
      provides: "OSC 8 wrap of the alert detail in _weather_segment Step 3c + UGC extraction"
      contains: "alerts/active?zone="
    - path: "tests/test_weather_links.py"
      provides: "Render-level tests: link present when enabled+valid UGC, no \\x1b]8 when disabled, tally outside span"
      contains: "class"
  key_links:
    - from: "_weather_segment Step 3c"
      to: "osc8 helper"
      via: "osc8(colored_glyph_event_timing, url, enabled=_osc8_enabled(cfg))"
      pattern: "osc8\\("
    - from: "_weather_segment Step 3c"
      to: "props.geocode.UGC"
      via: ".get('geocode') or {} → UGC list → _valid_ugc"
      pattern: "geocode"
---

<objective>
Wire OSC 8 into the weather alert render site. The Phase-8 alert detail
(`{class_glyph} {event} · {from|until <time>}`) becomes a clickable link to the NWS per-zone
active-alerts endpoint, while the trailing per-class tally stays plain. Extract the UGC zone
code from the already-cached raw NWS feature and validate it via the Plan-01 validator.

Purpose: Deliver LINK-02 (weather alerts are clickable to the NWS detail URL) while preserving
the LINK-03 guarantee (zero stray escapes when off / no valid zone) at this call site.

Output: A modified `_weather_segment` Step 3c alert-override block and a new
`tests/test_weather_links.py`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-clickable-links/09-CONTEXT.md
@.planning/phases/09-clickable-links/09-PATTERNS.md
@.planning/phases/09-clickable-links/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 09-01 (must exist before this plan runs): -->
osc8(text: str, url, *, enabled: bool) -> str   # returns text unchanged when not enabled or not url
_osc8_enabled(cfg: dict) -> bool                # resolves off/auto/on
_valid_ugc(value) -> str | None                 # ^[A-Z]{2}[CZ][0-9]{3}$ via re.fullmatch, else None

<!-- Existing weather render site (claude-statusline.py _weather_segment Step 3c): -->
L3763  props = best.get("properties") or best      # geocode.UGC lives under props
L3784  detail = f"{class_glyph} {safe_event}"
L3790-92  timing_fragment append → detail += f" · {timing_fragment}"   # END of clickable span (D-06)
L3796-99  tally append (if remaining_alerts) → detail += f"  {tally}"   # STAYS OUTSIDE the link (D-06)
L3800  trailing_detail = f"{color}{detail}{RESET}"   # single canonical SGR wrap (Phase-8 D-01)
Whole block is inside try/except → pass (L3801-3802) falling back to _sun_segment.
cfg is in scope (signature _weather_segment(data, cfg)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract+validate UGC and OSC 8-wrap the alert detail in _weather_segment Step 3c</name>
  <files>claude-statusline.py, tests/test_weather_links.py</files>
  <read_first>
    - claude-statusline.py L3753-3818 (the full Step 3c alert-override block being modified)
    - claude-statusline.py L3674-3675 (icon_set render-time resolve — sibling for resolving links)
    - .planning/phases/09-clickable-links/09-PATTERNS.md § 5 and § 7 (weather wrap + UGC read pattern)
    - .planning/phases/09-clickable-links/09-CONTEXT.md D-04/D-05/D-06 (URL template, UGC chain, span boundary)
    - .planning/phases/09-clickable-links/09-01-SUMMARY.md (the osc8 / _osc8_enabled / _valid_ugc contracts)
  </read_first>
  <behavior>
    - With links enabled and props.geocode.UGC == ["OKZ034"], the rendered detail wraps
      "{color}{glyph} {event} · {timing}{RESET}" in OSC 8 → url https://api.weather.gov/alerts/active?zone=OKZ034
    - The trailing "  {tally}" (when remaining_alerts) appears AFTER the OSC 8 close — not inside the span
    - With geocode.UGC == ["OKC109"] (county only, no Z), url uses zone=OKC109 (county fallback)
    - With geocode.UGC == ["XX9999"] (no valid code), the detail renders with NO \x1b]8 bytes (plain colored text)
    - With links=off, the detail renders byte-identical to the pre-Phase-9 output (no \x1b]8 bytes)
  </behavior>
  <action>
    In the Step 3c alert-override block: resolve `enabled = _osc8_enabled(cfg)` once near the
    icon_set resolve. After the timing fragment is appended (L3792) and BEFORE the tally is appended
    (L3796), split the assembly so the tally is excluded from the link: build
    glyph_event_timing = detail (the glyph+event+timing string at that point); extract UGC via
    `geocode = props.get("geocode") or {}; ugc_list = geocode.get("UGC") or []`, pick the first item
    matching `^[A-Z]{2}Z[0-9]{3}$`, else the first matching `^[A-Z]{2}C[0-9]{3}$`, run it through
    `_valid_ugc` (returns None on bad/empty); url = f"https://api.weather.gov/alerts/active?zone={ugc}"
    only when validated, else None. Compose: linkable = osc8(f"{color}{glyph_event_timing}{RESET}",
    url, enabled=enabled); then if a tally exists append it OUTSIDE: trailing_detail = linkable +
    (f"  {tally}" if tally else ""). Keep the single SGR color wrap INSIDE the osc8 span (D-06). Do
    not change what text is rendered — only how the glyph+event+timing portion is wrapped. The existing
    outer try/except → pass (L3801-3802) and the segment-level try/except (L3817-3818) stay; on any
    failure the block degrades to plain/sun (omit-not-fake). NOTE: extract UGC defensively with the
    `.get(...) or default` idiom so a missing geocode never raises.
  </action>
  <verify>
    <automated>python -m pytest tests/test_weather_links.py -k WeatherLinkEnabled -x -q</automated>
  </verify>
  <acceptance_criteria>
    - claude-statusline.py Step 3c contains the literal `alerts/active?zone=` URL template and a call to `osc8(`
    - The tally append occurs after the osc8 close in source order (tally string is concatenated to the osc8 result, not to `detail` before wrapping)
    - With a fixture alert carrying geocode.UGC=["OKZ034"] and links=on, the rendered weather segment contains the bytes `\x1b]8;;https://api.weather.gov/alerts/active?zone=OKZ034`
    - pytest exits 0 for the WeatherLinkEnabled selector
  </acceptance_criteria>
  <done>The weather alert detail is OSC 8-wrapped to the NWS zone URL when enabled+valid; tally stays outside; UGC chain (Z→C→none) honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render-level LINK-03 + tally-outside tests for the weather site</name>
  <files>claude-statusline.py, tests/test_weather_links.py</files>
  <read_first>
    - tests/test_weather_alerts.py L1-35 (fixture-load + render-test convention for this segment)
    - tests/fixtures/nws_alerts_active.json (the active-alert fixture; confirm/extend its geocode.UGC)
    - .planning/phases/09-clickable-links/09-CONTEXT.md L98-100 (LINK-03 acceptance bar: assert no \x1b]8)
    - .planning/phases/09-clickable-links/09-01-SUMMARY.md (helper contracts)
  </read_first>
  <behavior>
    - links=off → rendered weather segment contains no b"\x1b]8" / no "\\x1b]8" substring
    - links=on + valid UGC fixture → rendered segment contains "\x1b]8;;https://api.weather.gov/alerts/active?zone="
    - links=on + valid UGC + a remaining-alert tally → the "+N" tally substring appears AFTER the OSC 8 close sequence in the rendered string (tally not inside the link)
    - links=on + alert whose geocode.UGC has no valid code → no \x1b]8 bytes
  </behavior>
  <action>
    Create tests/test_weather_links.py using the fixture-load convention from tests/test_weather_alerts.py
    (importlib module-load OR subprocess run_script with CLAUDE_STATUSLINE_FAKE_ALERTS). Add a
    TestWeatherLinkDisabled class asserting no \x1b]8 with links=off, and a TestWeatherLinkEnabled
    class asserting the zone URL bytes appear with links=on and a valid UGC, the county fallback,
    the no-UGC plain-text case, and the tally-outside-span ordering (find index of the OSC 8 close
    sequence and assert the tally substring index is greater). If the existing
    tests/fixtures/nws_alerts_active.json lacks a geocode.UGC suitable for these cases, add a small
    dedicated fixture (e.g. tests/fixtures/nws_alert_with_ugc.json) rather than mutating the shared one.
    Force links via a cfg dict ({"display": {"links": "on"}} / {"display": {"links": "off"}}) so the
    test does not depend on the developer's terminal env.
  </action>
  <verify>
    <automated>python -m pytest tests/test_weather_links.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_weather_links.py exists with TestWeatherLinkDisabled and TestWeatherLinkEnabled classes
    - The disabled test asserts the rendered weather segment contains no `\x1b]8` byte sequence
    - The tally-outside test asserts the tally substring position is after the OSC 8 close position
    - pytest exits 0
  </acceptance_criteria>
  <done>Render-level tests prove LINK-03 (no escapes when off/no-UGC) and the D-06 tally-outside-span boundary for the weather site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NWS CAP response → OSC 8 URL | `geocode.UGC` arrives from api.weather.gov and is interpolated into the OSC 8 URI field rendered by the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | UGC → `alerts/active?zone={ugc}` URL | mitigate | UGC is passed through `_valid_ugc` (Plan-01 allowlist `re.fullmatch`) before building the url; non-match → url None → osc8 returns plain text, zero `\x1b]8` bytes. (Task 1) |
| T-09-04 | Information Disclosure | tally inside link span | accept | The tally is plain text outside the span by design (D-06); it references other alerts and carries no secret. Boundary verified by the tally-outside test. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages — stdlib only. No package-legitimacy gate required. |
</threat_model>

<verification>
- `python -m pytest tests/test_weather_links.py -q` passes.
- `python -m pytest tests/test_weather_alerts.py -q` still passes (no regression in existing alert rendering).
- `grep -c 'alerts/active?zone=' claude-statusline.py` returns ≥ 1.
</verification>

<success_criteria>
- Weather alert glyph+event+timing is a clickable OSC 8 link to the NWS zone URL when links enabled and UGC valid (LINK-02).
- Tally stays outside the link; off/invalid-UGC renders byte-identically with no escapes (LINK-03).
- No regression in existing weather/alert tests.
</success_criteria>

<output>
Create `.planning/phases/09-clickable-links/09-02-SUMMARY.md` when done.
</output>
