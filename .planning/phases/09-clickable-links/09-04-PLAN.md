---
phase: 09-clickable-links
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-statusline.py
  - tests/test_weather_links.py
  - tests/test_weather_link_target.py
autonomous: true
gap_closure: true
requirements:
  - LINK-02
user_setup: []

must_haves:
  truths:
    - "Clicking a weather alert opens forecast.weather.gov/showsigwx.php (a human-readable WWA-by-location page), NOT api.weather.gov raw CAP JSON"
    - "The weather link includes BOTH warnzone (the forecast-zone UGC) and warncounty (the county UGC derived from geocode.SAME) — warncounty is required for the page to populate"
    - "_same_to_county_ugc('006037') returns 'CAC037' and _same_to_county_ugc('040109') returns 'OKC109'"
    - "When the alert has no usable SAME (missing/empty/bad state FIPS/derived county fails _valid_ugc), the weather alert renders as plain text with zero \\x1b]8 bytes (omit-not-fake, D-10)"
    - "Under links=auto, OSC 8 is enabled for VTE only when VTE_VERSION >= 5000; a non-numeric or pre-5000 VTE_VERSION yields no link and never crashes"
    - "The per-class tally (+N) remains outside the OSC 8 link span"
  artifacts:
    - path: "claude-statusline.py"
      provides: "_same_to_county_ugc helper + _FIPS_STATE_POSTAL table + showsigwx URL template + VTE>=5000 gate"
      contains: "def _same_to_county_ugc"
    - path: "claude-statusline.py"
      provides: "module-level FIPS-state -> USPS postal lookup (50 states + DC + PR/GU/VI/AS/MP)"
      contains: "_FIPS_STATE_POSTAL"
    - path: "tests/test_weather_link_target.py"
      provides: "unit tests for _same_to_county_ugc + _FIPS_STATE_POSTAL"
      contains: "_same_to_county_ugc"
    - path: "tests/test_weather_links.py"
      provides: "updated integration tests asserting showsigwx URL + omit-not-fake on missing SAME"
      contains: "showsigwx.php"
  key_links:
    - from: "claude-statusline.py _weather_segment Step 3c"
      to: "_same_to_county_ugc"
      via: "county UGC derivation from props.geocode.SAME"
      pattern: "_same_to_county_ugc\\("
    - from: "claude-statusline.py _weather_segment Step 3c"
      to: "forecast.weather.gov/showsigwx.php"
      via: "warnzone + warncounty URL build, gated on both validating"
      pattern: "showsigwx\\.php\\?warnzone="
    - from: "claude-statusline.py _osc8_enabled auto branch"
      to: "VTE_VERSION >= 5000 gate"
      via: "int() parse with defensive except"
      pattern: "5000"
---

<objective>
Close the two UAT gaps and one documented WON'T-FIX from Phase 9's human testing, all in `claude-statusline.py`:

- **GAP-09-A (LINK-02):** the weather-alert OSC 8 link currently points at `https://api.weather.gov/alerts/active?zone={UGC}`, which renders raw CAP JSON in a browser. Re-target it to the human-readable NWS "WWA Summary by Location" page `https://forecast.weather.gov/showsigwx.php?warnzone={zoneUGC}&warncounty={countyUGC}`. `warncounty` is REQUIRED for the page to list active alerts; it must be derived from `geocode.SAME` (FIPS) because NWS zone alerts carry no county code in `geocode.UGC`.
- **GAP-09-B (WR-01):** under `links="auto"`, the VTE branch enables OSC 8 for any non-empty `VTE_VERSION`. OSC 8 landed in VTE 0.50 (`VTE_VERSION == 5000`); gate on `int(VTE_VERSION) >= 5000`, parsing defensively.
- **WR-02 (WON'T FIX, doc-only):** keep JetBrains in the `auto` allowlist (the user's PyCharm terminal supports OSC 8) but add a code comment explaining that the env var cannot distinguish legacy from reworked JediTerm.

Purpose: clicking a weather alert opens a page a human can actually read, while preserving every existing guarantee (omit-not-fake / D-10, tally-outside-span / D-06, zero escape bytes when off or unsupported / LINK-03).

Output: a new pure helper `_same_to_county_ugc`, a module-level FIPS->postal table, the swapped URL template, the VTE gate, the WR-02 comment, a new unit-test file, and updated integration tests.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/09-clickable-links/09-HUMAN-UAT.md
@.planning/phases/09-clickable-links/09-REVIEW.md
@.planning/phases/09-clickable-links/09-CONTEXT.md
@.planning/phases/09-clickable-links/09-02-SUMMARY.md
@.planning/phases/09-clickable-links/09-01-SUMMARY.md
@.planning/phases/09-clickable-links/09-PATTERNS.md

<interfaces>
<!-- Existing helpers/constants from 09-01/09-02 to reuse directly — do NOT re-create them. -->

From claude-statusline.py (Plan 09-01 foundation):
- def osc8(text, url, *, enabled) -> str        # returns text unchanged when not enabled OR url falsy; zero \x1b]8 bytes on that path
- def _osc8_enabled(cfg) -> bool                 # tri-state off/auto/on resolver; auto branch L280-298
- def _valid_ugc(value) -> str | None            # re.fullmatch(r"[A-Z]{2}[CZ][0-9]{3}") allowlist; None on any non-match
- _OSC8_OPEN_PRE / _OSC8_ST / _OSC8_CLOSE        # octal \033-spelled OSC 8 byte constants

Current weather link site — _weather_segment Step 3c (claude-statusline.py ~L3930-3965):
- props = best.get("properties") or best         # already in scope; CAP fields live here
- _geocode  = props.get("geocode") or {}
- _ugc_list = _geocode.get("UGC") or []          # list of Z/C codes; SAME list is _geocode.get("SAME")
- two passes select _ugc: Z-first then C-fallback via _valid_ugc
- _wx_url = f"https://api.weather.gov/alerts/active?zone={_ugc}" if _ugc else None   # <-- REPLACE THIS
- linkable = osc8(f"{color}{detail}{RESET}", _wx_url, enabled=_links_enabled)
- tally appended to `linkable` AFTER (outside span) — PRESERVE this exactly

Current auto-detect site — _osc8_enabled (claude-statusline.py L291-296):
- L291-293:  # VTE_VERSION ...   if os.environ.get("VTE_VERSION"): return True     # <-- REPLACE with int>=5000 gate
- L294-296:  # TERMINAL_EMULATOR — JetBrains ...   if "JetBrains" in term_emu: return True   # <-- KEEP; add WR-02 comment
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _same_to_county_ugc pure helper + FIPS->postal table + unit tests</name>
  <files>claude-statusline.py, tests/test_weather_link_target.py</files>
  <read_first>
    - claude-statusline.py (read L240-330: the OSC 8 helpers, _valid_ugc, and the _OSC8_TERM_PROGRAM_ALLOW frozenset — place the new table + helper in this same module-level region, matching the existing allowlist-constant style)
    - tests/test_weather_links.py (mirror its module-import idiom: importlib.util.spec_from_file_location loader, the OSC8_* str constants — reuse for the new test file's imports)
  </read_first>
  <behavior>
    - _same_to_county_ugc("006037") == "CAC037"   (SAME 0|06|037 → CA + C + 037; Los Angeles)
    - _same_to_county_ugc("040109") == "OKC109"   (SAME 0|40|109 → OK + C + 109; Oklahoma County)
    - _same_to_county_ugc("048201") == "TXC201"   (Harris County, TX — sanity case for a different state)
    - _same_to_county_ugc(None) is None
    - _same_to_county_ugc("") is None
    - _same_to_county_ugc("12345") is None        (5 digits, not 6 → reject, never index out of range)
    - _same_to_county_ugc("0xx037") is None       (non-numeric state field → reject)
    - _same_to_county_ugc("099037") is None       (state FIPS 99 not in table → reject, omit-not-fake)
    - the returned value always satisfies _valid_ugc (pattern ^[A-Z]{2}C[0-9]{3}$) or is None — assert _valid_ugc(_same_to_county_ugc(x)) is not None for every success case
    - _FIPS_STATE_POSTAL covers all 50 states + DC ("11"→"DC") + PR("72") GU("66") VI("78") AS("60") MP("69")
  </behavior>
  <action>
    Add a module-level constant `_FIPS_STATE_POSTAL: dict[str, str]` mapping 2-digit zero-padded
    state FIPS strings to 2-letter USPS postal codes, placed alongside the other OSC 8 / validator
    constants (~L255-313 region, stdlib-only, no new import). Required keys: all 50 states
    ("01"→"AL" ... "56"→"WY"), DC ("11"→"DC"), and the NWS-issued territories PR("72"), GU("66"),
    VI("78"), AS("60"), MP("69"). Use the standard Census/ANSI FIPS-to-USPS mapping.

    Add a pure helper `def _same_to_county_ugc(same) -> str | None` next to `_valid_ugc`, following
    the same total/never-raises contract (return None on ANY bad input; never raise):
      1. If `same` is None → return None. Coerce to str in a try/except (None on failure).
      2. SAME must be exactly 6 digits matching `^[0-9]{6}$` (use re.fullmatch); else return None.
         (Format is P|SS|CCC: 1-digit part-of-county marker, 2-digit state FIPS, 3-digit county FIPS.
         Take state = same[1:3], county = same[3:6]. Validating the full 6-digit shape first means
         the slices can never raise.)
      3. Look up `postal = _FIPS_STATE_POSTAL.get(state)`; if not found → return None (D-10:
         unknown state FIPS → omit, do not fake).
      4. Build `candidate = f"{postal}C{county}"`.
      5. Validate with the EXISTING `_valid_ugc(candidate)` and return its result (so a malformed
         derived code degrades to None through the same allowlist used everywhere else). Reuse
         `_valid_ugc` — do NOT write a second county regex.

    Create `tests/test_weather_link_target.py` as a NEW unit-test file (system pytest target).
    Import the module via the same importlib loader used in tests/test_weather_links.py. Add a
    TestSameToCountyUgc class asserting every case in <behavior> above, plus a table-coverage test
    asserting len(_FIPS_STATE_POSTAL) >= 56 and that "06"→"CA", "40"→"OK", "48"→"TX", "11"→"DC",
    "72"→"PR" are present. Do NOT inline OSC 8 rendering here — this file unit-tests the pure
    helper + table only.
  </action>
  <verify>
    <automated>/usr/bin/pytest -q tests/test_weather_link_target.py</automated>
  </verify>
  <done>
    `_same_to_county_ugc` and `_FIPS_STATE_POSTAL` exist in claude-statusline.py; tests/test_weather_link_target.py passes; _same_to_county_ugc("006037")=="CAC037", ("040109")=="OKC109"; None/short/non-numeric/unknown-state all return None; table has >=56 entries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-target weather link to showsigwx (warnzone+warncounty) with omit-not-fake; update integration tests</name>
  <files>claude-statusline.py, tests/test_weather_links.py</files>
  <read_first>
    - claude-statusline.py (read L3930-3965: the exact Step 3c block — the UGC two-pass selection, the `_wx_url = f"https://api.weather.gov/alerts/active?zone={_ugc}"` line to replace, the `osc8(...)` call, and the tally-outside-span assembly to preserve)
    - claude-statusline.py (read L315-360: _valid_ugc — confirm reuse) and the Task-1 _same_to_county_ugc helper
    - tests/test_weather_links.py (read in full: _make_alert_with_ugc builds geocode={"UGC": ugc_list} with NO SAME; every "link present" assertion checks the OLD api.weather.gov/alerts/active?zone= URL — these MUST be updated because the new link needs SAME-derived warncounty)
  </read_first>
  <behavior>
    - Alert with geocode.UGC=["OKZ034"] AND geocode.SAME=["040109"], links=on → rendered segment
      contains the substring `https://forecast.weather.gov/showsigwx.php?warnzone=OKZ034&warncounty=OKC109`
      wrapped in OSC 8 (i.e. OSC8_OPEN immediately precedes that URL).
    - The rendered URL contains NO `api.weather.gov/alerts/active` substring (old target fully removed).
    - Alert with a valid Z UGC but NO geocode.SAME (and no derivable county) → links=on → zero
      \x1b]8 bytes (omit-not-fake: warncounty required, so no link rather than a half URL).
    - Alert with geocode.SAME present but state FIPS unknown / SAME malformed → zero \x1b]8 bytes.
    - links=off with full UGC+SAME → zero \x1b]8 bytes (LINK-03 unchanged) and byte-identical to
      the no-links-key render.
    - Tally still appears AFTER the OSC8_CLOSE index when extra_alerts>0 (D-06 preserved).
  </behavior>
  <action>
    In `_weather_segment` Step 3c (~L3930-3965), KEEP the existing Z-preferred zone-UGC selection
    (the `_ugc` two-pass loop) — `_ugc` is now specifically the warnzone. ADD county derivation:
    read `_same_list = _geocode.get("SAME") or []` inside the existing try block; iterate it and set
    `_county = _same_to_county_ugc(s)` taking the first non-None result; default `_county = None`.

    REPLACE the `_wx_url = f"https://api.weather.gov/alerts/active?zone={_ugc}" if _ugc else None`
    line with the showsigwx target, built ONLY when BOTH the zone and the county validated:
      `_wx_url = (f"https://forecast.weather.gov/showsigwx.php?warnzone={_ugc}&warncounty={_county}"
                  if (_ugc and _county) else None)`
    Rationale: GAP-09-A verified that `warncounty` is REQUIRED — zone-only does not populate the
    alert list — so if county cannot be derived, build NO link (D-10 omit-not-fake). Do NOT emit a
    zone-only or half URL. Keep the whole derivation inside the existing `try/except → _ugc=None`
    guard so any unexpected geocode shape degrades to no link. Do NOT touch the `osc8(...)` call
    signature or the tally-outside-span assembly (`trailing_detail = linkable + tally`) — both stay
    exactly as in 09-02.

    Update `tests/test_weather_links.py`:
      - Extend the `_make_alert_with_ugc` helper to accept a `same_list` parameter (default a
        matching county, e.g. ["040109"] when the UGC is an OK zone) and write it into
        `props["geocode"]["SAME"]` for the primary alert AND the extra alerts. Keep existing
        signature back-compatible (new kwarg with a default).
      - Swap every assertion that checks `alerts/active?zone=` / `zone=OKZ034` to assert the new
        `forecast.weather.gov/showsigwx.php?warnzone=...&warncounty=...` substring (use OK zone
        OKZ034 + SAME 040109 → warncounty=OKC109). Update test_zone_ugc_link_present,
        test_county_fallback_ugc_link_present (rename intent: county fallback now means the warnzone
        path — keep a Z code present; the warncounty always comes from SAME), test_zone_preferred_over_county,
        and the OSC8-bytes tests accordingly.
      - ADD a test `test_no_same_no_link`: valid Z UGC, geocode with NO SAME, links=on → assert no
        OSC8_OPEN bytes (omit-not-fake).
      - Keep test_links_off_no_osc8_bytes, test_links_off_byte_identical_to_plain, and the
        tally-after-close tests; just feed them a SAME so the "off" cases prove off still suppresses
        a now-buildable link.
    Do NOT weaken any existing LINK-03 / D-06 assertion — only retarget the URL string and add SAME.
  </action>
  <verify>
    <automated>/usr/bin/pytest -q tests/test_weather_links.py</automated>
  </verify>
  <done>
    Rendered weather link is `forecast.weather.gov/showsigwx.php?warnzone={zone}&warncounty={county}`; no `api.weather.gov/alerts/active` substring remains; missing/invalid SAME → zero \x1b]8 bytes; links=off byte-identical to no-key; tally still outside the span; tests/test_weather_links.py passes under /usr/bin/pytest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate VTE auto-detect on VTE_VERSION>=5000 (WR-01) + WR-02 JetBrains doc comment + tests</name>
  <files>claude-statusline.py, tests/test_osc8_links.py</files>
  <read_first>
    - claude-statusline.py (read L280-300: the _osc8_enabled auto branch — the VTE_VERSION presence test at L291-293 and the JetBrains/TERMINAL_EMULATOR branch at L294-296)
    - tests/test_osc8_links.py (read its TestOsc8Enabled class — the auto+known-terminal / auto+no-markers tests that clear env markers via patch.dict; mirror that idiom for the new VTE cases)
  </read_first>
  <behavior>
    - links=auto, env VTE_VERSION="5000", all other allowlist markers cleared → _osc8_enabled True
    - links=auto, env VTE_VERSION="6800" → True   (>= 5000)
    - links=auto, env VTE_VERSION="4604" (VTE 0.46.4, pre-OSC8) → False
    - links=auto, env VTE_VERSION="" or unset → False
    - links=auto, env VTE_VERSION="garbage" (non-numeric) → False, no exception raised
    - links=auto, env TERMINAL_EMULATOR="JetBrains-JediTerm", VTE unset → still True (WR-02: JetBrains stays in allowlist)
  </behavior>
  <action>
    In `_osc8_enabled` (claude-statusline.py ~L291-293), REPLACE the bare VTE presence test
    (`if os.environ.get("VTE_VERSION"): return True`) with a version-gated check: read
    `_vte = os.environ.get("VTE_VERSION", "")`, then `try: if int(_vte) >= 5000: return True
    except (TypeError, ValueError): pass`. Add a one-line comment noting OSC 8 landed in VTE 0.50
    (== 5000) and that unparseable VTE_VERSION biases to False per D-02. Do NOT change the return
    flow for the other allowlist branches.

    For WR-02 (no behavior change), at the JetBrains branch (~L294-296) ADD a comment, e.g.:
    "WR-02: legacy and reworked JediTerm both export TERMINAL_EMULATOR=JetBrains-JediTerm and
    cannot be distinguished via env; kept in the auto allowlist because the author's PyCharm
    terminal supports OSC 8 (UAT Test 3). Users on a truly legacy terminal can set links=off."
    Leave the `if "JetBrains" in term_emu: return True` logic intact.

    Add tests to tests/test_osc8_links.py (or a small new class) covering every <behavior> case
    above. Use patch.dict(os.environ, {...}, clear=False) and explicitly clear the other allowlist
    markers (TERM_PROGRAM, WT_SESSION, KITTY_WINDOW_ID, TERMINAL_EMULATOR) so only VTE_VERSION is
    under test — mirror the existing auto+no-markers test's env-clearing idiom. Assert the garbage
    case returns False without raising.
  </action>
  <verify>
    <automated>/usr/bin/pytest -q tests/test_osc8_links.py</automated>
  </verify>
  <done>
    _osc8_enabled returns True only for VTE_VERSION>=5000 under auto; pre-5000 / empty / non-numeric → False without raising; JetBrains branch unchanged with WR-02 comment present; tests/test_osc8_links.py passes under /usr/bin/pytest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NWS CAP response → OSC 8 URL field | `geocode.SAME` (and `geocode.UGC`) are network-sourced strings interpolated into the OSC 8 URI; a control/ST byte could break out of the hyperlink span |
| env vars → auto capability gate | `VTE_VERSION` is attacker-influenceable in a hostile env; must parse defensively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `_same_to_county_ugc` → showsigwx URL | mitigate | SAME validated `^[0-9]{6}$` then derived county re-validated through existing `_valid_ugc` allowlist; any non-match → None → no link, zero `\x1b]8` bytes |
| T-09-05 | Denial of Service | `int(VTE_VERSION)` in `_osc8_enabled` | mitigate | `int()` wrapped in try/except (TypeError, ValueError) → bias to False; never raises, never hangs (statusline must not crash the bar) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; stdlib-only per project constraint (no new deps) |
</threat_model>

<verification>
- `/usr/bin/pytest -q tests/test_weather_link_target.py` passes (Task 1 pure helper + table)
- `/usr/bin/pytest -q tests/test_weather_links.py` passes (Task 2 integration — new showsigwx target + omit-not-fake)
- `/usr/bin/pytest -q tests/test_osc8_links.py` passes (Task 3 VTE gate)
- `grep -c 'showsigwx.php?warnzone=' claude-statusline.py` returns 1
- `grep -c 'api.weather.gov/alerts/active' claude-statusline.py` returns 0 (old target fully removed)
- `grep -c 'def _same_to_county_ugc' claude-statusline.py` returns 1
- Full suite sanity: `/usr/bin/pytest -q` (no regressions in the other test modules)
</verification>

<success_criteria>
- Clicking a weather alert in a supporting terminal opens `https://forecast.weather.gov/showsigwx.php?warnzone={zone}&warncounty={county}` — a human-readable NWS WWA-by-location page — not raw CAP JSON (GAP-09-A / LINK-02).
- `warncounty` is derived from `geocode.SAME` via `_same_to_county_ugc`; if it cannot be derived and validated, NO link is emitted (D-10 omit-not-fake) — never a half/zone-only URL.
- Under `links="auto"`, VTE terminals get OSC 8 only at `VTE_VERSION >= 5000`; unparseable values bias to False and never crash the bar (GAP-09-B / WR-01).
- JetBrains stays in the auto allowlist with an explanatory WR-02 comment (no behavior change).
- All three test modules pass under system pytest `/usr/bin/pytest`; tally-outside-span (D-06) and links-off / no-escape-bytes (LINK-03) guarantees are preserved.
</success_criteria>

<output>
Create `.planning/phases/09-clickable-links/09-04-SUMMARY.md` when done
</output>
