---
phase: 03-presets-for-the-type-of-block-fill-for-the-progress-bar-incl
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - claude-statusline.py
  - tests/test_bottom_line.py
autonomous: true
requirements: []   # No REQ-IDs mapped; enhancement work driven by CONTEXT.md D-02,D-04,D-05
must_haves:
  truths:
    - "D-02: bar_style=\"gradient\" renders full `█` for whole filled cells, a single left-aligned partial-block boundary cell from the set ▏▎▍▌▋▊▉ (1/8 precision), and a blank space \" \" for the empty track (not ░ or ▒)"
    - "D-04: sub-cell (1/8 partial-block) precision applies to gradient ONLY — shade/solid/solid-dim remain whole-cell floored, unchanged from Plan 01"
    - "D-04: 0% renders an all-blank 20-cell gradient bar; 100% renders an all-█ 20-cell gradient bar (no stray partial cell at the edges)"
    - "D-05: gradient has exactly one fixed look — no smooth toggle; selecting bar_style=\"gradient\" is the only control"
    - "gradient stays a 20-cell-wide bar (the boundary cell occupies one of the 20 cells, not an extra one)"
  artifacts:
    - path: "claude-statusline.py"
      provides: "gradient branch in _context_segment using eighth-block math + the ▏▎▍▌▋▊▉ partial-block set and blank empty track"
      contains: "gradient"
    - path: "tests/test_bottom_line.py"
      provides: "gradient sub-cell rendering assertions at known percentages incl 0% and 100% edge cases"
  key_links:
    - from: "_context_segment gradient branch"
      to: "_BAR_WIDTH eighth math"
      via: "round(pct/100 * _BAR_WIDTH * 8) eighths → full cells + boundary glyph index + blank remainder"
      pattern: "gradient"
---

<objective>
Add the `gradient` preset to the preset table and `_context_segment`: full `█` filled cells, a single 1/8-precision left-aligned partial-block boundary cell (`▏▎▍▌▋▊▉`), and a blank-space empty track. This is the only preset with sub-cell precision (D-04); all others stay whole-cell from Plan 01. Cover the eighth-block math and the 0%/100% edge cases with tests.

Purpose: Deliver the user's own visually-richest preset (D-02), the reason sub-cell rendering enters this phase.
Output: Modified `claude-statusline.py` (gradient branch), gradient tests in `test_bottom_line.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-presets-for-the-type-of-block-fill-for-the-progress-bar-incl/03-CONTEXT.md
@.planning/phases/03-presets-for-the-type-of-block-fill-for-the-progress-bar-incl/03-01-SUMMARY.md

<interfaces>
<!-- After Plan 01: _context_segment takes a bar_style param and selects glyphs via a -->
<!-- preset resolver that already reserves a `gradient` slot. Build the gradient branch here. -->

_BAR_WIDTH = 20   # bar is 20 cells wide; gradient must also total exactly 20 cells.
RESET = "\033[0m"
color_for(pct, warn, crit) -> str   # threshold color for the filled run.

Partial-block set (left-aligned, ascending 1/8 → 7/8), exactly 7 glyphs:
  ▏ (1/8) ▎ (2/8) ▍ (3/8) ▌ (4/8) ▋ (5/8) ▊ (6/8) ▉ (7/8)
Full cell: █ (8/8). Empty track for gradient: " " (single space).

Eighth math (Claude's Discretion in CONTEXT.md, suggested):
  total_eighths = round(pct/100 * _BAR_WIDTH * 8)   # clamp to [0, _BAR_WIDTH*8]
  full_cells    = total_eighths // 8
  remainder     = total_eighths % 8                 # 0..7
  boundary glyph = partial[remainder-1] when remainder>0 else none
  blank cells fill the remaining width so total cells == _BAR_WIDTH
  Edge: 0% → 0 full, 0 remainder → 20 blanks. 100% → 20 full, 0 remainder → 20 █ (no stray partial).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the gradient preset (eighth-block sub-cell render)</name>
  <files>claude-statusline.py</files>
  <read_first>
    - claude-statusline.py — the refactored preset table + _context_segment from Plan 01 (the reserved gradient slot and the per-cell color wrapping)
    - claude-statusline.py lines 88-94 region (constants incl _BAR_WIDTH)
    - claude-statusline.py lines 1147-1162 (color_for)
  </read_first>
  <behavior>
    - bar_style="gradient" produces a 20-cell bar: `full_cells` × `█`, then (if a fractional remainder exists) exactly one boundary glyph from ▏▎▍▌▋▊▉ indexed by the eighth-remainder, then blank spaces " " to pad to 20 cells total.
    - Math: total_eighths = round(pct/100 * _BAR_WIDTH * 8), clamped to [0, _BAR_WIDTH*8]; full_cells = total_eighths // 8; remainder = total_eighths % 8; boundary = ▏..▉ for remainder 1..7, none when remainder == 0.
    - 0% → 20 blank cells, no █, no partial glyph.
    - 100% → 20 █ cells, no partial glyph, no blank.
    - A mid value such that remainder>0 (e.g. a pct yielding total_eighths not divisible by 8) → full_cells █ + one partial glyph + blanks, totaling 20 cells.
    - Coloring (D-06/D-07): the █ run and the partial boundary glyph are wrapped in color_for(pct); the blank empty track needs no color (gray treatment is moot per D-07). Whichever Plan-01 wrapping convention applies, the gradient branch must keep the trailing "{pct}%" in the threshold color and remain inside the existing try/except.
    - shade/solid/solid-dim are untouched (D-04): only the gradient branch uses eighth math.
  </behavior>
  <action>
    Fill in the reserved `gradient` slot in the preset table with the gradient glyph data: full glyph `█`, the ordered 7-glyph partial set `▏▎▍▌▋▊▉`, and blank `" "` empty track. In `_context_segment`, add a gradient code path (selected when the resolved bar_style is `gradient`) that computes `total_eighths = round(pct / 100 * _BAR_WIDTH * 8)` clamped to `[0, _BAR_WIDTH*8]`, then `full_cells = total_eighths // 8` and `remainder = total_eighths % 8`; build the cell string as `█ * full_cells` + (the partial glyph at index `remainder-1` when `remainder > 0`) + `" " * (remaining cells to reach _BAR_WIDTH)`. Guarantee the assembled string is exactly `_BAR_WIDTH` cells (when full_cells == _BAR_WIDTH there is no remainder and no blank; the 100% case must NOT append a partial). Wrap the filled+boundary run in `color_for(pct, warn, crit)` + `RESET` (the blank track is left uncolored — D-07); keep the `{pct}%` suffix colored as in Plan 01. Do NOT alter the whole-cell branch used by shade/solid/solid-dim (D-04). Keep everything inside `_context_segment`'s existing try/except so any failure returns None.
  </action>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/claude_statusline && grep -v '^#' claude-statusline.py | grep -c 'gradient' | { read n; [ "$n" -ge 1 ]; } && python -c "
import importlib.util,re
s=importlib.util.spec_from_file_location('m','claude-statusline.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)
strip=lambda x: re.sub(r'\x1b\[[0-9;]*m','',x)
def bar(p):
    seg=m._context_segment({'context_window':{'used_percentage':p}}, bar_style='gradient'); b=strip(seg); return b[b.index('[')+1:b.index(']')]
b0=bar(0); b100=bar(100)
assert len(b0)==20 and b0==' '*20, repr(b0)
assert len(b100)==20 and b100=='█'*20, repr(b100)
print('edges ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `claude-statusline.py` contains a `gradient` preset entry and a gradient branch in `_context_segment` (verify: `grep -c gradient claude-statusline.py` >= 2 in non-comment lines).
    - At 0%, the gradient bar (ANSI-stripped, chars between first `[` and `]`) is exactly 20 space characters (verified by the inline assertion).
    - At 100%, the gradient bar is exactly 20 `█` characters with no partial glyph (verified by the inline assertion).
    - The whole-cell branch (shade/solid/solid-dim) is unchanged — `python -m pytest tests/ -q` exits 0.
    - The gradient bar is always exactly `_BAR_WIDTH` (20) cells at any percentage.
  </acceptance_criteria>
  <done>gradient renders █ full cells + one 1/8-precision boundary glyph + blank track totaling 20 cells; 0%→all blank, 100%→all █; sub-cell math is gradient-only; other presets untouched; never crashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for gradient sub-cell rendering and edge cases</name>
  <files>tests/test_bottom_line.py</files>
  <read_first>
    - tests/test_bottom_line.py lines 244-257 (the ANSI-strip + bar-extraction pattern to reuse) and the synthetic-payload `_run`/`load_fixture` helpers (lines 307-401)
    - claude-statusline.py — the gradient branch implemented in Task 1
  </read_first>
  <behavior>
    - With bar_style="gradient", a 0% context renders a 20-space blank bar (no █, no partial, no ░/▒).
    - With bar_style="gradient", a 100% context renders a 20×█ bar (no partial glyph, no blank).
    - With bar_style="gradient", a mid percentage chosen so total_eighths is not a multiple of 8 renders exactly one partial-block glyph from ▏▎▍▌▋▊▉, with full_cells █ before it and blanks after, totaling 20 cells.
    - The partial glyph index matches the eighth remainder (test asserts the specific expected glyph for the chosen percentage).
    - shade default at 7% is unaffected (regression — reuse/keep existing assertion).
  </behavior>
  <action>
    Add gradient tests to `tests/test_bottom_line.py`. Reuse the ANSI-strip + first-`[`/`]` bar-extraction helper from the existing fill-cell test. Either call `_context_segment(..., bar_style="gradient")` directly on synthetic `{'context_window':{'used_percentage':N}}` payloads, or drive the script via a `[display]\nbar_style = "gradient"` TOML using the config harness — direct function calls are simpler and preferred here. Assert: (1) 0% → bar is 20 spaces, contains no `█`/`▏▎▍▌▋▊▉`; (2) 100% → bar is 20 `█`, contains none of the partial glyphs and no space; (3) a chosen mid percentage whose `round(pct/100*20*8)` is not divisible by 8 → bar length is 20, contains the expected count of `█`, exactly one partial glyph equal to the computed `▏▎▍▌▋▊▉[remainder-1]`, and the rest spaces. Compute the expected glyph in the test from the same eighth formula so the assertion is self-documenting. Keep the existing default-shade test intact.
  </action>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/claude_statusline && python -m pytest tests/test_bottom_line.py -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_bottom_line.py -q` exits 0.
    - `python -m pytest tests/ -q` exits 0 (full suite green).
    - test_bottom_line.py contains gradient assertions including 0% and 100% edge cases (verify: `grep -c gradient tests/test_bottom_line.py` >= 2).
    - test_bottom_line.py asserts a specific partial-block glyph from the set ▏▎▍▌▋▊▉ for a mid percentage (verify: `grep -cE '▏|▎|▍|▌|▋|▊|▉' tests/test_bottom_line.py` >= 1).
  </acceptance_criteria>
  <done>Gradient rendering verified at 0%, 100%, and a fractional mid percentage with the correct boundary glyph; default-shade regression preserved; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config → render | `display.bar_style = "gradient"` selects the gradient branch. Same already-safe TOML parse path as Plan 01; no new input source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | gradient eighth-block math in `_context_segment` | mitigate | total_eighths is clamped to [0, _BAR_WIDTH*8] and the cell string is padded/truncated to exactly _BAR_WIDTH, so out-of-range pct (already pct_int-validated, finite) cannot over/underflow the bar or index the partial set out of range; the branch stays inside _context_segment's try/except returning None on any failure (RUN-02). |
| T-03-04 | Tampering | rendered ANSI output | accept | gradient glyphs come from a fixed internal set; no user input is echoed. Pure local terminal render — no network/auth/PII. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` exits 0.
- bar_style="gradient" at 0% → 20-space bar; at 100% → 20×█ bar; at a fractional pct → █ run + one ▏▎▍▌▋▊▉ glyph + blanks, 20 cells total.
- shade/solid/solid-dim output unchanged from Plan 01 (D-04).
</verification>

<success_criteria>
- gradient preset renders full █ cells + one 1/8-precision boundary glyph + blank track (D-02).
- Sub-cell precision is gradient-only (D-04); 0%/100% edge cases correct.
- gradient has one fixed look, controlled solely by bar_style (D-05).
- Bar is always exactly 20 cells; never crashes (RUN-02).
- All tests pass.
</success_criteria>

<output>
Create `.planning/phases/03-presets-for-the-type-of-block-fill-for-the-progress-bar-incl/03-02-SUMMARY.md` when done.
</output>
