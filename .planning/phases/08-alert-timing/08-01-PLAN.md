---
phase: 08-alert-timing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - claude-statusline.py
  - tests/test_weather_alerts.py
autonomous: true
requirements: [WX-07, WX-08, WX-09, WX-10]

must_haves:
  truths:
    - "A datetime later today formats as a bare 12hr time like `3:00 PM` (no leading hour zero, uppercase AM/PM, space before AM/PM) (D-05)"
    - "A datetime on the next calendar day formats as `Tmrw. at 3:00 PM` (D-05)"
    - "A datetime 2-6 calendar days ahead formats as `<Wkdy> at 3:00 PM` (abbreviated weekday, no period) (D-05)"
    - "A datetime 7+ calendar days ahead formats as the dated form `Jul 3 at 3:00 PM` (D-05)"
    - "A None / unparseable / malformed-string input returns None (never raises, never fakes a value) (D-10)"
    - "Given start in the future, the upcoming/active decision selects upcoming and uses the start time; given start at-or-before now, it selects active and uses the end time (D-03)"
    - "Anomaly guard: start in the future but end missing/unparseable/past returns no timing fragment (D-03a)"
  artifacts:
    - path: "claude-statusline.py"
      provides: "Pure formatter _fmt_alert_time and timing-fragment builder _fmt_alert_timing"
      contains: "def _fmt_alert_time"
    - path: "tests/test_weather_alerts.py"
      provides: "Unit tests for the formatter and timing decision logic"
      contains: "class TestAlertTimingFormatter"
  key_links:
    - from: "claude-statusline.py:_fmt_alert_timing"
      to: "claude-statusline.py:_fmt_alert_time"
      via: "direct call after parsing onset/ends ISO strings to local datetimes"
      pattern: "_fmt_alert_time\\("
---

<objective>
Create the pure, stdlib-only timing layer for Phase 8: a WX-10-styled relative-day time
formatter (`_fmt_alert_time`) and the upcoming-vs-active decision builder
(`_fmt_alert_timing`) that turns raw NWS `onset`/`effective`/`ends`/`expires` strings into a
`from <time>` or `until <time>` fragment. No network, no astral, no venv — fully testable
under system `python3`.

Purpose: This is the testable core of the phase. Plan 02 only splices the fragment into the
render site. Isolating the logic here (TDD) keeps the relative-day branching and the
field-precedence / anomaly-guard rules under deterministic unit tests independent of the
weather segment's import gating.

Output: Two new functions in `claude-statusline.py` (`_fmt_alert_time`, `_fmt_alert_timing`)
and a new `TestAlertTimingFormatter` class in `tests/test_weather_alerts.py`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alert-timing/08-CONTEXT.md
@.planning/phases/08-alert-timing/08-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing analog the new formatter mirrors. Extracted from claude-statusline.py. -->

fmt_reset (claude-statusline.py L2537-2559) — the closest STYLE analog. DO NOT call it;
its style is `5:15pm` (lowercase, no space, no "at"). Mirror only the strftime idioms:
  - `%-I` strips the leading zero on the 12h hour (Linux)
  - `%M` zero-padded minutes
  - `%p` AM/PM (uppercase on Linux — keep uppercase, do NOT `.lower()`)
  - `%a` abbreviated weekday (e.g. `Mon`)
  - `%b` abbreviated month (e.g. `Jul`); `%-d` day without leading zero (Linux) — for the
    7+-day dated arm
  - error idiom: `except (OSError, OverflowError, ValueError, TypeError): return None`

ISO-8601 parse idiom (claude-statusline.py L1351-1361, L2879):
  s2 = s.replace("Z", "+00:00"); dt = datetime.fromisoformat(s2); dt.astimezone()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD the _fmt_alert_time relative-day formatter</name>
  <files>tests/test_weather_alerts.py, claude-statusline.py</files>
  <read_first>
    - claude-statusline.py L2537-2559 (fmt_reset — the strftime/style analog to mirror, NOT call)
    - tests/test_weather_alerts.py L36-41 (_load_script_module convention)
    - tests/test_weather_alerts.py — TestSelectAlert class (a pure-formatter test class with NO _WEATHER_OK guard, the template for this class)
    - .planning/phases/08-alert-timing/08-CONTEXT.md D-05 (the four relative-day arms + 7-day dated threshold) and "Claude's Discretion" (strftime patterns)
    - .planning/phases/08-alert-timing/08-PATTERNS.md "_fmt_alert_time(dt, now)" section (branching spec, strftime idioms)
  </read_first>
  <behavior>
    Signature: `_fmt_alert_time(dt, now)` where both are tz-aware (or naive-consistent) datetimes; returns str or None.
    - dt at 15:00 the same calendar date as `now` → `"3:00 PM"` (no leading hour zero, single space before uppercase AM/PM)
    - dt at 09:05 same day → `"9:05 AM"` (minutes zero-padded, hour not)
    - dt on the next calendar day at 15:00 → `"Tmrw. at 3:00 PM"`
    - dt 3 calendar days ahead at 15:00 → `"<Wkdy> at 3:00 PM"` where Wkdy is the `%a` weekday, no trailing period (e.g. `"Wed at 3:00 PM"`)
    - dt exactly 6 calendar days ahead → still `<Wkdy> at` form
    - dt exactly 7 calendar days ahead at 15:00 → dated form `"<Mon> <D> at 3:00 PM"` (e.g. `"Jul 3 at 3:00 PM"`, no leading zero on day)
    - dt 30 days ahead → dated form
    - dt is None → None
    - dt is a non-datetime (e.g. an int or a garbage string) → None (no exception)
    - Day arithmetic uses calendar-date deltas: `(dt_local.date() - now_local.date()).days` (NOT 24h windows), threshold `>= 7` for the dated arm, `== 0` same day, `== 1` Tmrw., `2..6` weekday.
  </behavior>
  <action>
    Write the `TestAlertTimingFormatter` unit-test class FIRST (RED), appended to
    tests/test_weather_alerts.py after the existing TestWeatherSegmentAlertOverrideV2 class.
    Construct explicit `datetime` objects with `datetime(2026, 6, 20, 15, 0)` etc. and a fixed
    `now` — do NOT use real wall-clock `datetime.now()` in assertions (anchor `now` so the
    relative-day arms are deterministic). This class is a PURE-formatter test: no _WEATHER_OK
    guard (mirror TestSelectAlert), call `self.mod._fmt_alert_time(...)` directly. Cover every
    `<behavior>` case above including the +6 / +7 boundary and the None/garbage returns.
    Run the tests, confirm they fail (function absent).

    Then implement `_fmt_alert_time(dt, now)` in claude-statusline.py near fmt_reset (GREEN).
    Branch on `(dt.astimezone().date() - now.astimezone().date()).days` — but only after
    converting; guard the whole body in `try/except Exception: return None` so a non-datetime
    or unconvertible input omits silently (D-10). Build the bare time with `dt.strftime("%-I:%M %p")`
    (space before %p, NO .lower() — uppercase per WX-10). Same-day → bare time; ==1 → `f"Tmrw. at {time}"`;
    2..6 → `f"{dt.strftime('%a')} at {time}"`; >=7 → `f"{dt.strftime('%b')} {dt.strftime('%-d')} at {time}"`.
    Reuse the `%-I`/`%-d` Linux idioms from fmt_reset per D-05's "Claude's Discretion". Re-run, confirm green.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter -v 2>&1 | tail -20; python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter</automated>
  </verify>
  <acceptance_criteria>
    - claude-statusline.py contains `def _fmt_alert_time(`
    - tests/test_weather_alerts.py contains `class TestAlertTimingFormatter`
    - `python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter` exits 0 with these tests RUN (not skipped — no _WEATHER_OK guard on this class)
    - A datetime at 15:00 same day as anchored `now` returns exactly `"3:00 PM"`
    - A datetime 1 calendar day ahead returns a string starting `"Tmrw. at "`
    - A datetime 7 calendar days ahead returns the `%b`-month dated form (contains `" at "`, starts with an abbreviated month, no `"Tmrw"`/weekday-only prefix)
    - `_fmt_alert_time(None, now)` returns None and `_fmt_alert_time("garbage", now)` returns None (no exception raised)
  </acceptance_criteria>
  <done>All four relative-day arms plus the None/garbage omit-cases are covered by passing tests run under system python3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD the _fmt_alert_timing upcoming/active decision builder</name>
  <files>tests/test_weather_alerts.py, claude-statusline.py</files>
  <read_first>
    - claude-statusline.py L1351-1361 (_parse_dt ISO idiom) and L2879 (inline replace("Z","+00:00") idiom)
    - claude-statusline.py — the just-added _fmt_alert_time function (Task 1)
    - .planning/phases/08-alert-timing/08-CONTEXT.md D-02 (middot order), D-03 (start/end precedence + start>now decision), D-03a (anomaly guard)
    - .planning/phases/08-alert-timing/08-PATTERNS.md "ISO-8601 Timestamp Parsing" + "Alert Property Access Pattern"
  </read_first>
  <behavior>
    Signature: `_fmt_alert_timing(start_raw, end_raw, now=None)` taking the two RAW NWS ISO
    strings already chosen by the caller (onset-or-effective, ends-or-expires). Returns the
    fragment text WITHOUT the leading separator (caller prepends ` · `), i.e. `"from 6:00 PM"`
    or `"until 3:00 PM"`, or None to omit.
    - start parses to a datetime AFTER now, end parses fine → upcoming → `"from " + _fmt_alert_time(start, now)`
    - start parses to a datetime AT or BEFORE now (incl. ==now), end parses → active → `"until " + _fmt_alert_time(end, now)`
    - start in the future BUT end is None/unparseable/in the past → None (D-03a anomaly guard: omit, do not show `from`)
    - active branch but end is None/unparseable → None (can't show `until` → omit, D-10)
    - upcoming branch but the chosen start formats to None (e.g. _fmt_alert_time fails) → None
    - both start_raw and end_raw None → None
    - any parse exception anywhere → None (never raises)
    - Timestamps with trailing `Z` are parsed (replace `Z`→`+00:00`) and converted to local via `.astimezone()` before comparison/formatting.
    - `now` defaults to a tz-aware local now when not passed, but tests always pass an explicit anchored `now`.
  </behavior>
  <action>
    Append decision-logic tests to TestAlertTimingFormatter (or a sibling pure class) FIRST
    (RED). Build raw ISO strings like `"2026-06-20T18:00:00-05:00"` and an anchored `now`;
    cover: upcoming-with-valid-end, active-now, the D-03a anomaly (future start + past/missing
    end → None), active-with-missing-end → None, both-None → None, and a malformed string →
    None. Run, confirm failing.

    Then implement `_fmt_alert_timing(start_raw, end_raw, now=None)` in claude-statusline.py
    directly below _fmt_alert_time (GREEN). Use an inner parse helper mirroring _parse_dt
    (L1351-1361): on falsy/garbage return None, else `datetime.fromisoformat(s.replace("Z","+00:00")).astimezone()`.
    Default `now` to `datetime.now().astimezone()` when None. Decide per D-03: if start is not
    None and start > now → upcoming, fmt the start via _fmt_alert_time; BEFORE returning, apply
    D-03a — if end is None/unparseable/<= now, return None (omit). Else (start None or start <= now)
    → active: require a parseable end, fmt via _fmt_alert_time, prefix `"until "`. Wrap the whole
    body in `try/except Exception: return None`. Return None whenever the underlying _fmt_alert_time
    yields None. Re-run, confirm green.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter -v 2>&1 | tail -25; python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter</automated>
  </verify>
  <acceptance_criteria>
    - claude-statusline.py contains `def _fmt_alert_timing(`
    - A future start with a valid end returns a string starting `"from "` (active label absent)
    - A start at-or-before now with a valid end returns a string starting `"until "`
    - D-03a: future start with end None or end earlier than now returns None (verified by an explicit test asserting `assertIsNone`)
    - Both raw inputs None returns None; a malformed ISO string returns None without raising
    - Trailing-`Z` ISO strings parse correctly (a `...T18:00:00Z` input produces a non-None fragment in the appropriate branch)
    - `python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter` exits 0
  </acceptance_criteria>
  <done>The upcoming/active decision, field precedence is exercised by the caller (Plan 02), and the D-03a anomaly guard are proven by passing pure tests under system python3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached NWS JSON → formatter | `onset`/`ends`/`effective`/`expires` originate from external NWS responses stored in the local alerts cache; the formatter treats every timestamp string as untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | `_fmt_alert_time` / `_fmt_alert_timing` parsing of malformed/oversized/garbage timestamp strings | mitigate | Whole body wrapped in `try/except Exception: return None`; non-datetime and unparseable inputs return None instead of raising or hanging (omit-not-fake, D-10). Unit tests assert None on garbage. |
| T-08-02 | Tampering | ANSI/control-char injection via timing text | accept | Timing text is machine-formatted from parsed `datetime` objects via fixed `strftime` patterns — it contains only digits, `:`, spaces, fixed words, `%a`/`%b`/`%p` locale tokens. No raw NWS string reaches output, so no sanitization is needed (noted explicitly). Event text sanitization stays in Plan 02's render site. |
| T-08-03 | Denial of Service | timezone/locale edge cases (`.astimezone()` on odd offsets) | mitigate | Conversion happens inside the guarded try/except; an unconvertible tz or locale error returns None rather than crashing the render. |
</threat_model>

<verification>
- `python3 -m unittest tests.test_weather_alerts.TestAlertTimingFormatter` exits 0 with all timing/format tests RUN (unskipped — pure functions, no venv).
- `python3 -m unittest tests.test_weather_alerts` still exits 0 (no regression in the existing 92 tests; pre-existing _WEATHER_OK skips unchanged).
- `python3 -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('m','claude-statusline.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print(callable(m._fmt_alert_time), callable(m._fmt_alert_timing))"` prints `True True`.
</verification>

<success_criteria>
- `_fmt_alert_time` renders all four WX-10 relative-day arms exactly (same-day bare, Tmrw., weekday, 7+-day dated) and returns None on bad input (success criterion 3 + 4).
- `_fmt_alert_timing` distinguishes upcoming (`from`, onset→effective via caller) from active (`until`, ends→expires via caller) per WX-07/08/09, applies the D-03a anomaly guard, and omits on null/unparseable timestamps (success criterion 4).
- All new tests run and pass under system `python3` with no venv and no network.
</success_criteria>

<output>
Create `.planning/phases/08-alert-timing/08-01-SUMMARY.md` when done.
</output>
