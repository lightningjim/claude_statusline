---
phase: 08-alert-timing
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - claude-statusline.py
  - tests/test_weather_alerts.py
autonomous: true
requirements: [WX-07, WX-08, WX-09, WX-10]

must_haves:
  truths:
    - "An upcoming alert (start in the future) renders the primary alert detail as `{glyph} {event} · from <time>` before the tally"
    - "An active alert (start at/before now) renders `{glyph} {event} · until <time>` before the tally"
    - "The timing fragment is set off by ` · ` (U+00B7) with a single space on each side, and the tally (when present) stays the trailing element after the timing"
    - "The whole detail (glyph + event + timing + tally) is wrapped in a single class color + RESET, so the timing inherits the alert's class color/intensity"
    - "An alert with null/unparseable relevant timestamps falls back to `{glyph} {event}  {tally}` with no time fragment and no error"
    - "Timing appears only on the primary (best) alert; tallied remainder stays a bare per-class count"
  artifacts:
    - path: "claude-statusline.py"
      provides: "Timing-fragment splice in _weather_segment Step 3c"
      contains: "_fmt_alert_timing"
    - path: "tests/test_weather_alerts.py"
      provides: "Integration render tests for alert timing"
      contains: "class TestWeatherSegmentAlertTiming"
  key_links:
    - from: "claude-statusline.py:_weather_segment"
      to: "claude-statusline.py:_fmt_alert_timing"
      via: "props onset/effective + ends/expires passed to _fmt_alert_timing, spliced into detail"
      pattern: "_fmt_alert_timing\\("
    - from: "detail string"
      to: "trailing_detail color wrap"
      via: "timing inside detail before the f-string {color}{detail}{RESET}"
      pattern: "\\{color\\}\\{detail\\}\\{RESET\\}"
---

<objective>
Splice the Plan 01 timing fragment into the live render: the `_weather_segment` Step 3c
alert-override block. Build the start/end raw strings from the best alert's CAP properties
(onset->effective, ends->expires), call `_fmt_alert_timing`, and insert ` · {from|until <time>}`
into the existing `detail` between the event and the tally — inside the single class-color
wrap so the timing inherits the alert's color/intensity.

Purpose: Turns the tested core logic into the user-visible behavior the phase exists for.
Output: A modified Step 3c block in `claude-statusline.py` and a new
`TestWeatherSegmentAlertTiming` integration test class.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alert-timing/08-CONTEXT.md
@.planning/phases/08-alert-timing/08-PATTERNS.md
@.planning/phases/08-alert-timing/08-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (claude-statusline.py) — the functions this plan consumes. -->
_fmt_alert_timing(start_raw, end_raw, now=None) -> str | None
    # returns "from <time>" / "until <time>" (NO leading separator) or None to omit
_fmt_alert_time(dt, now) -> str | None

<!-- Existing Step 3c render block (claude-statusline.py L3653-3692) the splice goes into. -->
# props = best.get("properties") or best           (L3663)
# detail = f"{class_glyph} {safe_event}"            (L3684)  <-- splice point is right after this
# if remaining_alerts: detail += f"  {tally}"       (L3686-3689)
# trailing_detail = f"{color}{detail}{RESET}"       (L3690)  <-- single class-color wrap, unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Splice the timing fragment into Step 3c</name>
  <files>claude-statusline.py</files>
  <read_first>
    - claude-statusline.py L3653-3692 (the full Step 3c alert-override block — exact current state)
    - claude-statusline.py — the Plan 01 _fmt_alert_timing / _fmt_alert_time functions
    - .planning/phases/08-alert-timing/08-CONTEXT.md D-01 (single color wrap), D-02 (middot order), D-04 (best-only)
    - .planning/phases/08-alert-timing/08-PATTERNS.md "_weather_segment Step 3c — integration splice" (splice point + pattern)
  </read_first>
  <action>
    In `_weather_segment` Step 3c, insert the timing build between L3684
    (`detail = f"{class_glyph} {safe_event}"`) and L3686 (`if remaining_alerts:`), so render
    order becomes `{class_glyph} {safe_event} · {from|until <time>}  {tally}`.

    Read the timing fields from the alert's properties using the guarded
    `props = best.get("properties") or best` idiom already present at L3663 (reuse the existing
    `props` if in scope, else re-read). Choose start = `props.get("onset") or props.get("effective")`
    and end = `props.get("ends") or props.get("expires")` (D-03 field precedence). Call
    `_fmt_alert_timing(start_raw, end_raw, now=now)` — `now` is already in scope (used by
    section_within_ceiling above). If it returns a non-None fragment, append ` · ` (middot
    U+00B7 with one space on each side, D-02) plus the fragment to `detail`. Wrap the splice in
    `try/except Exception: pass` so any timing failure silently omits the fragment and falls
    back to `{glyph} {event}  {tally}` (D-10 omit-not-fake, success criterion 4).

    Do NOT change L3690 `trailing_detail = f"{color}{detail}{RESET}"` — the timing is now inside
    `detail`, so it inherits the class color and Immediate+Observed BOLD intensity automatically
    (D-01). Do NOT add timing to the tally path (D-04: best-only). Keep the splice to the single
    guarded block described — no other edits to the block.
  </action>
  <verify>
    <automated>python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('m','claude-statusline.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print('import ok')"; grep -c "_fmt_alert_timing(" claude-statusline.py</automated>
  </verify>
  <acceptance_criteria>
    - claude-statusline.py imports cleanly (the one-liner exec prints `import ok`)
    - `grep -c "_fmt_alert_timing(" claude-statusline.py` >= 2 (the definition in Plan 01 plus the new call site in Step 3c)
    - The middot codepoint U+00B7 with surrounding spaces appears in the splice (` · `)
    - The splice sits AFTER `detail = f"{class_glyph} {safe_event}"` and BEFORE the `if remaining_alerts:` tally append (the timing precedes the tally in render order)
    - L3690 `trailing_detail = f"{color}{detail}{RESET}"` is unchanged (single color wrap still wraps the whole detail)
    - The splice body is guarded by `try/except Exception: pass`
  </acceptance_criteria>
  <done>The best alert's detail string carries a class-colored ` · from|until <time>` fragment before the tally, with a silent fallback on bad data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration render tests for alert timing</name>
  <files>tests/test_weather_alerts.py</files>
  <read_first>
    - tests/test_weather_alerts.py L36-41 (_load_script_module), L50-76 (_make_alert factory)
    - tests/test_weather_alerts.py — TestWeatherSegmentAlertOverrideV2 class incl. _run_segment helper (~L1236-1247), _make_active_cache (~L1249-1254), and the _WEATHER_OK skip guard (~L965-966)
    - .planning/phases/08-alert-timing/08-CONTEXT.md "Specific Ideas" (worked examples), D-02/D-03/D-03a/D-04
    - claude-statusline.py — the modified Step 3c block (Task 1) and _fmt_alert_timing
  </read_first>
  <behavior>
    New class `TestWeatherSegmentAlertTiming` in tests/test_weather_alerts.py, after
    TestWeatherSegmentAlertOverrideV2. Pin `icon_set="emoji"` cfg and copy the `_run_segment` /
    `_make_active_cache` helpers; every test calls `_weather_segment` so each begins with the
    `_WEATHER_OK` skip guard. Use `onset`/`ends` (and `effective`/`expires` for fallback cases)
    set RELATIVE to `time`/`datetime.now()` so the tests stay correct regardless of wall-clock —
    e.g. onset = now + 2h for upcoming, ends = now + 2h for active.
    - Upcoming (onset 2h in the future, valid ends further out) → output contains ` · from ` and does NOT contain `until`
    - Active (onset 1h in the past, ends 2h in the future) → output contains ` · until ` and does NOT contain `from`
    - Fallback: onset null but effective set in the future → still renders ` · from ` (effective used)
    - Fallback: ends null but expires set → active alert renders ` · until ` (expires used)
    - Null/unparseable: all four timestamps null or garbage → output contains the event but NO ` · ` middot fragment (omit-not-fake)
    - D-03a anomaly: onset in the future but ends in the past → output contains the event but NO ` · ` fragment
    - D-04 best-only: with 2+ alerts, the timing ` · ` appears at most once (only on the primary), and the tally count still renders
    - The middot fragment, when present, falls between the event text and the tally in the output string
  </behavior>
  <action>
    Append `TestWeatherSegmentAlertTiming` to tests/test_weather_alerts.py. Reuse the
    `_make_alert` factory and inject `onset`/`effective`/`ends`/`expires` into the returned
    `properties` dict (the factory already accepts `expires`; set the others by mutating the
    returned dict or via a thin local helper). Compute timestamps from `datetime.now().astimezone()`
    offsets and serialize with `.isoformat()` so they are valid NWS-style ISO strings. Assert on
    substrings of the `_weather_segment` return (`· from`, `· until`, absence of the middot for
    omit cases). Guard every test with the `_WEATHER_OK` skip. Run the class and confirm green
    (or skipped when venv absent — see verify).
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_weather_alerts.TestWeatherSegmentAlertTiming -v 2>&1 | tail -25; python3 -m unittest tests.test_weather_alerts</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_weather_alerts.py contains `class TestWeatherSegmentAlertTiming`
    - `python3 -m unittest tests.test_weather_alerts.TestWeatherSegmentAlertTiming` exits 0 (tests pass under venv, or are explicitly skipped via the `_WEATHER_OK` guard under system python3 — never error)
    - `python3 -m unittest tests.test_weather_alerts` exits 0 (full module, no regressions)
    - An upcoming-alert test asserts the output contains `· from ` and not `until`
    - An active-alert test asserts the output contains `· until ` and not `from`
    - An onset-null/effective-set test asserts `· from ` still renders (fallback proven)
    - An all-null-timestamps test asserts the event renders but the ` · ` middot fragment is absent
    - A D-03a test (future onset + past ends) asserts the ` · ` fragment is absent
    - A multi-alert test asserts the timing middot appears at most once and the tally count is present
  </acceptance_criteria>
  <done>The upcoming/active labels, onset->effective and ends->expires fallbacks, omit-on-null, D-03a guard, and best-only scope are all covered by integration tests that pass (or cleanly skip without venv).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached NWS JSON -> render | The best alert's `onset`/`ends`/`effective`/`expires` come from the external NWS-sourced alerts cache and cross into the render path at Step 3c. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | Step 3c timing splice in `_weather_segment` | mitigate | The splice is wrapped in `try/except Exception: pass`, nested inside the existing block-level `try/except` (L3656/L3691) and the whole-segment `try/except -> return None`. A malformed timestamp omits the fragment and never breaks the bar (exit-fast contract, D-10). Tested via the all-null and D-03a cases. |
| T-08-05 | Tampering | ANSI/control-char injection through the timing fragment | accept | The timing fragment is produced entirely by `_fmt_alert_timing` from parsed datetimes (fixed `strftime` output) — no raw NWS string is concatenated into `detail`, so it carries no escape/control-char vector and needs no sanitization. Event text continues to be sanitized at L3676-3679 (unchanged). |
| T-08-06 | Information disclosure | leaking malformed/oversized NWS payload into output | accept | Only machine-formatted clock text is emitted; the raw timestamp strings are never rendered, so oversized/garbage payloads cannot reach the terminal via this path. |
</threat_model>

<verification>
- `python3 -m unittest tests.test_weather_alerts` exits 0 (full module passes; new timing integration tests pass under venv or cleanly skip under system python3).
- `python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('m','claude-statusline.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print('import ok')"` prints `import ok`.
- `grep -c "_fmt_alert_timing(" claude-statusline.py` >= 2.
</verification>

<success_criteria>
- The primary weather alert renders ` · from <time>` when upcoming and ` · until <time>` when active, in the class color, with the tally trailing (WX-07/08/09, success criteria 1-2).
- onset->effective and ends->expires fallbacks work; null/unparseable timestamps and the D-03a anomaly omit the fragment with no error (WX-08/09, success criterion 4).
- Relative-day formatting (from Plan 01) is visible end-to-end in the rendered segment (WX-10, success criterion 3).
- The full test module passes / cleanly skips under system `python3`; no regressions.
</success_criteria>

<output>
Create `.planning/phases/08-alert-timing/08-02-SUMMARY.md` when done.
</output>
