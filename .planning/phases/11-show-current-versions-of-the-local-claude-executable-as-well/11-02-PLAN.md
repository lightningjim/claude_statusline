---
phase: 11-show-current-versions-of-the-local-claude-executable-as-well
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false
requirements: [VER-03, VER-04]

must_haves:
  truths:
    - "In Kyle's terminal, the two new Nerd Font version glyphs render as real, correct icons (not tofu/boxes or a wrong glyph)"
    - "The dimmed versions fragment sits at the trailing end of the bottom line, after the Claude-status segment, and reads as unobtrusive reference info at a glance"
  artifacts: []
  key_links: []
---

<objective>
Visually confirm — in Kyle's actual terminal and font — that the new version fragment renders
correctly: both Nerd Font glyphs are real icons (not tofu/boxes), the fragment is dimmed,
trailing, and after the Claude-status segment, and the version numbers are accurate.

Purpose: automated tests prove the codepoints are single-cell and present in EXPECTED_CONSTANTS,
but they CANNOT prove the glyph actually renders in Kyle's terminal font (the Phase 8 lesson:
a "wrong weather icon" was a missing Nerd Font in WebStorm's terminal, not a code bug). Kyle is
a precision visual reviewer; this glyph render is exactly the class of detail he catches. This
checkpoint exists separately from the implementation plan per the checkpoint-vs-implementation
split rule.
Output: human confirmation (or glyph-swap feedback) on the rendered fragment.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/4.0.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/4.0.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-show-current-versions-of-the-local-claude-executable-as-well/11-CONTEXT.md
@.planning/phases/11-show-current-versions-of-the-local-claude-executable-as-well/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visually verify the rendered version fragment in the terminal</name>
  <files>none (verification-only — no files modified)</files>
  <what-built>
    Plan 11-01 added a dimmed trailing fragment to the bottom line showing the running Claude
    version (from stdin `version`) and the active GSD plugin version (from
    installed_plugins.json), each led by a Nerd Font glyph (with a text-label fallback when
    icon_set != "nerd"). All omit-not-fake paths and an injection guard are covered by passing
    automated tests under /usr/bin/pytest.
  </what-built>
  <action>
    Pause for human visual verification. Present the rendered bottom line to Kyle and collect
    approval or a glyph-swap instruction. Do not auto-approve this gate (Kyle reviews glyph
    rendering finely — see memory user-precision-review and the Phase 8 font lesson). If Kyle
    names a replacement codepoint, swap it in Plan 11-01's `_NF_VERSION_*` constant, re-validate
    it as single-codepoint against EXPECTED_CONSTANTS, and re-render before completing the phase.
  </action>
  <how-to-verify>
    1. Render with the real stdin example in your terminal:
         `python3 claude-statusline.py < .examples/claude_stdin.json`
    2. On the SECOND output line, find the trailing block (after the Claude-status segment).
       Confirm:
       a. Two glyphs render as real Nerd Font icons — NOT tofu boxes (▯), question marks, or a
          visibly wrong/unintended icon. (If a glyph is tofu, your terminal font is the cause —
          same root cause as Phase 8 — or the codepoint choice needs swapping.)
       b. The fragment is visibly dimmed relative to the rest of the line.
       c. The two version numbers are correct: Claude shows "2.1.154" (from the example payload)
          and GSD shows "4.0.0" (the active ledger version on this machine).
       d. The fragment is the last block on the line and reads as unobtrusive reference info.
    3. (Optional) Run live inside a Claude Code session to see your real running Claude version
       in place of the example's 2.1.154.
  </how-to-verify>
  <verify>
    <human-check>Kyle confirms both glyphs render as real icons, the fragment is dimmed and trailing after the Claude-status segment, and the version numbers are accurate.</human-check>
  </verify>
  <acceptance_criteria>
    - Both glyphs render as intended Nerd Font icons (no tofu / no wrong glyph), OR Kyle names a
      replacement codepoint to swap in
    - The fragment is dimmed, trailing, and after the Claude-status segment
    - Version numbers are accurate (2.1.154 / 4.0.0 for the example payload)
  </acceptance_criteria>
  <done>Kyle approves the rendered fragment, or a named glyph swap is applied, re-validated, and re-rendered.</done>
  <resume-signal>Type "approved", or describe the glyph/layout issue (e.g. "Claude glyph is tofu — use U+F0xx instead").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan is verification-only and modifies no files. All security
mitigations were implemented and tested in Plan 11-01 (see its `<threat_model>`: T-11-01 escape
injection, T-11-02 byte-capped read, T-11-03 never-crash guards, T-11-04 fixed-path read,
T-11-SC no new packages).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-V | n/a | verification-only plan, files_modified empty | accept | No code or config changes; no new attack surface. Security is fully covered by Plan 11-01. |
</threat_model>

<verification>
- Human confirms the two glyphs render as real icons, the fragment is dimmed and trailing, and
  the version numbers are accurate.
</verification>

<success_criteria>
- Kyle approves the rendered version fragment, or specifies a glyph swap which is then applied
  (single-codepoint, re-validated against EXPECTED_CONSTANTS) before phase completion.
</success_criteria>

<output>
Create `.planning/phases/11-show-current-versions-of-the-local-claude-executable-as-well/11-02-SUMMARY.md` when done.
</output>
