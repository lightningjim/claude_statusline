---
phase: 10-tech-debt-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .planning/milestones/v1.0-REQUIREMENTS.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05]

must_haves:
  truths:
    - "pyproject.toml version equals _APP_VERSION in claude-statusline.py (both read 0.2.0)"
    - "The WX-05 requirement text and the claude-statusline.py weather_ttl default cite the same TTL number (10 min)"
    - "The current REQUIREMENTS.md traceability table and footer reflect what actually shipped (LINK-01..03 complete, DEBT-01..05 complete, current date)"
    - "The weather/sun/alert tests run under system python3 and exit 0 — each either passes or skips with a stated reason (never errors, never a silent/empty skip)"
    - "The requirements-completed SUMMARY-frontmatter audit item is explicitly resolved with a documented note — no silent gap left in the audit trail"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package version metadata aligned to runtime constant"
      contains: 'version = "0.2.0"'
    - path: ".planning/milestones/v1.0-REQUIREMENTS.md"
      provides: "WX-05 requirement text aligned to the code default"
      contains: "WX-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled traceability table + refreshed footer for the v1.1 milestone"
      contains: "Last updated:"
    - path: ".planning/milestones/v1.0-MILESTONE-AUDIT.md"
      provides: "Resolution notes retiring the requirements-completed backfill (DEBT-02) and recording the system-python weather-test resolution (DEBT-04)"
      contains: "DEBT-02"
  key_links:
    - from: "pyproject.toml"
      to: "claude-statusline.py"
      via: "version string equals _APP_VERSION"
      pattern: 'version = "0.2.0"'
    - from: ".planning/milestones/v1.0-REQUIREMENTS.md"
      to: "claude-statusline.py"
      via: "WX-05 TTL text equals weather_ttl=600 (10 min)"
      pattern: "10 ?min"
---

<objective>
Clear all five items of the v1.0 milestone-audit tech-debt block (DEBT-01..05): version metadata, planning-artifact reconciliation, and weather-test environment coverage. After this plan, version metadata, planning artifacts, and test coverage are consistent and accurate, with no silent gaps left in the audit trail.

Purpose: Close out residual tracking/metadata debt carried from v1.0 so the v1.1 milestone audit trail is internally consistent. No runtime behavior changes — every edit is metadata, planning markdown, or test verification/documentation.
Output: Bumped pyproject version, aligned WX-05 TTL text, reconciled REQUIREMENTS.md table/footer, and documented resolutions for the SUMMARY-frontmatter and system-python-test items.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.0-REQUIREMENTS.md

<facts>
<!-- Source-of-truth values extracted during planning. Use directly — no re-derivation needed. -->
- claude-statusline.py:878 → `_APP_VERSION = "0.2.0"` (canonical version)
- pyproject.toml:3 → `version = "0.1.0"` (the lagging value to bump)
- claude-statusline.py:175 → `"weather_ttl": 600,  # 10 min` (canonical TTL; NO code change in this plan)
- .planning/milestones/v1.0-REQUIREMENTS.md:36 → WX-05 text says "Weather (~15min) ..." (the outlier prose to correct)
- v1.0 phase SUMMARY dirs (01..07.1) were CLEARED in commit a413558 ("chore: clear v1.0 phase directories for v1.1 milestone"); they exist only in git history. v1.1 SUMMARYs (08, 09) already populate `requirements_completed`.
- System python3 has `requests` but NOT `astral`; under `/usr/bin/pytest` the weather suite already exits 0 (228 passed, 47 skipped, 0 empty skip reasons). `_WEATHER_OK = _ASTRAL_OK and _REQUESTS_OK` gates the astral-dependent branches.
- Phase 9 (LINK-01..03) is COMPLETE per STATE.md but REQUIREMENTS.md still rows them "Pending".
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync pyproject version (DEBT-01) and align WX-05 TTL text (DEBT-05)</name>
  <files>pyproject.toml, .planning/milestones/v1.0-REQUIREMENTS.md</files>
  <read_first>
    - pyproject.toml (the version line being changed)
    - claude-statusline.py lines 875-880 (the canonical `_APP_VERSION = "0.2.0"` — source of truth for DEBT-01)
    - claude-statusline.py lines 173-181 (the canonical `weather_ttl: 600  # 10 min` — source of truth for DEBT-05)
    - .planning/milestones/v1.0-REQUIREMENTS.md line 36 (the WX-05 text being corrected)
  </read_first>
  <action>
    DEBT-01: In pyproject.toml, change the `version` field from `0.1.0` to `0.2.0` so it is byte-equal to `_APP_VERSION` in claude-statusline.py. Do not touch any other field.

    DEBT-05: In .planning/milestones/v1.0-REQUIREMENTS.md, correct the WX-05 line so its cache-TTL figure matches the code default: change "Weather (~15min)" to "Weather (~10min)". Leave the "alerts (~5min)" figure unchanged (it already matches alerts_ttl=300). Make no change to claude-statusline.py — the code value (600s / 10 min) is canonical and stays.

    ASSUMPTION (DEBT-05, vetoable at execute time): the planner picked the CODE value (10 min) as canonical and is correcting the requirement prose to match, rather than raising the code TTL to 15 min. Rationale: fresher weather is preferable for a meteorologist-author, the 10-min default already matches PROJECT.md/ROADMAP.md, and changing prose is lower-risk than changing runtime behavior. If the user prefers 15 min instead, change `weather_ttl` to 900 in claude-statusline.py:175 and set the WX-05 text to "~15min" — but the default choice is 10 min.
  </action>
  <verify>
    <automated>grep -q '^version = "0.2.0"$' pyproject.toml &amp;&amp; grep -q '_APP_VERSION = "0.2.0"' claude-statusline.py &amp;&amp; grep -q 'WX-05.*~10min' .planning/milestones/v1.0-REQUIREMENTS.md &amp;&amp; grep -q '"weather_ttl": *600' claude-statusline.py &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^version = "0.2.0"$' pyproject.toml` matches (exactly one line) AND `grep '_APP_VERSION = "0.2.0"' claude-statusline.py` matches — the two version strings are byte-equal.
    - `grep '^version = "0.1.0"' pyproject.toml` returns nothing (old value gone).
    - The WX-05 line in v1.0-REQUIREMENTS.md contains "~10min" and no longer contains "~15min".
    - `grep '"weather_ttl": *600' claude-statusline.py` still matches (code TTL unchanged at 10 min).
  </acceptance_criteria>
  <done>pyproject version == _APP_VERSION == 0.2.0; WX-05 text TTL == code TTL == 10 min.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md traceability table and footer (DEBT-03)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the whole file — traceability table lines ~65-87 and footer)
    - .planning/STATE.md lines 1-31 (confirms Phase 9 COMPLETE; this milestone is v1.1)
    - .planning/phases/09-clickable-links/09-04-SUMMARY.md frontmatter (confirms LINK-01..03 shipped)
  </read_first>
  <action>
    Update the current v1.1 .planning/REQUIREMENTS.md so the traceability table and footer reflect what actually shipped across this milestone (Phases 8, 9, and this Phase 10):
    - LINK-01, LINK-02, LINK-03 rows: change Status from "Pending" to "Complete" (Phase 9 is verified/complete per STATE.md).
    - DEBT-01..DEBT-05 rows: change Status from "Pending" to "Complete" (resolved by this plan).
    - Flip the requirement checkboxes in the body for LINK-01..03 and DEBT-01..05 from `[ ]` to `[x]` to match the table.
    - Footer: change the "Last updated" line to today's date with a reconciliation note, e.g. `*Last updated: 2026-06-25 after Phase 10 tech-debt cleanup (LINK-01..03 + DEBT-01..05 reconciled to shipped)*`. Leave the "Requirements defined: 2026-06-20" line intact.
    - If the Coverage block counts are now inaccurate, correct them so mapped == total and unmapped == 0 still holds.
    Do not alter the WX-07..10 rows (already Complete). Do not invent requirement IDs.
  </action>
  <verify>
    <automated>grep -Eq 'LINK-0[123].*Complete' .planning/REQUIREMENTS.md &amp;&amp; grep -Eq 'DEBT-0[12345].*Complete' .planning/REQUIREMENTS.md &amp;&amp; ! grep -Eq 'LINK-0[123].*Pending|DEBT-0[12345].*Pending' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Last updated: 2026-06-25' .planning/REQUIREMENTS.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - No row in the traceability table for LINK-01..03 or DEBT-01..05 still reads "Pending" (`grep -E 'LINK-0[123].*Pending|DEBT-0[12345].*Pending'` returns nothing).
    - All LINK-01..03 and DEBT-01..05 rows read "Complete".
    - Footer "Last updated" line contains `2026-06-25` and a phase-10 reconciliation note; "Requirements defined: 2026-06-20" line still present.
    - No unchecked `[ ]` remains for LINK-0x or DEBT-0x in the requirement list body.
  </acceptance_criteria>
  <done>Traceability table and footer reflect shipped state for all v1.1 requirements; footer carries the current date.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve and document the SUMMARY-frontmatter (DEBT-02) and system-python weather-test (DEBT-04) audit items</name>
  <files>.planning/milestones/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md (the tech_debt block, frontmatter lines ~15-29 and the "## Tech Debt" prose section ~115-123 — the source of these debt items)
    - tests/test_weather_sun.py lines 135-145 and 220-230 (the `_ASTRAL_OK`/`skipTest` guards)
    - tests/test_weather_fetch.py lines 695-720 and tests/test_weather_alerts.py lines 665-700 (the `_WEATHER_OK` skipTest guards with stated reasons)
  </read_first>
  <action>
    DEBT-04 (verify + document, do NOT fake a pass): Run the full weather/sun/alert test suite under the SYSTEM interpreter and confirm it is runnable there — every test either passes or skips with a stated reason, and the run exits 0 (system python3 has `requests` but not `astral`, so `_WEATHER_OK`/`_ASTRAL_OK` branches self-skip with their existing reason strings). If any test ERRORS (import/collection) or any `skipTest` reason is empty, fix that specific guard so it skips with a clear reason; otherwise no test edit is needed. Then record the resolution.

    DEBT-02 (retire-with-note): Add a resolution note to v1.0-MILESTONE-AUDIT.md formally retiring the `requirements-completed` backfill obligation as redundant, leaving no silent gap. Add a short "## Tech-Debt Resolution (Phase 10)" section near the existing "## Tech Debt" prose, with one explicit line per DEBT item (DEBT-01..05) stating how each was resolved, including:
    - DEBT-02: backfill is RETIRED, not performed — the v1.0 phase SUMMARY files that lacked the field were cleared in commit a413558 ("chore: clear v1.0 phase directories for v1.1 milestone") and exist only in git history; coverage was already independently confirmed by VERIFICATION.md + the REQUIREMENTS traceability table; v1.1 SUMMARYs (08, 09) already populate `requirements_completed` going forward. No silent gap remains.
    - DEBT-04: tests are runnable under system python3 — astral-dependent branches skip-with-reason (astral not installed for the system interpreter; full pass-coverage runs under the .venv interpreter that main() re-execs into). State the exact verification command used.
    - DEBT-01, DEBT-03, DEBT-05: one line each pointing to where each was fixed (pyproject 0.2.0; REQUIREMENTS.md table/footer; WX-05 text → 10 min).

    ASSUMPTION (DEBT-02, vetoable at execute time): the planner chose RETIRE-with-note over backfill, because the SUMMARY files needing backfill no longer exist on disk (cleared at milestone close) — resurrecting cleared files to add a redundant tracking field is higher-risk and lower-value than a clear retirement note. If the user instead wants the field backfilled, the v1.0 SUMMARYs must first be restored from commit a413558, then `requirements_completed` added per the VERIFICATION evidence — but the default is retirement.
  </action>
  <verify>
    <automated>/usr/bin/pytest tests/test_weather_sun.py tests/test_weather_fetch.py tests/test_weather_cache.py tests/test_weather_alerts.py tests/test_weather_links.py tests/test_weather_link_target.py -q 2>&amp;1 | grep -Eq 'passed' &amp;&amp; grep -q 'DEBT-02' .planning/milestones/v1.0-MILESTONE-AUDIT.md &amp;&amp; grep -q 'DEBT-04' .planning/milestones/v1.0-MILESTONE-AUDIT.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/pytest tests/test_weather_sun.py tests/test_weather_fetch.py tests/test_weather_cache.py tests/test_weather_alerts.py tests/test_weather_links.py tests/test_weather_link_target.py -q` exits 0, output shows "passed" and "skipped" with NO "error"/"ERROR"/"ModuleNotFoundError" lines, and no empty `skipTest("")` exists (`grep -rn 'skipTest("")' tests/test_weather*.py` returns nothing).
    - v1.0-MILESTONE-AUDIT.md contains a "Tech-Debt Resolution (Phase 10)" section naming all five DEBT items, with DEBT-02 explicitly RETIRED (citing commit a413558) and DEBT-04 stating the exact `/usr/bin/pytest ...` verification command and the skip-with-reason rationale.
    - No DEBT item is left without a resolution line (audit trail has no silent gap).
  </acceptance_criteria>
  <done>Weather tests verified runnable (pass-or-skip-with-reason, exit 0) under system python3; all five DEBT items carry an explicit documented resolution; DEBT-02 retired with the clearing-commit cited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a pyproject version string, planning markdown, and runs/reads existing test files. No new input crosses any trust boundary; no runtime code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Planning docs / version metadata | accept | Metadata/docs-only edits; no untrusted input, no runtime behavior change. The version string only feeds the NWS User-Agent (already sourced from `_APP_VERSION` at runtime), so the pyproject value is non-load-bearing. No new attack surface. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no dependency changes; system `requests` already present, `astral` intentionally NOT installed — tests skip-with-reason). Legitimacy gate N/A. |

No new attack surface: changes are metadata, planning documents, and test verification only.
</threat_model>

<verification>
- pyproject.toml version (0.2.0) is byte-equal to `_APP_VERSION` (DEBT-01).
- WX-05 requirement text TTL (~10min) equals the code default `weather_ttl=600` (DEBT-05).
- REQUIREMENTS.md traceability table + footer reconciled to shipped state with current date (DEBT-03).
- Weather suite runs under `/usr/bin/pytest`, exits 0, pass-or-skip-with-reason, no errors (DEBT-04).
- v1.0-MILESTONE-AUDIT.md carries an explicit resolution line for every DEBT-01..05 item; DEBT-02 retired with clearing-commit citation (DEBT-02).
</verification>

<success_criteria>
1. `pyproject.toml` version matches `_APP_VERSION` in `claude-statusline.py` (both 0.2.0).
2. The `requirements-completed` SUMMARY-frontmatter item is formally retired with a clear documented note — no silent gap in the audit trail.
3. The REQUIREMENTS.md traceability table and footer are current and reconciled with what shipped.
4. The astral/requests weather tests are runnable (pass or explicitly skipped with a reason) under system `python3`, verified by an exact command.
5. The WX-05 cache-TTL text and code default agree (both 10 min).
</success_criteria>

<output>
Create `.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md` when done.
</output>
