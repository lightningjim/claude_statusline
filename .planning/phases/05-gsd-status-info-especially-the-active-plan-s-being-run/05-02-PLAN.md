---
phase: 05-gsd-status-info-especially-the-active-plan-s-being-run
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - claude-statusline.py
  - tests/test_gsd_segment.py
  - tests/test_nerd_icons.py
  - tests/test_skeleton_render.py
  - tests/test_bootstrap_degradation.py
autonomous: true
# CONTEXT-driven enhancement — no REQUIREMENTS.md IDs mapped (mirrors Phases 03/04).
# Scope tracked against CONTEXT decisions D-01..D-10; RUN-01/RUN-02 honored.
requirements: []
must_haves:
  truths:
    - "A project with .planning/ shows a [gsd] segment on the top line between [git] and [model]"
    - "The headline is the active plan id + task progress (e.g. 05-02 2/3), with no separate phase prefix"
    - "The lifecycle glyph is colored (executing/done→green, verifying→yellow, blocked→red, idle→dim); the plan-id/task text stays neutral"
    - "A non-GSD project (no .planning/ under project_dir) renders the bar with the GSD segment silently omitted"
    - "show_gsd=false omits the segment; default is True"
    - "icon_set='emoji' uses ascii/emoji fallback glyphs, not the _NF_GSD_* nerd codepoints"
    - "Malformed/missing planning data never crashes or hangs the bar — segment returns None"
  artifacts:
    - path: "claude-statusline.py"
      provides: "_NF_GSD_* glyph constants, DEFAULTS display.show_gsd, _gsd_segment(data,cfg) builder, render_top_line wiring after _git_segment (covers D-02 no separate phase prefix, D-08 .planning scoped to project_dir/omit if absent, D-09 colored lifecycle glyph + neutral label, D-10 top-line placement after git segment)"
      contains: "def _gsd_segment"
    - path: "tests/test_gsd_segment.py"
      provides: "Builder + E2E tests across all lifecycle/visibility/icon_set/never-raise cases"
      contains: "_gsd_segment"
    - path: "tests/test_nerd_icons.py"
      provides: "_NF_GSD_* constants added to the installed-font cmap guard list"
      contains: "_NF_GSD_"
  key_links:
    - from: "render_top_line"
      to: "_gsd_segment"
      via: "third element of the segments list, immediately after _git_segment"
      pattern: "_gsd_segment\\(data, cfg\\)"
    - from: "_gsd_segment"
      to: "_read_gsd_state + _infer_gsd_lifecycle"
      via: "resolve project_dir/.planning, read state, infer lifecycle, render"
      pattern: "_read_gsd_state|_infer_gsd_lifecycle"
    - from: "_gsd_segment"
      to: "DEFAULTS display.show_gsd"
      via: "cfg.get('display',{}).get('show_gsd', True) toggle gate"
      pattern: "show_gsd"
---

<objective>
Wire the GSD layer from Plan 01 into a visible top-line segment. This plan adds the `_NF_GSD_*` lifecycle/plan glyph constants, the `display.show_gsd` config default, the `_gsd_segment(data, cfg)` builder (mirroring `_git_segment` step-for-step), and inserts it into `render_top_line` immediately after `_git_segment` per D-10. It also updates the two existing exact-string top-line tests whose fixtures now sit in a `.planning/`-bearing project, and adds the `_NF_GSD_*` constants to the installed-font cmap guard.

Purpose: Deliver the user-visible segment — `[project] [git] [gsd] [model 💭] [weather]` — that surfaces the active plan being run with a colored lifecycle glyph and a neutral plan-id/task label, omitting cleanly off-GSD and never blocking the bar.
Output: Working `_gsd_segment` + glyphs + config toggle + wiring + integration/E2E tests, with all existing tests still green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gsd-status-info-especially-the-active-plan-s-being-run/05-CONTEXT.md
@.planning/phases/05-gsd-status-info-especially-the-active-plan-s-being-run/05-PATTERNS.md
@.planning/phases/05-gsd-status-info-especially-the-active-plan-s-being-run/05-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From claude-statusline.py + Plan 01 helpers + existing tests. -->

From Plan 01 (already on the module):
- `_read_gsd_state(planning_dir: str) -> dict | None` → {"handoff": dict, "state": dict, "roadmap": str} or None.
- `_infer_gsd_lifecycle(state: dict | None) -> dict | None` → {plan_id, tasks_done, total_tasks, plans_done, plans_total, state, milestone} or None. `state` field ∈ {"executing","verifying","blocked","done","idle"}.

ANSI constants (claude-statusline.py lines 76–80): GREEN, YELLOW, RED, DIM, RESET.

Existing _NF_GIT_* glyph block (lines 410–423) — the comment/format template for _NF_GSD_*:
```python
_NF_GIT_BRANCH   = ""   # nf-pl-branch        U+E0A0
_NF_GIT_WORKTREE = ""   # nf-fa-code_fork     U+F126
```
Validated GSD codepoints present in the installed JetBrains Nerd Font (confirmed via fontTools cmap during planning):
- U+F04B fa-play (executing), U+F046 fa-check_square_o (verifying), U+F05E fa-ban (blocked),
  U+F058 fa-ok_sign (done), U+F04C fa-pause (idle/parked), U+F278 fa-map_o (plan slot).

DEFAULTS display block (lines 163–173) — add show_gsd after show_git:
```python
"display": {
    "icon_set": "nerd",
    "bar_style": "shade",
    "show_git": True,
},
```

_git_segment builder (lines 1456–1556) — the exact structural template; reproduced in 05-PATTERNS.md lines 156–227. Mirror its numbered steps: (1) toggle gate, (2) resolve dir, (3) fetch state, (4) infer, (6) icon_set glyphs, (7) neutral label, (8) colored state marker, (9) assemble `[...]`, outer `except Exception: return None`.

render_top_line current segments list (lines 1928–1935):
```python
segments = [
    _project_segment(data),
    _git_segment(data, cfg),        # D-09: immediately after project, before model
    _model_segment(data, show_thinking_glyph=show_thinking_glyph, icon_set=icon_set),
    _weather_segment(data, cfg),
]
```

EXISTING-TEST IMPACT (the fixture/.examples/claude_stdin.json project_dir is THIS repo, which HAS .planning/ → show_gsd defaults True → a [gsd] segment WOULD render and break exact-string assertions):
- tests/test_skeleton_render.py: `_NO_GIT_HOME` config (lines 37–38) writes `[display]\nshow_git = false` and `test_fixture_top_line_exact` (line 82) asserts EXACTLY `"[claude_statusline] [Opus 4.8 (1M context) ]"`. Must add `show_gsd = false` to that config so the asserted string stays valid.
- tests/test_bootstrap_degradation.py: `test_top_line_no_weather_equals_phase1_format` (line 147) sets `cfg["display"] = {"show_git": False, "icon_set": "nerd", "bar_style": "shade"}` and asserts ≤2 bracketed segments. Must add `"show_gsd": False` to that cfg dict.

Nerd cmap-guard list (tests/test_nerd_icons.py lines 1311–1317) ends with the Phase-04 git constants — append the _NF_GSD_* names there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _NF_GSD_* glyph constants + display.show_gsd default, and extend the installed-font cmap guard</name>
  <read_first>
    - claude-statusline.py lines 405–423 (_NF_GIT_* block — comment/format template)
    - claude-statusline.py lines 163–173 (DEFAULTS display block)
    - tests/test_nerd_icons.py lines 1277–1347 (installed-font cmap guard: GLYPH_CONSTANTS list + the assertion)
    - tests/test_nerd_icons.py lines 252–308 (TestGlyphConstantsExist / single-cell guards — confirm _NF_GSD_* satisfy len==1)
    - .planning/phases/05-.../05-PATTERNS.md "_NF_GSD_* glyph constants" (lines 22–55) and "DEFAULTS display block extension" (lines 484–494)
  </read_first>
  <files>claude-statusline.py, tests/test_nerd_icons.py</files>
  <action>
    In claude-statusline.py, after the `_NF_GIT_*` block (≈ line 423), add a "GSD segment glyphs — Phase 05 lifecycle / plan indicators" block of single-cell `_NF_GSD_*` constants using the exact `_NF_GSD_<ROLE> = "<glyph>"  # nf-<family>-<name>  U+XXXX` comment format. Use these planning-verified codepoints (all present in the installed font): `_NF_GSD_EXECUTING` U+F04B (fa-play), `_NF_GSD_VERIFYING` U+F046 (fa-check_square_o), `_NF_GSD_BLOCKED` U+F05E (fa-ban), `_NF_GSD_DONE` U+F058 (fa-ok_sign), `_NF_GSD_IDLE` U+F04C (fa-pause), `_NF_GSD_PLAN` U+F278 (fa-map_o). Each value is the literal single glyph character, not the codepoint text. In DEFAULTS (≈ line 172), add `"show_gsd": True,` immediately after `"show_git": True,` with a Phase-05 comment mirroring the show_git comment (D-08 discretion: display.show_gsd; True renders when .planning/ exists under project_dir; set false to suppress).
    In tests/test_nerd_icons.py, append the six `_NF_GSD_*` names to the `GLYPH_CONSTANTS` list (after `_NF_GIT_BEHIND`, ≈ line 1316) so the installed-font cmap guard validates them.
  </action>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/claude_statusline && python -m pytest tests/test_nerd_icons.py -k "cmap or GlyphConstants or Defaults" -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^_NF_GSD_" claude-statusline.py` returns 6.
    - Source: each `_NF_GSD_*` value is a single Unicode codepoint — `python -c "import importlib.util,sys; ..."` (or the existing single-cell test) confirms len==1 for all six.
    - Source: `grep -n '"show_gsd": True' claude-statusline.py` returns one match inside the DEFAULTS display block.
    - Source: `grep -c "_NF_GSD_" tests/test_nerd_icons.py` returns ≥6 (all six names in GLYPH_CONSTANTS).
    - Behavior: the installed-font cmap guard test passes (or cleanly skips if fontTools/font absent) with the new constants included.
    - Test command: `python -m pytest tests/test_nerd_icons.py -q` exits 0.
  </acceptance_criteria>
  <done>Six single-cell `_NF_GSD_*` constants exist with the standard comment format, `display.show_gsd` defaults True in DEFAULTS, and the installed-font cmap guard covers all six glyphs.</done>
</task>

<task type="auto">
  <name>Task 2: _gsd_segment builder + render_top_line wiring (after _git_segment), with builder + E2E tests</name>
  <read_first>
    - claude-statusline.py lines 1456–1556 (_git_segment — mirror its eight numbered steps + outer guard)
    - claude-statusline.py lines 1442–1453 (_project_segment — project_dir resolution shape; D-08 uses project_dir NOT current_dir)
    - claude-statusline.py lines 1914–1935 (render_top_line — segments list + docstring)
    - claude-statusline.py lines 76–80 (GREEN/YELLOW/RED/DIM/RESET)
    - claude-statusline.py the `_read_gsd_state` / `_infer_gsd_lifecycle` helpers (Plan 01 input contracts)
    - .planning/phases/05-.../05-PATTERNS.md "_gsd_segment builder" (lines 152–227) + "render_top_line insertion" (lines 231–259) + "Shared Patterns" (lines 419–494)
    - .planning/phases/05-.../05-CONTEXT.md D-08 (project_dir scope), D-09 (color the state, neutral label), D-10 (placement)
    - tests/test_git_segment.py lines 11–26 (loader), 439–494 (builder monkeypatch + show_*-false test), 677–727 (never-raise param cases + E2E helpers)
  </read_first>
  <files>claude-statusline.py, tests/test_gsd_segment.py</files>
  <action>
    Add `def _gsd_segment(data: dict, cfg: dict) -> str | None:` after `_git_segment` (≈ line 1557). Mirror the git builder steps: (1) `if not cfg.get("display", {}).get("show_gsd", True): return None`. (2) Resolve `project_dir` from `data.get("workspace", {})` (guard non-dict) using `project_dir` ONLY (D-08, NOT current_dir); return None if empty. Build `planning_dir = os.path.join(project_dir, ".planning")`; `if not os.path.isdir(planning_dir): return None` (silent omit off-GSD, D-08). (3) `state = _read_gsd_state(planning_dir)`; if None → None. (4) `info = _infer_gsd_lifecycle(state)`; if None → None. (6) icon_set glyph resolution: `icon_set = cfg.get("display", {}).get("icon_set", "nerd")`; nerd → map each lifecycle state to its `_NF_GSD_*` glyph (executing→EXECUTING, verifying→VERIFYING, blocked→BLOCKED, done→DONE, idle→IDLE) + `_NF_GSD_PLAN` for the plan slot; else → ascii/emoji fallbacks (e.g. "▶"/"☑"/"⊘"/"✓"/"⏸" and "" or a plain prefix for the plan slot) — distinct from the nerd codepoints. (7) Neutral label (D-09, no color wrap): for active/idle states, `f"{plan_id} {tasks_done}/{total_tasks}"` when task counts present else just `plan_id`; for done state, the milestone label (e.g. `f"{milestone}"`). Optionally include plan-of-total (D-04) as a neutral `plans_done/plans_total` fragment when present. (8) Colored lifecycle glyph ONLY (D-09): map state→color (executing→GREEN, done→GREEN, verifying→YELLOW, blocked→RED, idle→DIM) and render `f"{color}{glyph}{RESET}"`. (9) Assemble interior `f"{label} {status_glyph}"` (with the optional plan-of-total fragment placed neutrally) and return `f"[{interior}]"`. For the done state, render e.g. `f"[{milestone} {GREEN}{done_glyph}{RESET}]"` (D-07 explicit done, do NOT omit). Wrap the ENTIRE body in `try/except Exception: return None` (D-09/RUN-01/RUN-02). Render ONLY structured fields (plan id, counts, milestone, glyphs) — never raw file text (security: no ANSI-injection surface).
    In render_top_line (≈ line 1930), insert `_gsd_segment(data, cfg),` as the third element immediately after `_git_segment(data, cfg),` and before `_model_segment(...)`; update the docstring to note Phase-05 ordering `[project] [git] [gsd] [model] [weather]` (D-10). None-filtering is already handled by `present = [s for s in segments if s is not None]`.
    Extend tests/test_gsd_segment.py with `TestGsdSegmentBuilder` using the monkeypatch pattern from 05-PATTERNS.md lines 334–368 (patch `self.mod._read_gsd_state` and/or `_infer_gsd_lifecycle`, build cfg with `display.show_gsd=True`, data with workspace.project_dir). Cover: show_gsd=False→None; _read_gsd_state None→None; executing→contains plan id + GREEN + executing glyph; idle→next-up id + DIM + idle glyph; blocked→RED; verifying→YELLOW; milestone-complete→milestone label + GREEN done glyph; plan-id text carries NO raw GREEN/RED wrap (D-09 neutral label assertion); icon_set='emoji'→fallback glyphs, none of the _NF_GSD_* codepoints present; never-raises on empty/None-workspace data. Add an E2E test (helper pattern from 05-PATTERNS.md lines 383–415): pipe minimal JSON with `workspace.project_dir` = the repo root (which has .planning/) and assert the top line contains a third bracketed segment positioned after the git segment and before the model segment.
  </action>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/claude_statusline && python -m pytest tests/test_gsd_segment.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "def _gsd_segment" claude-statusline.py` returns one match; the body uses `_read_gsd_state` and `_infer_gsd_lifecycle` and `os.path.isdir`.
    - Source: `grep -c "_gsd_segment" claude-statusline.py` ≥ 2 (definition + render_top_line call).
    - Source: render_top_line segments list has `_gsd_segment(data, cfg)` immediately after `_git_segment(data, cfg)` (verify by reading the list).
    - Source: builder uses `project_dir` for the .planning lookup, NOT `current_dir`.
    - Behavior: monkeypatched executing-state info → result contains the plan id substring, the GREEN code, and the executing glyph; plan-id substring is NOT preceded by GREEN/RED (neutral label, D-09).
    - Behavior: blocked→RED present; verifying→YELLOW present; idle→DIM present; milestone-complete→milestone label + GREEN done glyph.
    - Behavior: icon_set='emoji'→output contains the ascii/emoji fallback, contains none of the six _NF_GSD_* characters.
    - Behavior: data with no workspace / empty project_dir → None (no traceback).
    - Behavior (E2E): piping repo-root project_dir yields a top line where a [gsd] segment appears between [git] and [model].
    - Test command: `python -m pytest tests/test_gsd_segment.py` exits 0.
  </acceptance_criteria>
  <done>`_gsd_segment` renders the active plan id + neutral task progress with a single colored lifecycle glyph, scoped to project_dir/.planning, omits silently off-GSD and on any error, is wired into render_top_line after _git_segment per D-10, and its builder + E2E tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update the two existing exact-string top-line tests for the new default-True GSD segment</name>
  <read_first>
    - tests/test_skeleton_render.py lines 24–82 (_NO_GIT_HOME config setup + test_fixture_top_line_exact exact-string assertion)
    - tests/test_bootstrap_degradation.py lines 108–157 (TestRenderTopLineWeatherOmitted — the ≤2-segment guard at line 147)
    - .examples/claude_stdin.json (confirm workspace.project_dir == this repo root, which has .planning/)
    - claude-statusline.py render_top_line (confirm the new ordering the tests must account for)
  </read_first>
  <files>tests/test_skeleton_render.py, tests/test_bootstrap_degradation.py</files>
  <action>
    These two tests assert exact / bounded top-line shape and their fixture's `workspace.project_dir` is this repo, which contains `.planning/` — so with `show_gsd` defaulting True a `[gsd]` segment would now render and break them. Update their CONFIG POSTURE (not their asserted Phase-1 strings):
    - tests/test_skeleton_render.py: in the `_NO_GIT_HOME` config write (lines 37–38), change the written TOML to also disable the GSD segment, i.e. `[display]\nshow_git = false\nshow_gsd = false\n`, and update the adjacent comment to mention show_gsd alongside show_git. This keeps `test_fixture_top_line_exact`'s asserted `"[claude_statusline] [Opus 4.8 (1M context) ]"` valid.
    - tests/test_bootstrap_degradation.py: in `test_top_line_no_weather_equals_phase1_format` (line 147), add `"show_gsd": False` to the `cfg["display"]` dict so the ≤2-segment guard holds. Update the test docstring (lines 134–140) to note the GSD segment is also disabled for this two-segment guard.
    Do NOT weaken the assertions; only adjust the show_gsd config so the existing exact/bounded guards remain meaningful with the new default-on segment.
  </action>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/claude_statusline && python -m pytest tests/test_skeleton_render.py tests/test_bootstrap_degradation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "show_gsd" tests/test_skeleton_render.py` shows it in the `_NO_GIT_HOME` config write; `grep -n "show_gsd" tests/test_bootstrap_degradation.py` shows it in the no-weather cfg.
    - Behavior: `test_fixture_top_line_exact` still asserts the exact `"[claude_statusline] [Opus 4.8 (1M context) ]"` string and passes.
    - Behavior: `test_top_line_no_weather_equals_phase1_format` still asserts ≤2 bracketed segments and passes.
    - Test command: `python -m pytest tests/test_skeleton_render.py tests/test_bootstrap_degradation.py` exits 0.
  </acceptance_criteria>
  <done>Both exact/bounded top-line guards explicitly disable show_gsd, keep their original assertions, and pass with the new default-True GSD segment present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin JSON → filesystem | `data.workspace.project_dir` (untrusted, stdin-supplied) is used to derive the `.planning/` directory the segment reads. |
| planning files → terminal output | Structured fields (plan id, counts, milestone, glyphs) are rendered into an ANSI terminal line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Information Disclosure | `_gsd_segment` resolving `.planning/` from stdin `project_dir` | mitigate | Lookup is `os.path.join(project_dir, ".planning")` + `os.path.isdir` gate only; reads are delegated to Plan 01's path-confined `_read_gsd_state`; no path components beyond the fixed `.planning/` + fixed filenames; read-only. |
| T-05-05 | Tampering | ANSI-escape injection via planning-file content rendered to the terminal | mitigate | The builder renders ONLY structured/derived fields (plan id, integer counts, milestone label, fixed glyph constants) — never raw file text; color codes come only from module ANSI constants, not from file content. |
| T-05-06 | Denial of Service | malformed/huge planning data hanging or crashing the bar | mitigate | Whole-body `try/except Exception: return None` (D-09/RUN-01/RUN-02); upstream `_read_gsd_state` byte-caps reads (T-05-01); no subprocess, no network, no caching — pure local reads each render. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan; glyphs are literal Unicode constants validated against the already-installed Nerd Font cmap. No package-manager install task. |
</threat_model>

<verification>
- Full suite green: `python -m pytest -q` (new GSD tests + updated existing top-line tests + untouched git/weather/skeleton tests all pass).
- `grep -n "def _gsd_segment" claude-statusline.py` and the render_top_line segments list show the segment defined and wired after `_git_segment`.
- `grep -c "^_NF_GSD_" claude-statusline.py` == 6 and `grep -n '"show_gsd": True' claude-statusline.py` present.
- Installed-font cmap guard passes/skips cleanly with the six new constants: `python -m pytest tests/test_nerd_icons.py -q`.
- Manual smoke (optional): `python -c "import json,subprocess,sys; ..."` piping repo-root project_dir shows `[gsd]` between `[git]` and the model segment.
- `python -c "import ast; ast.parse(open('claude-statusline.py').read())"` parses.
</verification>

<success_criteria>
- The top line renders `[project] [git] [gsd] [model 💭] [weather]` with the GSD segment third, after git and before model (D-10).
- Headline is the active plan id + task progress with no separate phase prefix (D-01/D-02); a colored lifecycle glyph rides along (executing/done→green, verifying→yellow, blocked→red, idle→dim) while the plan-id/task label stays neutral (D-09).
- HANDOFF-first source with roadmap fallback, idle next-up display, and explicit milestone-complete done state are all visible through the segment (D-05/D-06/D-07, via Plan 01 inference).
- Segment is scoped to `.planning/` under `project_dir` and omits silently off-GSD (D-08); `show_gsd` toggles it (default True).
- icon_set switches nerd `_NF_GSD_*` glyphs vs ascii/emoji fallbacks consistently with every other segment.
- The bar never crashes/hangs on malformed planning data (RUN-01/RUN-02); the two existing exact-string top-line tests are updated to disable show_gsd and still pass; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/05-gsd-status-info-especially-the-active-plan-s-being-run/05-02-SUMMARY.md` when done.
</output>
