---
phase: 01-core-statusline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - ~/.claude/claude-statusline.py
autonomous: false
requirements: [CTX-01, CTX-02, LIM-01, LIM-02, LIM-03, LIM-04, FMT-01]
user_setup: []

must_haves:
  decisions: [D-03, D-04, D-05]
  truths:
    - "The bottom line shows a 20-character bar of filled/empty cells followed by an explicit context percentage"
    - "The context bar fill, context %, 5h %, and weekly % are colored green<70 / yellow 70-90 / red>90"
    - "The 5-hour usage is glyph-labeled with the hourglass and the weekly usage with the calendar glyph"
    - "A reset time is appended (dim) only to a rate-limit indicator that is not green (>=70%)"
    - "Reset time is same-day shorthand otherwise abbreviated-weekday shorthand, converted from unix epoch to LOCAL time"
    - "A missing rate_limits or context_window block omits only that segment and still exits 0"
  artifacts:
    - path: "~/.claude/claude-statusline.py"
      provides: "Second line: context bar+pct and 5h/weekly with threshold colors and conditional reset times"
      min_lines: 80
  key_links:
    - from: "~/.claude/claude-statusline.py"
      to: "rate_limits.five_hour.resets_at"
      via: "datetime.fromtimestamp on epoch seconds, formatted same-day/abbrev-weekday"
      pattern: "fromtimestamp"
    - from: "~/.claude/claude-statusline.py"
      to: "context_window.used_percentage"
      via: "20-wide bar fill = pct * 20 / 100"
      pattern: "used_percentage"
---

<objective>
Add the second status line on top of the Plan 01 skeleton: a 20-wide context bar with an
explicit percentage, plus glyph-labeled 5-hour (hourglass) and weekly (calendar) rate-limit
usage. The context bar fill, context %, 5h %, and weekly % are colored by threshold
(green <70 / yellow 70-90 / red >90). A reset time is appended — rendered dim/neutral — only
to a rate-limit indicator that is NOT green (>=70%), in same-day shorthand or abbreviated-weekday
shorthand derived from the unix-epoch resets_at.

Purpose: Completes the data-from-stdin bar — the user can now read context and rate-limit
headroom at a glance, the core value of the project.
Output: ~/.claude/claude-statusline.py extended with the bottom line. Covers CTX-01, CTX-02,
LIM-01, LIM-02, LIM-03, LIM-04, FMT-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-statusline/01-CONTEXT.md
@.planning/phases/01-core-statusline/01-01-SUMMARY.md
@.examples/statusline-command.sh
@.examples/claude_stdin.json

<interfaces>
<!-- stdin JSON keys this plan consumes (from .examples/claude_stdin.json). All may be absent. -->
<!-- context_window.used_percentage : number (fixture = 7) -> bar + explicit % -->
<!-- rate_limits.five_hour.used_percentage  : number (fixture = 30, green -> no reset shown) -->
<!-- rate_limits.five_hour.resets_at        : unix epoch seconds (fixture = 1780004400 -> 2026-05-28 16:40 local, same-day) -->
<!-- rate_limits.seven_day.used_percentage  : number (fixture = 3, green -> no reset shown) -->
<!-- rate_limits.seven_day.resets_at        : unix epoch seconds (fixture = 1780243200 -> 2026-05-31 11:00 local, future day) -->

<!-- Ported logic from .examples/statusline-command.sh: -->
<!--   perc_color thresholds: >=90 red, >=70 yellow, else green -->
<!--   bar: BAR_WIDTH=20, FILLED = PCT*20/100, fill char filled-block, empty char light-shade -->
<!--   glyphs: hourglass = five_hour, calendar = seven_day -->
<!-- Concrete layout target (D-03), 5h non-green example: -->
<!--   [<9 filled><11 empty>] 47%   <hourglass> 78% 5:15pm   <calendar> 3%  (5:15pm dim, 78% yellow) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Color, threshold, and reset-time helpers</name>
  <files>~/.claude/claude-statusline.py</files>
  <read_first>
    - .planning/phases/01-core-statusline/01-CONTEXT.md (D-04, D-05, LIM-04, FMT-01, and Claude's-Discretion: rounding + bar math)
    - .examples/statusline-command.sh (perc_color 70/90; FILLED=PCT*20/100; rate glyphs; cut -d. truncation)
    - .examples/claude_stdin.json (fixture values: ctx 7, 5h 30 green, wk 3 green, resets_at epochs)
    - ~/.claude/claude-statusline.py (the skeleton from Plan 01 — extend in place)
  </read_first>
  <behavior>
    - color_for(69) -> green code; color_for(70) -> yellow; color_for(89) -> yellow; color_for(90) -> yellow; color_for(91) -> red (FMT-01 bands: green<70, yellow 70-90, red>90; red is strictly >90)
    - is_green(30) -> True; is_green(70) -> False (the >=70 threshold that gates reset display, D-04/LIM-03)
    - fmt_reset for a same-local-day epoch -> same-day form like 4:40pm (LOCAL, no leading zero on hour, lowercase am/pm)
    - fmt_reset for an epoch on a different calendar day -> abbreviated-weekday form like Sun 11:00am
    - Percentage rounding is consistent across all three indicators (choose floor to match the bash predecessor's cut -d.)
  </behavior>
  <action>
    Extend ~/.claude/claude-statusline.py (do NOT create a new file). Add ANSI color constants
    GREEN, YELLOW, RED, a DIM/neutral code for reset times (D-04), and RESET. Add color_for(pct)
    returning RED for pct>90 (i.e. >=91), YELLOW for 70<=pct<=90, GREEN otherwise (FMT-01, D-05);
    note red is strictly greater than 90, so 90 is yellow. Add is_green(pct) returning pct < 70
    (the gate for whether a reset time is shown, D-04). Add fmt_reset(epoch) that converts unix
    epoch seconds to LOCAL time via datetime.fromtimestamp(epoch): if the reset's local date equals
    today's local date, format as hour:minute + lowercased am/pm with no leading zero on the hour
    (e.g. 5:15pm); otherwise prepend the abbreviated weekday and a space (e.g. Mon 5:15pm) per
    LIM-04. Add a pct_int(value) helper that floors to an int (consistent rounding, Claude's
    discretion) and tolerates missing/non-numeric input by returning None. Wrap each helper so bad
    input degrades to omission rather than raising (D-10). Keep these helpers ahead of the render
    code so Task 2 can call them.
  </action>
  <verify>
    <automated>grep -q 'def color_for' ~/.claude/claude-statusline.py && grep -q 'def fmt_reset' ~/.claude/claude-statusline.py && grep -q 'def is_green' ~/.claude/claude-statusline.py</automated>
    <automated>grep -q 'fromtimestamp' ~/.claude/claude-statusline.py</automated>
    <automated>python3 -c "import ast; ast.parse(open('$HOME/.claude/claude-statusline.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: script defines color_for, is_green, and fmt_reset functions; file still parses (ast.parse)
    - Behavior: color_for returns green for 69, yellow for 70 and 90, red for 91 (verifiable by an inline python check)
    - Behavior: is_green returns True for 69 and False for 70
    - Behavior: fmt_reset for a same-local-day epoch yields an hour:minute am/pm string with no weekday; for a different-day epoch yields an abbreviated-weekday-prefixed string
    - Source: a dim/neutral ANSI code is present for reset-time styling
    - Source: rounding helper floors consistently (matches bash cut -d. behavior)
  </acceptance_criteria>
  <done>color_for/is_green/fmt_reset exist with correct band boundaries (red strictly >90), local-time reset formatting in both same-day and other-day shorthand, dim styling available, consistent floor rounding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the bottom line — bar, %, 5h, weekly, conditional resets</name>
  <files>~/.claude/claude-statusline.py</files>
  <read_first>
    - .planning/phases/01-core-statusline/01-CONTEXT.md (D-03, D-04, D-05, D-10)
    - .examples/claude_stdin.json (fixture: ctx 7, 5h 30 green, wk 3 green -> NO reset times shown for fixture)
    - ~/.claude/claude-statusline.py (helpers from Task 1 + top line from Plan 01)
  </read_first>
  <behavior>
    - For the fixture (ctx=7, 5h=30, wk=3, all green): the bottom line contains a 20-char bar in brackets, then 7%, then the hourglass 30%, then the calendar 3%, and NO reset time anywhere (all green per D-04)
    - The bar is exactly 20 cells: filled count = floor(pct*20/100), remainder empty cells (CTX-01); for ctx=7 -> 1 filled + 19 empty
    - The explicit percentage follows the bar (CTX-02); bar fill and the % number both wear color_for(ctx) (D-05)
    - Synthetic 5h=78%: bottom line shows the hourglass 78% in yellow followed by a dim reset time; weekly green -> calendar 3% with no reset
    - Synthetic 5h=95%: hourglass 95% in red, dim reset time appended
    - Missing rate_limits block: both rate segments omitted, context bar still renders, exit 0 (D-10)
    - Missing context_window block: bar/% omitted, rate segments still render if present, exit 0 (D-10)
  </behavior>
  <action>
    Extend ~/.claude/claude-statusline.py to print a SECOND line (after the Plan 01 top line).
    Build the context segment per D-03: open bracket + bar + close bracket where bar = filled-block
    repeated floor(used_percentage*20/100) times + light-shade repeated (20 minus that count) times,
    wrapped in color_for(ctx) ... RESET; then a space and the explicit percentage as <n>% also
    wrapped in color_for(ctx) (CTX-01, CTX-02, D-05). Use pct_int from Task 1; if
    context_window.used_percentage is missing/invalid, omit the whole context segment (D-10). Build
    the 5-hour segment as the hourglass glyph + space + color_for(5h)<n>%RESET, and append a space +
    DIM + fmt_reset(resets_at) + RESET ONLY when not is_green(5h) and resets_at is present (D-04,
    LIM-03). Build the weekly segment identically with the calendar glyph and seven_day (LIM-02).
    Omit a rate segment entirely if its used_percentage is missing (D-10); omit just the reset
    suffix if resets_at is missing even when non-green. Join the present bottom-line segments with
    the spacing shown in the D-03 layout (three spaces between the context block, the 5h block, and
    the weekly block). Print the bottom line only if it has at least one segment; otherwise print
    only the top line. Preserve the Plan 01 top line and the empty/garbage-stdin safe-exit path
    unchanged.
  </action>
  <verify>
    <automated>printf '%s' "$(cat .examples/claude_stdin.json)" | python3 ~/.claude/claude-statusline.py | grep -c '' | grep -qx '2'</automated>
    <automated>printf '%s' "$(cat .examples/claude_stdin.json)" | python3 ~/.claude/claude-statusline.py | sed -n '2p' | grep -q '7%'</automated>
    <automated>echo '' | python3 ~/.claude/claude-statusline.py; test $? -eq 0</automated>
    <automated>python3 -c "import ast; ast.parse(open('$HOME/.claude/claude-statusline.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: piping the fixture produces exactly two output lines and exits 0
    - Behavior: the fixture bottom line contains a 20-cell bar inside brackets, the literal 7%, an hourglass-prefixed 30%, and a calendar-prefixed 3%
    - Behavior: the fixture bottom line contains NO reset time (all three indicators are green)
    - Behavior: a synthetic payload with five_hour.used_percentage=78 produces an hourglass 78% followed by a dim reset time on the bottom line; weekly (3, green) has no reset suffix
    - Behavior: a payload with the rate_limits block removed still prints the context bar and exits 0; a payload with context_window removed still prints the rate segments and exits 0
    - Behavior: empty stdin still exits 0 with no traceback (skeleton safe-path preserved)
    - Source: file still parses (ast.parse succeeds)
  </acceptance_criteria>
  <done>Fixture renders exactly two lines with a correct 20-cell colored bar, explicit %, glyph-labeled colored 5h/weekly, and reset times shown only for non-green indicators; missing blocks degrade per-segment; empty stdin still safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code -> script stdin | Semi-trusted local process; numeric fields (percentages, epoch seconds) may be absent, non-numeric, or out of range |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | bar-fill / pct math | mitigate | pct_int floors and guards non-numeric input -> None -> segment omitted; bar width is a fixed constant 20 so fill math cannot loop unbounded (RUN-02) |
| T-01-05 | Denial of Service | fmt_reset on resets_at | mitigate | Wrap datetime.fromtimestamp in try/except; an out-of-range or non-numeric epoch omits only the reset suffix, never raises (D-10) |
| T-01-06 | Information Disclosure | reset times for green indicators | accept | By design only non-green indicators show resets (D-04); reset time is non-sensitive local clock data, no PII |

No high severity threats: no network, no secrets, no third-party deps. ASVS L1 satisfied for this local render path.
</threat_model>

<verification>
- Fixture renders exactly two lines and exits 0 (all three indicators green -> no reset times)
- Bar is exactly 20 cells; filled count = floor(pct*20/100); explicit % follows the bar
- color_for bands correct at boundaries (69 green, 70/90 yellow, 91 red)
- Non-green rate indicator shows a dim reset in same-day or abbreviated-weekday shorthand from LOCAL epoch
- Missing context_window or rate_limits block omits only that segment; empty stdin still safe-exits 0
</verification>

<success_criteria>
- CTX-01: 20-wide filled/empty bar, colored by threshold
- CTX-02: explicit percentage alongside the bar
- LIM-01: 5-hour usage shown (hourglass), colored
- LIM-02: weekly usage shown (calendar), colored
- LIM-03: reset time appended for any non-green indicator
- LIM-04: reset shorthand same-day / abbreviated-weekday, from LOCAL epoch
- FMT-01: green<70 / yellow 70-90 / red>90 applied to context, 5h, weekly
</success_criteria>

<output>
Create `.planning/phases/01-core-statusline/01-02-SUMMARY.md` when done.
</output>
