---
phase: 07.1-distinguish-resolved-from-unresolved-claude-status-incidents
verified: 2026-06-18T00:00:00Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase 07.1: Distinguish Resolved from Unresolved Claude Status Incidents — Verification Report

**Phase Goal:** Make the Phase 6/7 Claude service-health segment tell the truth about a recently-resolved incident: while a resolved incident's tracked component is still degraded, render it GREEN with a check-circle glyph and a `resolved:` prefix instead of an alarming red outage (D-01/D-03/D-05); keep an unexplained degraded component red (D-05); keep active incidents and maintenance outranking it (D-04); keep a muted incident fully quiet through its resolved phase — no green and no red fallback (D-06, both store-prune and render layers); add a `resolved` STATE to `--status-incidents` (D-07). In-place edits to claude-statusline.py + tests only; never blocks render path, never crashes, never fakes (D-10).
**Verified:** 2026-06-18
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01/D-05: resolved incident + still-degraded component → kind=resolved, severity=resolved | VERIFIED | `_derive_claude_status(status_resolved_degraded.json)` returns `{'severity':'resolved','kind':'resolved','incident_id':'inc-resolved-001','component':'Claude Code'}` |
| 2 | D-05: degraded component with NO matching resolved incident → kind=degraded (red, unchanged) | VERIFIED | Same fixture with `incidents=[]` returns `{'severity':'major','kind':'degraded','incident_id':None}` |
| 3 | D-01: component operational → None even with a lingering resolved incident | VERIFIED | Flipping Claude Code to operational returns `None` |
| 4 | D-04: Rule 1 active incident outranks Rule 3 resolved | VERIFIED | Adding an active critical incident to the fixture returns `{'kind':'incident','severity':'critical'}` |
| 5 | D-04: Rule 2 maintenance outranks Rule 3 resolved | VERIFIED | Adding an in-progress maintenance + under_maintenance component returns `{'kind':'maintenance'}` |
| 6 | D-03: GREEN + check-circle glyph + `resolved:` prefix in rendered output | VERIFIED | `_claude_status_segment` with resolved section returns `'\x1b[32m resolved: Claude Code partial outage\x1b[0m'` — GREEN confirmed, `_NF_GSD_DONE` confirmed, `resolved:` prefix confirmed |
| 7 | D-03: `resolved` severity maps to GREEN in `_claude_status_color` | VERIFIED | `_claude_status_color('resolved') == GREEN` ('\x1b[32m') — hue_map entry at line 1538 |
| 8 | CR-01 (truth-telling): across multiple degraded components, genuinely-unexplained RED wins over resolved-explained GREEN, order-independent | VERIFIED | 6 CR-01 tests all pass: `test_cr01_unexplained_degraded_wins_over_resolved_code_first`, `..._ai_first`, `test_cr01_all_degraded_components_resolved_returns_green`, `test_cr01_muted_explained_first_does_not_mask_unexplained_red`, `test_cr01_all_muted_explained_returns_muted_verdict`, `test_cr01_resolved_green_outranks_muted_none` |
| 9 | CR-01 (surviving case): muted-incident-explained degraded component does NOT mask a later unexplained RED (second fix at commit 8a82db7) | VERIFIED | `test_cr01_muted_explained_first_does_not_mask_unexplained_red` passes; code at lines 1961-1994 defers muted-explained via `muted_result` and keeps scanning |
| 10 | D-06 store layer: `fetch_claude_status` live_ids prune retains resolved-but-still-degraded ids; prunes when component returns to operational | VERIFIED | `test_dismissed_resolved_degraded_component_dismissal_retained` + `test_dismissed_resolved_operational_component_dismissal_pruned` both pass; code at lines 2324-2336 |
| 11 | D-06 render layer (CR-02): baked resolved/degraded item explained only by a muted incident renders None — no green, no red | VERIFIED | 3 CR-02 tests pass: `test_cr02_dismissed_explaining_incident_with_unrelated_survivor_returns_none`, `test_cr02_unrelated_survivor_does_not_leak_when_explaining_present`, `test_cr02_explaining_incident_absent_from_feed_returns_none`; render path at lines 3825-3855 binds to `explaining_id` from cached `incident_id` field |
| 12 | D-07: `--status-incidents` STATE column shows `resolved` for resolved tracked incidents; `dismissed` still takes precedence | VERIFIED | `test_resolved_incident_shows_resolved_state`, `test_dismissed_resolved_shows_dismissed_not_resolved` both pass; docstring at lines 489-494 updated; code at lines 563-571 |
| 13 | D-10: all modified functions wrapped in try/except and never raise | VERIFIED | AST check confirms all six functions (`_print_status_incidents`, `_claude_status_color`, `_derive_claude_status`, `_collect_tracked_incidents`, `fetch_claude_status`, `_claude_status_segment`) have outer try/except; `printf '{}' | python claude-statusline.py` exits 0 |
| 14 | `_collect_tracked_incidents` widened to include resolved incidents (status=="resolved") with same dict shape | VERIFIED | Status filter at line 2186 admits `"resolved"` alongside the three unresolved statuses; docstring at lines 2158-2168 updated; `test_dismissed_resolved_degraded_component_dismissal_retained` exercises this path |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `claude-statusline.py` | Resolved-vs-red Rule 3 branch; resolved hue in `_claude_status_color`; widened `_collect_tracked_incidents`; live_ids prune retention; resolved render branch; D-06 fall-through guard; `--status-incidents` resolved STATE | VERIFIED | All features present at lines documented above |
| `tests/fixtures/status_resolved_degraded.json` | Resolved incident + still-degraded tracked component | VERIFIED | File exists; Claude Code at `partial_outage`, incident `inc-resolved-001` at `status=resolved` |
| `tests/test_claude_status.py` | Full test matrix including 6 CR-01 tests and 3 CR-02 tests | VERIFIED | 246 tests pass in the claude_status module (30 subtests); 727 total suite passes |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `_derive_claude_status` Rule 3 | `_is_suppressed` | Resolved-incident scan reuses same suppression gate as Rule 1 | VERIFIED | Lines 1929-1935 call `_is_suppressed(inc.get("id",""), inc.get("impact","none"), inc.get("name",""), dismissals, cfg)` |
| `_claude_status_color` | `GREEN` | hue_map resolved case | VERIFIED | Line 1538: `"resolved": GREEN` in `hue_map` dict |
| `_claude_status_segment` | `_NF_GSD_DONE` / `GREEN` | Resolved render branch keyed on `kind=="resolved" or severity=="resolved"` | VERIFIED | Lines 3907-3915: branch assigns `glyph = _NF_GSD_DONE`, `color = _claude_status_color(severity)` |
| `_claude_status_segment` suppression re-check | `None` | Baked resolved/degraded item with suppressed explaining incident returns None | VERIFIED | Lines 3825-3855: binding reads `sec.get("incident_id")`, returns `None` if absent or suppressed |
| `_print_status_incidents` | resolved state | `entry.get('status') == 'resolved'` maps to STATE column value `"resolved"` | VERIFIED | Lines 568-569; `dismissed` precedence intact at line 566 |
| `fetch_claude_status` live_ids | `_prune_dismissals` | Resolved-but-still-degraded ids retained in live_ids | VERIFIED | Lines 2324-2336; component status map built at lines 2300-2307 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `_claude_status_segment` | `sec` (cache section) | `read_cache(_CACHE_PATH)` → `write_cache_section` from `fetch_claude_status` | Yes — derives from live Statuspage.io feed; fixture confirms real JSON flows through | FLOWING |
| `_derive_claude_status` | `incidents_raw` | `summary.get("incidents", [])` from parsed feed | Yes — fixture has real incident object with all required fields | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| D-01/D-05: resolved+degraded → kind=resolved | `_derive_claude_status(status_resolved_degraded.json)` | `{'severity':'resolved','kind':'resolved','incident_id':'inc-resolved-001','component':'Claude Code'}` | PASS |
| D-05: no incident → red | `_derive_claude_status(summary with incidents=[])` | `{'severity':'major','kind':'degraded','incident_id':None}` | PASS |
| D-01: operational component → None | `_derive_claude_status(summary with comp operational)` | `None` | PASS |
| D-03: render output format | `_claude_status_segment(resolved section)` | `'\x1b[32m resolved: Claude Code partial outage\x1b[0m'` | PASS |
| D-03: resolved maps to GREEN | `_claude_status_color('resolved') == GREEN` | `True` | PASS |
| D-04: Rule 1 wins | `_derive_claude_status(summary + active critical)` | `{'kind':'incident','severity':'critical'}` | PASS |
| D-04: Rule 2 wins | `_derive_claude_status(summary + maintenance)` | `{'kind':'maintenance'}` | PASS |
| D-10: never-crash on empty input | `printf '{}' \| python claude-statusline.py` | exit 0 | PASS |
| Full test suite | `python -m pytest tests/test_claude_status.py -q` | 246 passed, 30 subtests | PASS |
| Full repo suite | `python -m pytest tests/ -q` | 727 passed, 60 skipped, 0 failed | PASS |

### Probe Execution

Step 7c: SKIPPED (no probe-*.sh files declared for this phase; no conventional probe paths found).

### Requirements Coverage

No REQ-IDs mapped; verification is goal-backward against CONTEXT.md decisions D-01 through D-07 and D-10. All decisions verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `claude-statusline.py` | 2639, 2944 | `TBD` literal | Info | Both occurrences are in GSD task-list parsing comments describing how the parser handles roadmap placeholder tokens; they are pre-existing Phase 5 code not modified by Phase 07.1. Not unresolved debt markers. |

No blockers or warnings found.

### Human Verification Required

None. All must-haves are mechanically verifiable and confirmed.

### Gaps Summary

No gaps. All 14 must-haves are verified. The code review → fix → re-review → second-fix loop closed both critical findings (CR-01 and CR-02) plus all three warnings. The shipped code on `main` (commit `8a82db7`) correctly implements:

- D-01/D-05: lifecycle keyed to live component state; resolved-explained green vs unexplained red
- CR-01 fix (both iterations): genuinely-unexplained RED wins over resolved-green AND muted-explained, order-independent, via the `resolved_result` / `muted_result` deferred-verdict pattern
- CR-02 fix: render-time mute re-check binds to the specific `incident_id` baked into the cache section, not "any surviving tracked incident"
- D-03: GREEN + `_NF_GSD_DONE` + `resolved:` prefix at render time (RAW-in-cache)
- D-04: Rule 1 > Rule 2 > Rule 3 ordering preserved end to end
- D-06: muting wins in every state at both the store-prune layer (live_ids retention) and the render layer (explaining_id binding)
- D-07: `--status-incidents` STATE column shows active/resolved/dismissed/stale with `dismissed` precedence
- D-10: all six modified functions wrapped in outer try/except; render path zero-network; `printf '{}' | python claude-statusline.py` exits 0

---

_Verified: 2026-06-18_
_Verifier: Claude (gsd-verifier)_
